
                   Racal 3152B Arbitrary Waveform Generator
________________________________________________________________________________

  Introduction:

  This instrument driver provides programming support for Racal 3152B Arbitrary Waveform Generator.
  It contains functions for opening, configuring, taking measurements from, and
  closing the instrument.

________________________________________________________________________________

  Assumptions:

  To successfully use this module, the following conditions must be met:

  For GPIB instrument drivers:
         -  the instrument is connected to the GPIB.
         -  the GPIB address supplied to the initialize function must match the
            GPIB address of the instrument.

  For VXI instrument drivers:
         -  the instrument is installed in the VXI mainframe and you are
            using one of the following controller options:
                  Embedded controller
                  MXI
                  MXI2
                  GPIB-VXI
         -  the logical address supplied to the initialize function must
            match the logical address of the instrument.

  For RS-232 instrument drivers:
         -  the instrument is connected to the RS-232 interface.
         -  the COM port, baud rate, parity, and timeout supplied to the
            initialize function must match the settings of the instrument.

________________________________________________________________________________

  Error and Status Information:

  Each function in this instrument driver returns a status code that
  either indicates success or describes an error or warning condition.
  Your program should examine the status code from each call to an
  instrument driver function to determine if an error occurred.
  The general meaning of the status code is as follows:

       Value                  Meaning
       -------------------------------
       0                      Success
       Positive Values        Warnings
       Negative Values        Errors

  The description of each instrument driver function lists possible error
  codes and their meanings

________________________________________________________________________________

  How To Use This Document:

  Use this document as a programming reference manual.
  It describes each function in the

                   Racal 3152B Arbitrary Waveform Generator

  instrument.  The functions appear in alphabetical order, with a description
  of the function and its C syntax, a description of each parameter, and a list
  of possible error codes.

________________________________________________________________________________

Function Tree Layout:

   Class/Panel Name:                          Function Name:

      Initialize                              ri3152b_init
      Application Functions
         Example: Generate Seq. Waveform      ri3152b_example_generate_seq_waveform
      Configure Functions
         Set Active Channel                   ri3152b_set_active_channel
         Query Active Channel                 ri3152b_query_active_channel
         Output On/Off                        ri3152b_output
         Query Output                         ri3152b_query_output
         Output Shunt On/Off                  ri3152b_output_shunt
         Query Output Shunt                   ri3152b_query_output_shunt
         Output ECLT                          ri3152b_output_eclt
         Query Output ECLT                    ri3152b_query_output_eclt
         Output ECLT1                         ri3152b_output_ECLT1
         Query Output ECLT1                   ri3152b_query_output_ECLT1
         Set Load Impedance                   ri3152b_set_load_impedance
         Query Load Impedance                 ri3152b_query_load_impedance
         Set Ref Clock Source                 ri3152b_set_ref_clock_source
         Query Ref Clock Source               ri3152b_query_ref_clock_source
         Set Phase Offset                     ri3152b_set_phase_offset
         Query Phase Offset                   ri3152b_query_phase_offset
         Set Output Load Calibration          ri3152b_set_output_load_calibration
         Query Output Load Calibration        ri3152b_query_output_load_calibration
         Operating Mode
            Waveform Mode Selection           ri3152b_select_waveform_mode
            Standard Waveform Selection       ri3152b_select_standard_waveform
            Query Waveform Mode               ri3152b_mode_query
            Operating Mode                    ri3152b_operating_mode
            Query Operating Mode              ri3152b_query_operating_mode
         Waveform Parameters
            Set Frequency                     ri3152b_set_frequency
            Query Frequency                   ri3152b_frequency_query
            Set Amplitude                     ri3152b_set_amplitude
            Query Amplitude                   ri3152b_query_amplitude
            Set Offset                        ri3152b_set_offset
            Query Offset                      ri3152b_query_offset
         Standard Waveforms
            Sine Wave                         ri3152b_sine_wave
            Sine Phase                        ri3152b_set_sine_wave_phase
            Query Sine Phase                  ri3152b_sine_phase_query
            Sine Power                        ri3152b_set_sine_wave_power
            Query Sine Power                  ri3152b_sine_power_query
            Triangular Wave                   ri3152b_triangular_wave
            Triangle Phase                    ri3152b_set_triangle_phase
            Query Triangle Phase              ri3152b_triangle_phase_query
            Triangle Power                    ri3152b_set_triangle_power
            Query Triangle Power              ri3152b_triangle_power_query
            Square Wave                       ri3152b_square_wave
            Square Duty Cycle                 ri3152b_set_square_dcycle
            Query Square Duty Cycle           ri3152b_square_dcycle_query
            Pulse Wave                        ri3152b_pulse_wave
            Set Pulse Data                    ri3152b_set_pulse_data
            Query Pulse Data                  ri3152b_pulse_data_query
            Ramp Wave                         ri3152b_ramp_wave
            Set Ramp Data                     ri3152b_set_ramp_data
            Query Ramp Data                   ri3152b_ramp_data_query
            Sinc Wave                         ri3152b_sinc_wave
            Sinc NCycle                       ri3152b_set_sinc_ncycle
            Query Sinc NCycle                 ri3152b_sinc_ncycle_query
            Exponential Wave                  ri3152b_exponential_wave
            Exponential Exponent              ri3152b_set_exponential_exp
            Query Exponential Exponent        ri3152b_exponential_exp_query
            Gaussian Wave                     ri3152b_gaussian_wave
            Gauss Exponent                    ri3152b_set_gauss_exp
            Query Gauss Exponent              ri3152b_gauss_exp_query
            DC Signal                         ri3152b_dc_signal
            Query DC Amplitude                ri3152b_dc_amplitude_query
            Set DC Level                      ri3152b_set_dc_level
            Query DC Level                    ri3152b_query_dc_level
         Half Cycle Wave Functions
            Set Half Cycle Waveform           ri3152b_set_half_cycle_waveform
            Query Half Cycle Waveform         ri3152b_query_half_cycle_waveform
            Set Half Cycle Delay              ri3152b_set_half_cycle_delay
            Query Half Cycle Delay            ri3152b_query_half_cycle_delay
            Set Half Cycle Duty Cycle         ri3152b_set_half_cycle_duty_cycle
            Query Half Cycle Duty Cycle       ri3152b_query_half_cycle_duty_cycle
            Set Half Cycle Frequency          ri3152b_set_half_cycle_frequency
            Query Half Cycle Frequency        ri3152b_query_half_cycle_frequency
            Set Half Cycle Phase              ri3152b_set_half_cycle_phase
            Query Half Cycle Phase            ri3152b_query_half_cycle_phase
         Arbitrary Waveforms
            Load Segment Binary               ri3152b_load_segment_binary
            Define Arbitrary Segment          ri3152b_define_arb_segment
            Set Active Segment                ri3152b_set_active_segment
            Query Active Segment              ri3152b_query_active_segment
            Delete Segments                   ri3152b_delete_segments
            Output Arbitrary Waveform         ri3152b_output_arb_waveform
            Query Sample Clock Source         ri3152b_sclk_source_query
            Sampling Frequency                ri3152b_arb_sampling_freq
            Query Sampling Frequency          ri3152b_query_sampling_freq
         Sequence Programming
            Set Active Sequence               ri3152b_set_active_sequence
            Query Active Sequence             ri3152b_query_active_sequence
            Define Sequence                   ri3152b_define_sequence
            Define Sequence Adv.              ri3152b_define_sequence_adv
            Delete Sequence                   ri3152b_delete_sequence
            Output Sequence Waveform          ri3152b_output_sequence_waveform
            Query Sequence Mode               ri3152b_sequence_mode_query
            Load Sequence Binary              ri3152b_load_sequence_binary
            Set Sequence Sync Type            ri3152b_set_sequence_sync_type
            Query Sequence Sync Type          ri3152b_query_sequence_sync_type
         Trigger & Sync
            Burst Trigger Mode                ri3152b_burst_mode
            Set Burst Count                   ri3152b_set_burst_count
            Query Burst Count                 ri3152b_burst_count_query
            Trigger Source                    ri3152b_trigger_source
            Query Trigger Source Advance      ri3152b_trigger_source_adv_query
            Internal Trigger Rate             ri3152b_trigger_rate
            Query Trigger Rate                ri3152b_trigger_rate_query
            External Trigger Slope            ri3152b_trigger_slope
            Query Trigger Slope               ri3152b_trigger_slope_query
            Trigger Delay                     ri3152b_trigger_delay
            Query Trigger Delay               ri3152b_trigger_delay_query
            Set Trigger Delay Time            ri3152b_set_trigger_delay_time
            Query Trigger Delay Time          ri3152b_query_trigger_delay_time
            Set TTLT0 On/Off                  ri3152b_set_TTLT0_On_Off
            Query TTLT0 State                 ri3152b_query_TTLT0_state
            Set TTLT1 On/Off                  ri3152b_set_TTLT1_On_Off
            Query TTLT1 State                 ri3152b_query_TTLT1_state
            Set TTLT2 On/Off                  ri3152b_set_TTLT2_On_Off
            Query TTLT2 State                 ri3152b_query_TTLT2_state
            Set TTLT3 On/Off                  ri3152b_set_TTLT3_On_Off
            Query TTLT3 State                 ri3152b_query_TTLT3_state
            Set TTLT4 On/Off                  ri3152b_set_TTLT4_On_Off
            Query TTLT4 State                 ri3152b_query_TTLT4_state
            Set TTLT5 On/Off                  ri3152b_set_TTLT5_On_Off
            Query TTLT5 State                 ri3152b_query_TTLT5_state
            Set TTLT6 On/Off                  ri3152b_set_TTLT6_On_Off
            Query TTLT6 State                 ri3152b_query_TTLT6_state
            Set TTLT7 On/Off                  ri3152b_set_TTLT7_On_Off
            Query TTLT7 State                 ri3152b_query_TTLT7_state
            Output Trigger                    ri3152b_output_trigger
            Set Output Trigger Source         ri3152b_set_output_trigger_source
            Query Trigger Source              ri3152b_trigger_source_query
            Output Sync Pulse                 ri3152b_output_sync
            Output Sync Setup                 ri3152b_output_sync_setup
            Output Sync Enable                ri3152b_output_sync_enable
            Query Sync Source                 ri3152b_sync_source_query
            Output Sync Position              ri3152b_output_sync_pos
            Query Output Sync                 ri3152b_output_sync_query
            Query Sync Position               ri3152b_sync_pos_query
            Immediate Trigger                 ri3152b_immediate_trigger
            Set Trigger Level                 ri3152b_set_trigger_level
            Query Trigger Level               ri3152b_trigger_level_query
            Output Sync Width                 ri3152b_output_sync_width
            Query Sync Width                  ri3152b_sync_width_query
            Set Trigger Delay State           ri3152b_trig_delay_state
            Query Trigger Delay State         ri3152b_trig_delay_state_query
            Set Gate Mode                     ri3152b_set_gate_mode
            Query Gate Mode                   ri3152b_query_gate_mode
            Set Re-Trigger Delay State        ri3152b_set_re_trigger_delay_state
            Query Re-Trigger Delay State      ri3152b_query_re_trigger_delay_state
            Set Re-Trigger Time               ri3152b_set_re_trigger_time
            Query Re-Trigger Time             ri3152b_query_re_trigger_time
         Common Modulation Functions
            Set Modulation Type               ri3152b_set_modulation_type
            Query Modulation Type             ri3152b_query_modulation_type
            Set Carrier Frequency             ri3152b_set_carrier_frequency
            Query Carrier Frequency           ri3152b_query_carrier_frequency
            Set Carrier Baseline              ri3152b_set_carrier_baseline
            Query Carrier Baseline            ri3152b_query_carrier_baseline
         Sweep Functions
            Sweep Function                    ri3152b_sweep_function
            Query Sweep Function              ri3152b_sweep_func_query
            Set Sweep Time                    ri3152b_set_sweep_time
            Query Sweep Time                  ri3152b_query_sweep_time
            Set Sweep Step                    ri3152b_set_sweep_step
            Query Sweep Step                  ri3152b_query_sweep_step
            Set Sweep Direction               ri3152b_set_sweep_direction
            Query Sweep Direction             ri3152b_query_sweep_direction
            Set Sweep Spacing                 ri3152b_set_sweep_spacing
            Query Sweep Spacing               ri3152b_query_sweep_spacing
            Set Sweep Frequency Start         ri3152b_set_sweep_freq_start
            Query Sweep Frequency Start       ri3152b_query_sweep_freq_start
            Set Sweep Frequency Stop          ri3152b_set_sweep_freq_stop
            Query Sweep Frequency Stop        ri3152b_query_sweep_freq_stop
            Set Sweep Frequency Raster        ri3152b_set_sweep_freq_raster
            Query Sweep Frequency Raster      ri3152b_query_sweep_freq_raster
            Set Sweep Frequency Marker        ri3152b_set_sweep_freq_marker
            Query Sweep Frequency Marker      ri3152b_query_sweep_freq_marker
         Frequency Modulation (FM)
            Set FM Frequency Deviation        ri3152b_set_FM_frequency_deviation
            Query FM Frequency Deviation      ri3152b_query_FM_frequency_deviation
            Set FM Modulation Waveform        ri3152b_set_FM_modulation_waveform
            Query FM Modulation Waveform      ri3152b_query_FM_modulation_waveform
            Set FM Modulation Frequency       ri3152b_set_FM_modulation_frequency
            Query FM Modulation Frequency     ri3152b_query_FM_modulation_frequency
            Set FM Modulation SCLK            ri3152b_set_FM_modulation_SCLK
            Query FM Modulation SCLK          ri3152b_query_FM_modulation_SCLK
            Set FM Marker                     ri3152b_set_FM_marker
            Query FM Marker                   ri3152b_query_FM_marker
            Load Arb FM data                  ri3152b_load_arb_FM_data
            Load FM Arb Waveform From File    ri3152b_load_FM_Arb_waveform_from_file
         Amplitude Modulation (AM)
            Amplitude Modulation              ri3152b_amplitude_modulation
            Query Amplitude Modulation        ri3152b_amplitude_modulation_query
            Set AM Modulation Waveform        ri3152b_set_AM_modulation_waveform
            Query AM Modulation Waveform      ri3152b_query_AM_modulation_waveform
            Arb.Amplitude Modulation          ri3152b_ArbAmplitudeModulation
         Frequency Shift Keying (FSK)
            Set FSK Shifted Frequency         ri3152b_set_FSK_shifted_frequency
            Query FSK Shifted Frequency       ri3152b_query_FSK_shifted_frequency
            Set FSK Baud                      ri3152b_set_FSK_baud
            Query FSK Baud                    ri3152b_query_FSK_baud
            Set FSK Marker                    ri3152b_set_FSK_marker
            Query FSK Marker                  ri3152b_query_FSK_marker
            Load FSK Data File                ri3152b_load_FSK_data_file
            Load FSK Data Array               ri3152b_load_FSK_data_array
         ASK Modulation
            Set ASK Start Amplitude           ri3152b_set_ASK_start_amplitude
            Query ASK Start Amplitude         ri3152b_query_ASK_start_amplitude
            Set ASK Shifted Amplitude         ri3152b_set_ASK_shifted_amplitude
            Query ASK Shifted Amplitude       ri3152b_query_ASK_shifted_amplitude
            Set ASK Baud                      ri3152b_set_ASK_baud
            Query ASK Baud                    ri3152b_query_ASK_baud
            Set ASK Marker                    ri3152b_set_ASK_marker
            Query ASK Marker                  ri3152b_query_ASK_marker
            Load ASK Data File                ri3152b_load_ASK_data_file
            Load ASK Data Array               ri3152b_load_ASK_data_array
         Phase Shift Key Modulation
            Set PSK Start Phase               ri3152b_set_PSK_start_phase
            Query PSK Start Phase             ri3152b_query_PSK_start_phase
            Set PSK Shifted Phase             ri3152b_set_PSK_shifted_phase
            Query PSK Shifted Phase           ri3152b_query_PSK_shifted_phase
            Set PSK Rate                      ri3152b_set_PSK_rate
            Query PSK Rate                    ri3152b_query_PSK_rate
            Set PSK Marker                    ri3152b_set_PSK_marker
            Query PSK Marker                  ri3152b_query_PSK_marker
            Load PSK Data File                ri3152b_load_PSK_data_file
            Load PSK Data Array               ri3152b_load_PSK_data_array
         Frequency Hopping Modulation
            Set FHOP Dwell Mode               ri3152b_set_FHOP_dwell_mode
            Query FHOP Dwell Mode             ri3152b_query_FHOP_dwell_mode
            Set FHOP Dwell Time               ri3152b_set_FHOP_dwell_time
            Query FHOP Dwell Time             ri3152b_query_FHOP_dwell_time
            Set FHOP Marker                   ri3152b_set_FHOP_marker
            Query FHOP Marker                 ri3152b_query_FHOP_marker
            Load FHOP Data File               ri3152b_load_FHOP_data_file
            Load Freq Hop Fixed Data Array    ri3152b_load_freq_hop_fixed_data_array
            Load Freq Hop Var. Data Array     ri3152b_load_freq_hop_var_data_array
         Amplitude Hopping Modulation
            Set AHOP Dwell Mode               ri3152b_set_AHOP_dwell_mode
            Query AHOP Dwell Mode             ri3152b_query_AHOP_dwell_mode
            Set AHOP Dwell Time               ri3152b_set_AHOP_dwell_time
            Query AHOP Dwell Time             ri3152b_query_AHOP_dwell_time
            Set AHOP Marker                   ri3152b_set_AHOP_marker
            Query AHOP Marker                 ri3152b_query_AHOP_marker
            Load AHOP Data File               ri3152b_load_AHOP_data_file
            Load AHOP Fixed Data Array        ri3152b_load_AHOP_fixed_data_array
            Load AHOP Var. Data Array         ri3152b_load_AHOP_var_data_array
         3D Modulation
            Set 3D Marker                     ri3152b_set_3D_marker
            Query 3D Marker                   ri3152b_query_3D_marker
            Set 3D Sample Rate                ri3152b_set_3D_sample_rate
            Query 3D Sample Rate              ri3152b_query_3D_sample_rate
         Digital Pulse Output
            Set Pulse Delay                   ri3152b_set_pulse_delay
            Query Pulse Delay                 ri3152b_query_pulse_delay
            Set Double Pulse Delay            ri3152b_set_double_pulse_delay
            Query Double Pulse Delay          ri3152b_query_double_pulse_delay
            Set Double Pulse State On/Off     ri3152b_set_double_pulse_state
            Query Double Pulse State          ri3152b_query_double_pulse_state
            Set Pulse High Level              ri3152b_set_pulse_high_level
            Query Pulse High Level            ri3152b_query_pulse_high_level
            Set Pulse Low Level               ri3152b_set_pulse_low_level
            Query Pulse Low Level             ri3152b_query_pulse_low_level
            Set Pulse High Time               ri3152b_set_pulse_high_time
            Query Pulse High Time             ri3152b_query_pulse_high_time
            Set Pulse Polarity                ri3152b_set_pulse_polarity
            Query Pulse Polarity              ri3152b_query_pulse_polarity
            Set Pulse Period                  ri3152b_set_pulse_period
            Query Pulse Period                ri3152b_query_pulse_period
            Set Pulse Rise Time               ri3152b_set_pulse_rise_time
            Query Pulse Rise Time             ri3152b_query_pulse_rise_time
            Set Pulse Fall Time               ri3152b_set_pulse_fall_time
            Query Pulse Fall Time             ri3152b_query_pulse_fall_time
         Phase
            Phase Master Mode                 ri3152b_phase_master
            Phase Slave Mode                  ri3152b_phase_slave
            Query Phase                       ri3152b_phase_query
            Phase Lock Null                   ri3152b_phase_lock_null
         3152B Phase Lock Loop
            Phase Lock On/Off                 ri3152b_phase_lock_loop
            Phase Lock Phase Adjust           ri3152b_pll_phase
            Phase Lock Fine Phase Adjust      ri3152b_pll_fine_phase
            Query Phase Lock Loop             ri3152b_pll_query
            Set Phase2 Source                 ri3152b_set_phase2_source
            Query Phase2 Source               ri3152b_query_phase2_source
         Multi-Instr. Synchronization
            Set Couple Mode                   ri3152b_set_couple_mode
            Query Couple Mode                 ri3152b_query_couple_mode
            Set Couple Delay                  ri3152b_set_couple_delay
            Query Couple Delay                ri3152b_query_couple_delay
            Set Couple On/Off                 ri3152b_set_couple
            Query Couple                      ri3152b_query_couple
            Set Couple Path                   ri3152b_set_couple_path
            Query Couple Path                 ri3152b_query_couple_path
            Insert Slave                      ri3152b_insert_slave
            Delete Slave                      ri3152b_delete_slave
         Filter                               ri3152b_filter
         Query Filter                         ri3152b_query_filter
         Set Filter State On/Off              ri3152b_set_filter_state
         Query Filter State                   ri3152b_query_filter_state
         Counter
            Set Counter Function              ri3152b_set_counter_function
            Query Counter Function            ri3152b_query_counter_function
            Set Counter Display Mode          ri3152b_set_counter_display_mode
            Query Counter Display Mode        ri3152b_query_counter_display_mode
            Set Counter Gate Time             ri3152b_set_counter_gate_time
            Query Counter Gate Time           ri3152b_query_counter_gate_time
            Reset Counter                     ri3152b_reset_counter
            Read Counter Value                ri3152b_read_counter_value
      Action/Status Functions
         Clear instrument                     ri3152b_clear
         Trigger instrument                   ri3152b_trigger
         Poll Instrument                      ri3152b_poll
         Wait for OPC                         ri3152b_WaitForOPC
         Status Query                         ri3152b_status_query
         IEEE 488.2 Status
            Read Status Byte                  ri3152b_read_status_byte
            Set SRE Register                  ri3152b_set_SRE
            Read SRE Register                 ri3152b_read_SRE
            Read ESR Register                 ri3152b_read_ESR
            Set ESE Register                  ri3152b_set_ESE
            Read ESE Register                 ri3152b_read_ESE
      Data Operations
         Load Arbitrary Data                  ri3152b_load_arb_data
         Query Share Memory                   ri3152b_share_memory_query
         Load/Shift Arbitrary Data            ri3152b_load_and_shift_arb_data
         Read ASCII data file                 ri3152b_load_ascii_file
         Load WaveCAD File                    ri3152b_load_wavecad_file
         Load WaveCAD Waveform File           ri3152b_load_wavecad_wave_file
         Set Shared Flag On/Off               ri3152b_set_shared_flag_on_off
         Query Shared Flag                    ri3152b_query_shared_flag
      Utility Functions
         Change Mode                          ri3152b_change_mode
         Format Border                        ri3152b_format_border
         Query Format Border                  ri3152b_format_border_query
         Format Wave                          ri3152b_format_wave
         Query Format Wave                    ri3152b_format_wave_query
         Set Wave Resolution                  ri3152b_set_wave_resolution
         Query Wave Resolution                ri3152b_query_wave_resolution
         Format Instrument                    ri3152b_format_instrument
         Query Format Instrument              ri3152b_query_format_instrument
         Reset                                ri3152b_reset
         Operation Complete                   ri3152b_opc
         Self-Test                            ri3152b_self_test
         Revision Query                       ri3152b_revision_query
         Version Query                        ri3152b_version_query
         Option Query                         ri3152b_option_query
         Error Query                          ri3152b_error_query
         Error Message                        ri3152b_error_message
      Instrument I/O
         Write Instrument Data                ri3152b_write_instrument_data
         Query Instrument Data                ri3152b_query_instrument_data
      Close                                   ri3152b_close
________________________________________________________________________________

  Racal 3152B Arbitrary Waveform Generator

      Instrument Name:  Racal Instruments 3152B Arbitrary
                        Waveform Generator
      
      Description:      This instrument driver provides a range of
                        functions which can be used to operate the
                        3152B Arbitrary Waveform Generators
      
      Functions and Classes:
      
        (1)     Initialize:
      
                Initializes the timer/counter and associates an
                "instrument handle" with the instrument.  The
                instrument handle is used with all of the other
                functions within this driver.  THIS FUNCTION MUST
                BE CALLED BEFORE ANY OF THE OTHER FUNCTIONS OF THIS
                DRIVER ARE USED
      
                All functions in this driver may be used with a 3152B.
      
        (2)     Application Functions
      
                Functions in this class combine several of the lower
                level functions of this driver to produce a complete
                measurement value.  These functions illustrate the
                use of the other functions within the driver.
      
        (3)     Configure Functions
      
                Functions in this class configure the function
                generator to output specific waveforms, including
                standard and arbitrary waveform types.
      
        (4)     Action/Status Functions
      
                Functions in this class provide the capability of
                turning the generator on or off.  This class also
                includes a variety of functions used to query the
                present setup of the arbitrary waveform generator.
      
        (5)     Data Operations
      
                This class includes the functions used to define and
                download arbitrary waveform data blocks
      
        (6)     Utility Functions
      
                Functions in this class include the functions which
                are common to all VXIplug&play drivers.  These
                functions reset, perform a self-test, read the
                instrument and driver revision, perform an error
                query, and convert a numeric error code to a
                descriptive error message.
      
        (7)     Close
      
                This function closes the driver and releases the
                system resources (memory) which were allocated when
                the instrument was initialized.  This function should
                be called at the end of the application program before
                it terminates.
      
      
________________________________________________________________________________

              The following functions are in alphabetical order.

________________________________________________________________________________

  ri3152b_amplitude_modulation

      ViStatus ri3152b_amplitude_modulation (ViSession instrHandle,
                                             ViInt16 AMPercent,
                                             ViInt32 AMFrequency);

  Purpose

      Panel Name:    Amplitude Modulation
      
      Description:   Select the internal modulation depth in percent
                     and the carrier frequency.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      AMPercent

          Variable Type       ViInt16

          Control Name:  AM Percent
          
          Description:   Select the Internal Modulation depth in percent.
          
          Valid Range:
           0 to 100    (3152B)
           1 to 200  (3152A)
          
          Default:  50

      AMFrequency

          Variable Type       ViInt32

          Control Name:  Amplitude Modulation
          
          Description:   Select the carrier frequency.
          
          Valid Range:
           1 to 1.0E6 (3152B)
           10 to 500  (3152A)
          
          Default:  100

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_amplitude_modulation_query

      ViStatus ri3152b_amplitude_modulation_query (ViSession instrHandle,
                                                   ViPInt16 AMValue,
                                                   ViPInt32 AMFrequency);

  Purpose

      Panel Name:    Query Amplitude Modulation
      
      Description:   Returns the amplitude modulation data.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      AMValue

          Variable Type       ViInt16 (passed by reference)

          Control Name:  AM Value
          
          Description:   Returns the AM value.

      AMFrequency

          Variable Type       ViInt32 (passed by reference)

          Control Name:  AM Frequency
          
          Description:   Returns the AM internal frequency.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_arb_sampling_freq

      ViStatus ri3152b_arb_sampling_freq (ViSession instrHandle,
                                          ViReal64 samplingClock);

  Purpose

      Panell Name:   Sampling Frequency
      
      Description:   Select the sampling frequency for the
                     arbitrary waveform.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      samplingClock

          Variable Type       ViReal64

          Control Name:  Sampling Clock
          
          Description:   Selects the Sampling Clock for selected segment.
          
          Valid Range:
           10.0E-6 to 250.0E6   (3152B)
           100.0E-3 to 100.0E6  (3152A)
          
          Default: 1.0E6

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_ArbAmplitudeModulation

      ViStatus ri3152b_ArbAmplitudeModulation (ViSession instrHandle,
                                               ViReal64 amplDepth,
                                               ViReal64 carrierFreq,
                                               ViReal64 modFreq,
                                               ViInt16 segmentNumber,
                                               ViPInt32 segmLength,
                                               ViPReal64 sampleClock,
                                               ViInt16 ARBAMModulation);

  Purpose

      Panel Name:    Arb. Amplitude Modulation
      
      Description:   Downloads arbitrary amplitude modulation to the
                     instrument.
                     Sets function mode to mode Arbitrary.
                     Sets sample clock.
      
      Notes:         Not valid for RI3152A, RI3152, RI3151A

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      amplDepth

          Variable Type       ViReal64

          Control Name:  Arbitrary Amplitude Depth
          
          Description:   Arbitrary Amplitude Depth
          
          Valid Range:
           100 to 200
          
          Default:  100

      carrierFreq

          Variable Type       ViReal64

          Control Name:  Carrier Frequency
          
          Description:   Carrier Frequency for Arb. Amplitude Modulation
          
          Valid Range:
           10 to 100.0E6
          
          Default:  1.0E6

      modFreq

          Variable Type       ViReal64

          Control Name: Modulation Frequency
          
          Description:   Modulation Frequency for Arbitrary Amplitude
                         modulation.
          
          Valid Range:
           10e-3 to 1.0E6
          
          Default:  10E3

      segmentNumber

          Variable Type       ViInt16

          Control Name:  Segment Number
          
          Description:   Select the segment number.
          
          Valid Range:
           1 to 10000
          
          Default:  1

      segmLength

          Variable Type       ViInt32 (passed by reference)

          Control Name:      Segment length.
          
          Description:       Returned segment length.
          
          

      sampleClock

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Sample Clock
          
          Description:   Returned sample clock

      ARBAMModulation

          Variable Type       ViInt16

          Control Name:  ARB AM Modulation Waveform
          
          Description:   Pass the waveform you want to use for arbitrary
                         amplitude modulation.
          
          Valid Range:   RI3152B_ARB_AM_WAVE_SINE - (0)
                         RI3152B_ARB_AM_WAVE_TRI  - (1)
                         RI3152B_ARB_AM_WAVE_SQU  - (2)
                         RI3152B_ARB_AM_WAVE_RAMP - (3)
          
          Default:       RI3152B_ARB_AM_WAVE_SINE

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_burst_count_query

      ViStatus ri3152b_burst_count_query (ViSession instrHandle,
                                          ViPInt32 burstCount);

  Purpose

      Panel Name:    Burst Count Query
      
      Description:   Returns the current value of the burst count.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      burstCount

          Variable Type       ViInt32 (passed by reference)

          Control Name:  Burst Count
          
          Description:   Reads the current burst count.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_burst_mode

      ViStatus ri3152b_burst_mode (ViSession instrHandle,
                                   ViInt32 number_ofCycles);

  Purpose

      Panel Name:    Burst Trigger Mode
      
      Description:   Selects the Burst Mode of operation and the
                     Number of Cycles to produce when the waveform
                     generator is triggered.  Use the Trigger Source
                     function to select the trigger trigger source.
      
                     If the Internal Trigger Source is selected,
                     use the Trigger Rate function to program
                     the internal trigger rate.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      number_ofCycles

          Variable Type       ViInt32

          Control Name:  Number of Cycles
          
          Description:   Select the number of cycles to be output per
                         burst.
          
          Valid Range:   1 to 1000000
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_change_mode

      ViStatus ri3152b_change_mode (ViSession instrHandle, ViBoolean mode);

  Purpose

      Panel Name:    Change Mode
      
      Description:   Turns the instrument mode to either normal or
                     fast mode.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      mode

          Variable Type       ViBoolean

          Control Name:  Mode
          
          Description:   Turns the instrument mode to either normal or
                         fast mode.
          
          Valid Range:   RI3152B_MODE_FAST -   (1)
                         RI3152B_MODE_NORMAL - (0)
          
          Default:       RI3152B_MODE_NORMAL

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_clear

      ViStatus ri3152b_clear (ViSession instrHandle);

  Purpose

      Panel Name:    Clear
      
      Description:   Performs a VXI word serial clear command. This
                     results in clearing the input and output
                     buffer's of the arbitrary waveform generator.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_close

      ViStatus ri3152b_close (ViSession instrHandle);

  Purpose

      Panel Name:    Close
      
      Description:   Closes the instrument and deallocates the
                     resources allocated by the call to the
                     initialization function.  This should be
                     called once for every instrument handle returned
                     by the initialize function.  This should be
                     called prior to terminating the application
                     program.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_dc_amplitude_query

      ViStatus ri3152b_dc_amplitude_query (ViSession instrHandle,
                                           ViPInt16 DCAmplitude);

  Purpose

      Panel Name:    Query DC Amplitude
      
      Description:   Returns the current DC Amplitude value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      DCAmplitude

          Variable Type       ViInt16 (passed by reference)

          Control Name:   DC Amplitude
          
          Description:    Reads the current DC Amplitude.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_dc_signal

      ViStatus ri3152b_dc_signal (ViSession instrHandle,
                                  ViInt16 percentAmplitude);

  Purpose

      Panel Name:    DC Signal
      
      Description:   Sets the mode of operation to produce a DC
                     signal at the most recently programmed
                     amplitude.  This function provides the
                     ability to program between -100% and +100%
                     of the programmed amplitude.
      
                     Note:  The amplitude may be changed using
                     the ri3152b_set_amplitude() function.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      percentAmplitude

          Variable Type       ViInt16

          Control Name:  Percent_Amplitude
          
          Description:   Select a percentage of the amplitude.
          
          Valid Range:   -100% to 100%
          
          Default:       100%

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_define_arb_segment

      ViStatus ri3152b_define_arb_segment (ViSession instrHandle,
                                           ViInt16 segmentNumber,
                                           ViInt32 segmentSize);

  Purpose

      Control Name:  Define Arbitrary Segment
      
      Description:   Select the segment number and the segment
                     size ( number of points) for a waveform
                     segment.
      
                     NOTE: the segment size must match the number
                           of the data points in Input Data Points
                           panel.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      segmentNumber

          Variable Type       ViInt16

          Control Name:  Segment Number
          
          Description:   Select the segment number.
          
          Valid Range:
           1 to 10000 (3152B Modern mode)
           1 to 4096 (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:       1

      segmentSize

          Variable Type       ViInt32

          Control Name:  Segment Size ( Number of Points )
          
          Description:   Select the segment size.  It should match
                         the number of data points in the Data Points
                         panel.
          
          Valid Range:
           /* for 3152A */
           10 to 64536 (64K memory)
           10 to 262144 (256K memory)
           10 to 523288 (512K memory)
          
           /* for 3152B  Modern mode */
           16 to 1E6  (1M memory)
           16 to 2E6  (2M memory)
           16 to 4E6  (4M memory)
          
           /* for 3152B Legacy mode */
           10 to 500000
          
          Default:
           16  (3152B Modern mode)
           10  (3152B Legacy mode)
           10  (3152A)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_define_sequence

      ViStatus ri3152b_define_sequence (ViSession instrHandle,
                                        ViInt16 number_ofSteps,
                                        ViInt16 _VI_FAR segmentNumbers[],
                                        ViInt32 _VI_FAR repeatSegment[]);

  Purpose

      Panel Name:     Define Sequence
      
      Description:    Defines a Sequence and its number of
                      steps, segment numbers and number of times
                      each segment is repeated.
      
                      NOTE: the number of steps must match the number
                      of the elements in the segment numbers [] and
                      repeat segment [].

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      number_ofSteps

          Variable Type       ViInt16

          Control Name:  Number of steps
          
          Description:   Select the number of steps in a sequence.
          
          Valid Range:   1 to 4096
          
          Default:       1

      segmentNumbers

          Variable Type       ViInt16 []

          Control Name:   Segment Numbers []
          
          Description:    Each array element is the arbitrary waveform
                          segment number to generate for that step of
                          the sequence.  The number of cycles produced
                          of the segment is dictated by the corresponding
                          Repeat Segment [] array element.
          
          Valid Range:
           1 to 10000 (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:   1

      repeatSegment

          Variable Type       ViInt32 []

          Control Name:   Repeat Segment []
          
          Description:    Sets the number of times the arbitrary waveform
                          segment is generated for the sequence.  The
                          corresponding Segment Numbers [] element
                          determines which arbitrary segment is being
                          repeated.  This element indicates the number
                          of times the segment is repeated before moving
                          to the next step in the sequence.
          
          Valid Range:    1 to 1E6
          
          Default:        1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_define_sequence_adv

      ViStatus ri3152b_define_sequence_adv (ViSession instrHandle,
                                            ViInt16 number_ofSteps,
                                            ViInt16 _VI_FAR segmentNumbers[],
                                            ViInt32 _VI_FAR repeatSegment[],
                                            unsigned char mode[]);

  Purpose

      Panel Name:     Define Sequence Adv.
      
      Description:    Defines a Sequence and its number of
                      steps, segment numbers, number of times
                      each segment is repeated and advance mode.
      
                      NOTE: the number of steps must match the number
                      of the elements in the segment numbers [],
                      repeat segment [] and mode [] .

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      number_ofSteps

          Variable Type       ViInt16

          Control Name:  Number of steps
          
          Description:   Select the number of steps in a sequence.
          
          Valid Range:   1 to 4096
          
          Default:       1

      segmentNumbers

          Variable Type       ViInt16 []

          Control Name:   Segment Numbers []
          
          Description:    Each array element is the arbitrary waveform
                          segment number to generate for that step of
                          the sequence.  The number of cycles produced
                          of the segment is dictated by the corresponding
                          Repeat Segment [] array element.
          
          Valid Range:
           1 to 10000 (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:   1

      repeatSegment

          Variable Type       ViInt32 []

          Control Name:   Repeat Segment []
          
          Description:    Sets the number of times the arbitrary waveform
                          segment is generated for the sequence.  The
                          corresponding Segment Numbers [] element
                          determines which arbitrary segment is being
                          repeated.  This element indicates the number
                          of times the segment is repeated before moving
                          to the next step in the sequence.
          
          Valid Range:    1 to 1E6
          
          Default:        1

      mode

          Variable Type       unsigned char []

          Control Name:   Mode []
          
          Description:    Pass the array of advance mode from which you
                          want to create a new arbitrary sequence.  The
                          array must have at least as many elements as the
                          value you specify in the Number of Steps
                          parameter.
          
          
          
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_delete_segments

      ViStatus ri3152b_delete_segments (ViSession instrHandle,
                                        ViInt16 segmentNumber,
                                        ViBoolean deleteAllSegments);

  Purpose

      Panel Name:  Delete Segments
      
      Description:   Select a particular segment or all segments to
                     be deleted.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      segmentNumber

          Variable Type       ViInt16

          Control Name:  Segment Number
          
          Description:   Select the segment number to be deleted.
          
          Valid Range:
           1 to 10E3  (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:   1
          

      deleteAllSegments

          Variable Type       ViBoolean

          Control Name:  Delete All Segments?
          
          Description:   Selecting YES will delete all the segments.
          
          Valid Range:   0 or 1 (NO or YES)
          
          Default:       0
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_delete_sequence

      ViStatus ri3152b_delete_sequence (ViSession instrHandle,
                                        ViInt16 sequenceNumber);

  Purpose

      Panel Name:    Delete Sequence
      
      Description:   Deletes all steps from any sequence which
                     may have been previously defined.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sequenceNumber

          Variable Type       ViInt16

          Control Name:  Sequence Number
          
          Description:   Select the sequence number to be deleted.
          
          Valid Range:   0 to 4096
          
          Default:       1 (0 means delete all sequences)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_delete_slave

      ViStatus ri3152b_delete_slave (ViSession instrHandle);

  Purpose

      Panel Name:   Delete Slave
      
      Description:  This function deletes all slaves from a
                    synchronize multi-instruments system list.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_error_message

      ViStatus ri3152b_error_message (ViSession instrHandle,
                                      ViStatus errorReturnValue,
                                      ViChar _VI_FAR errorMessage[]);

  Purpose

      Panel Name:    Error Message
      
      Description:   Converts a numeric error code returned by one
                     of the functions of this driver into a
                     descriptive error message string.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

      errorReturnValue

          Variable Type       ViStatus

          Control Name:  Error return code
          
          Description:   Accepts the error code for returned
                         by one of the functions in this
                         instrument driver.
          

      errorMessage

          Variable Type       ViChar []

          Control Name:  Error Message
          
          Description:   Upon return from the function,
                         holds a text error message which
                         corresponds to the error code.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_error_query

      ViStatus ri3152b_error_query (ViSession instrHandle, ViPInt32 error,
                                    ViChar _VI_FAR errorMessage[]);

  Purpose

      Panel Name:    Error Query
      
      Description:   Queries the instrument for any errors it has
                     detected.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

      error

          Variable Type       ViInt32 (passed by reference)

           Control Name:  Error?
          
           Description:   The indication whether an error has occured
                          0 = no error detected by the instrument
                          1 = error detected by the instrument
          

      errorMessage

          Variable Type       ViChar []

          Control Name:  Error Message
          
          Description:   The error message for the error returned
                         by the instrument.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_example_generate_seq_waveform

      ViStatus ri3152b_example_generate_seq_waveform (ViSession instrHandle);

  Purpose

      Panel Name:    Example:  Generate Sequence Waveform
      
      Description:   This example function shows how to user the
                     functions of the driver to generate an arbitrary
                     waveform.  The waveform is constructed by
                     generating a sequence of segments.  This example
                     shows the order in which the segments are
                     defined, loaded with data, and sequenced to
                     generate a (potentially) complex waveform.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_exponential_exp_query

      ViStatus ri3152b_exponential_exp_query (ViSession instrHandle,
                                              ViPReal64 exponent);

  Purpose

      Panel Name:    Query Exponential Exponent
      
      Description:   Returns the current exponential wave exponent
                     value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      exponent

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Exponent
          
          Description:    Reads the current exponential wave exponent.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_exponential_wave

      ViStatus ri3152b_exponential_wave (ViSession instrHandle,
                                         ViReal64 frequency,
                                         ViReal64 amplitude, ViReal64 offset,
                                         ViInt16 exponent);

  Purpose

      Panel Name:    Exponential Wave
      
      Description:   Select Frequency, Amplitude, Offset and the
                     Exponent.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequency

          Variable Type       ViReal64

          Control Name:  Frequency
          
          Description:   Selects the frequency of the Exponential Wave.
          
          Valid Range:
          10.0E-3 to 100.0E6  (3152B)
          100.0E-6 to 1.0E6   (3152A)
          
          Default:  1.0E6
          
          
          

      amplitude

          Variable Type       ViReal64

          Control Name:  Amplitude
          
          Description:   Selects the amplitude (Volts peak-to-peak).
          
          Valid Range:   10.0E-3 to 16.0 V
          
          Default:       5.0 V
          
          Note:          The following relationship must hold between the
                         amplitude and the offset:
          
                               (Amplitude / 2) + ABS(offset) <= 8.0

      offset

          Variable Type       ViReal64

          Control Name:  Offset
          
          Description:   Programs the offset for the presently
                         selected waveform.
          
          Valid Range:
          -7.992 to +7.992   (3152B)
          -7.19 to +7.19     (3152A)
          
          Default:   0.0

      exponent

          Variable Type       ViInt16

          Control Name:  Exponent
          
          Description:   Select the exponent for the Exponential Wave.
          
          Valid Range:
           -100 to 100 (3152B)
           -200 to 200 (3152A)
          
          Default: -10
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_filter

      ViStatus ri3152b_filter (ViSession instrHandle, ViInt16 filterSwitch);

  Purpose

      Control Name:  Filter
      
      Description:   Turn one of the Low Pass Filters on, or turn
                     the Filter off.
      
                     Note:  This function should not be used if
                            the "Standard Waveform" mode is
                            in effect and the Sine Wave is
                            being output.  The filters are
                            automatically programmed by the
                            Standard Sine Wave.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      filterSwitch

          Variable Type       ViInt16

          Control Name:  Filter Switch
          
          Description:   Turns one of the Low Pass Filters on, or turns
                         the Filter off.
          
                         Note: Low Pass Filters have no effect on the
                               Sine Wave.
          
          Valid Range:
           RI3152B_FILTER_OFF    (0) - Turn Filter Off
           RI3152B_FILTER_20MHZ  (1) - Use 20 MHz Filter
           RI3152B_FILTER_25MHZ  (2) - Use 25 MHz Filter
           RI3152B_FILTER_50MHZ  (3) - Use 50 MHz Filter
           RI3152B_FILTER_60MHZ  (4) - Use 60 MHz Filter
           RI3152B_FILTER_120MHZ (5) - Use 120 MHz Filter
          
          Default:  RI3152B_FILTER_OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_format_border

      ViStatus ri3152b_format_border (ViSession instrHandle,
                                      ViBoolean format);

  Purpose

      Panel Name:    Format Border
      
      Description:   Specifies the border to be formatted in normal
                     swapped mode.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      format

          Variable Type       ViBoolean

          Control Name:  Format
          
          Description:   Specifies the border to be formatted in normal
                         swapped mode.
          
          Valid Range:   RI3152B_FORMAT_NORMAL -  (0)
                         RI3152B_FORMAT_SWAP -    (1)
          
          Default:       RI3152B_FORMAT_NORMAL

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_format_border_query

      ViStatus ri3152b_format_border_query (ViSession instrHandle,
                                            ViChar _VI_FAR formatBorder[]);

  Purpose

      Panel Name:    Query Format Border
      
      Description:   Reads the value of format border.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      formatBorder

          Variable Type       ViChar []

          Control Name:      Format Border
          
          Description:       Returns the value of format border.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_format_instrument

      ViStatus ri3152b_format_instrument (ViSession instrHandle,
                                          ViInt16 formatInstrument);

  Purpose

      Panel Name:   Format Instrument
      
      Description:  Sets instrument to Legacy or Modern model.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      formatInstrument

          Variable Type       ViInt16

          Control Name:  Format Instrument
          
          Description:   Sets instrument to Legacy or Modern model.
          
          Valid Range:   RI3152B_FORMAT_INSTR_LEG - (0)
                         RI3152B_FORMAT_INSTR_MOD - (1)
          
          Default:       RI3152B_FORMAT_INSTR_LEG

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_format_wave

      ViStatus ri3152b_format_wave (ViSession instrHandle, ViBoolean format);

  Purpose

      Panel Name:    Format Wave
      
      Description:   Enables or disables access to the least
                     significant 4 bits for arbitrary waveform
                     download data.  When enabled, the user can
                     set the least significant 4 bits.  When
                     disabled, the least significant bits are
                     ignored from the downloaded data.
      
      Note:          Not supported in 3152B
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      format

          Variable Type       ViBoolean

          Control Name:  Format
          
          Description:   Enables or disables access to the least
                         significant 4 bits of arbitrary waveform
                         data that is downloaded.
          
          Valid Range:   NORMAL - VI_FALSE (0) = access is disabled
                         USER   - VI_TRUE  (1) = access is enabled
          
          Default:       USER  (note:  instrument *RST
                                condition is NORMAL)
          
          Note:          Not supported in 3152B

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_format_wave_query

      ViStatus ri3152b_format_wave_query (ViSession instrHandle,
                                          ViChar _VI_FAR formatWave[]);

  Purpose

      Panel Name:    Format Wave Query
      
      Description:   Reads the value of format wave
      
      Note: Not supported in 3152B
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      formatWave

          Variable Type       ViChar []

          Control Name:      Format Wave
          
          Description:       Returns the value of format wave
                             "NORM" = Normal mode is active
                             "USER" = user mode is active
          
          Note:              Not supported in 3152B

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_frequency_query

      ViStatus ri3152b_frequency_query (ViSession instrHandle,
                                        ViPReal64 frequency);

  Purpose

      Panel Name:    Frequency Query
      
      Description:   Returns the frequency setting for the currently
                     active standard function.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequency

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Frequency
          
          Description:   Reads the current frequency in hertz.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_gauss_exp_query

      ViStatus ri3152b_gauss_exp_query (ViSession instrHandle,
                                        ViPInt16 exponent);

  Purpose

      Panel Name:    Query Gauss Exponent
      
      Description:   Returns the current gauss exponent value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      exponent

          Variable Type       ViInt16 (passed by reference)

          Control Name:   Exponent
          
          Description:    Reads the current gaussian wave exponent.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_gaussian_wave

      ViStatus ri3152b_gaussian_wave (ViSession instrHandle,
                                      ViReal64 frequency, ViReal64 amplitude,
                                      ViReal64 offset, ViInt16 exponent);

  Purpose

      Panel Name:    Gaussian Wave
      
      Description:   Select Frequency, Amplitude, Offset and
                     the exponent.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequency

          Variable Type       ViReal64

          Control Name:  Frequency
          
          Description:   Selects the frequency of the Gaussian Wave.
          
          Valid Range:
          10.0E-3 to 100.0E6   (3152B)
          100.0E-6 to 1.0E6    (3152A)
          
          Default:  1.0E6
          
          
          

      amplitude

          Variable Type       ViReal64

          Control Name:  Amplitude
          
          Description:   Selects the amplitude (Volts peak-to-peak).
          
          Valid Range:   10.0E-3 to 16.0 V
          
          Default:       5.0 V
          
          Note:          The following relationship must hold between the
                         amplitude and the offset:
          
                               (Amplitude / 2) + ABS(offset) <= 8.0

      offset

          Variable Type       ViReal64

          Control Name:  Offset
          
          Description:   Programs the offset for the presently
                         selected waveform.
          
          Valid Range:
          -7.992 to +7.992   (3152B)
          -7.19 to +7.19     (3152A)
          
          Default:   0.0

      exponent

          Variable Type       ViInt16

          Control Name:  Exponent
          
          Description:   Select the exponent for the Gaussian Wave.
          
          Valid Range:   1 to 200
          
          Default:       10
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_immediate_trigger

      ViStatus ri3152b_immediate_trigger (ViSession instrHandle);

  Purpose

      Panel Name:    Immediate Trigger
      
      Description:   Will send an immediate trigger when this
                     panel is executed.  This will trigger the
                     3152B if the "BUS" Trigger Source is presently
                     selected (see ri3152b_trigger_source()).
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_init

      ViStatus ri3152b_init (ViRsrc instrDescriptor, ViBoolean IDQuery,
                             ViBoolean resetDevice, ViPSession instrHandle);

  Purpose

      Panel Name:    Initialize
      
      Description:   Initializes the instrument and returns and
                     "instrument handle".  The instrument handle
                     must be used with all of the other functions
                     of this driver.
      
                     The initialize call allows the instrument
                     to be queried to ensure that it is a 3152B
                     Arbitrary Waveform/Function Generator.
                     It also resets the 3152B to the power-up
                     state if the "Reset" parameter is True (ON).
      
                     Note that for each "ri3152b_init()" call, a new
                     unique instrument handle is returned.  Thus,
                     if four calls are made to the initialize
                     call in succession, four unique instrument
                     handles will be returned.  This driver supports
                     10 instances of instrument handles, so that
                     if this function is called 11 times consecutively
                     (with no call to ri3152b_close()), the 11th call
                     will fail.
      
                     For each instrument handle returned by the
                     "ri3152b_init()" function, the "ri3152b_close()"
                     function should be called to free up the
                     resources allocated by "ri3152b_init()".  The
                     call(s) to "ri3152b_close()" should be made
                     before the application program terminates.
      

  Parameter List

      instrDescriptor

          Variable Type       ViRsrc

           Control Name:  Instr Descriptor
          
           Description:   Specifies which remote instrument
                          to establish a communication
                          session with.  Based on the syntax
                          of the Instr Descriptor, the
                          Initialize function configures the
                          I/O interface and generates an
                          Instr Handle
          
           Default Value: "VXI::7::INSTR"
          
           Based on the Instrument Descriptor, this operation
           establishes a communication session with a device.
           The grammar for the Instrument Descriptor is shown
           below.  Optional parameters are shown in square
           brackets ([]).  The default value is for a VXI
           interface for logical address 7.  For a GPIB-VXI interface
           with the timer/counter set to logical address 19, the
           value should be:
          
                "GPIB-VXI::7::INSTR"
          
          
          Interface Grammar
          ----------------------------------------------------
          GPIB      GPIB[board]::primary address
                    [::secondary address][::INSTR]
          VXI       VXI[board]::VXI logical address[::INSTR]
          GPIB-VXI  GPIB-VXI[board][::GPIB-VXI primary address]
                    ::VXI logical address[::INSTR]
          
          The GPIB keyword can be used with GPIB instruments.
          The VXI keyword is used for VXI instruments via
          either embedded or MXIbus controllers.
          The GPIB-VXI keyword is used for a  GPIB-VXI
          controller.
          
          The default value for optional parameters are shown
          below.
          
          Optional Parameter          Default Value
          -----------------------------------------
          board                       0
          secondary address           none - 31
          GPIB-VXI primary address    1
          

      IDQuery

          Variable Type       ViBoolean

          Control Name:  ID Query
          
          Description:    Specifies if an ID Query is sent
                          to the instrument during the
                          initialization procedure.
          
          Valid Range:    1 = Yes
                          0 = No
          
          Default Value:  1 - Yes
          
          NOTE:   Under normal circumstances the ID Query
                  insures that the instrument initialized
                  over the bus is the type supported by
                  this driver. However circumstances may
                  arise where it is undesirable to send an
                  ID Query to the instrument. In those
                  cases; set this control to Skip Query
                  and this function will initialize the
                  bus and the Command arrays in the driver,
                  without doing an ID Query.

      resetDevice

          Variable Type       ViBoolean

          Control Name:  Reset
          
          Description:    Specifies if the instrument is to
                          be reset to its power-on settings
                          during the initialization
                          procedure.
          
          Valid Range:    1 = Yes
                          0 = No
          
          Default Value:  1 - Yes
          
          NOTE:  If you do not want the instrument reset.
                 Set this control to No while initializing
                 the instrument.

      instrHandle

          Variable Type       ViSession (passed by reference)

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          NOTE:  A new (unique) handle will be
                          returned EACH time the Initialize
                          function is called.  The ri3152b_close()
                          call should be used for EACH handle
                          returned by the ri3152b_init() function.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_insert_slave

      ViStatus ri3152b_insert_slave (ViSession instrHandle,
                                     ViChar _VI_FAR IPAddress[]);

  Purpose

      Panel Name:   Insert Slave
      
      Description:  This function adds a designated slave unit to a
                    synchronized multi-instruments system list.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      IPAddress

          Variable Type       ViChar []

          Control Name:  IP Address
          
          Description:   Pass the IP Address of slave unit to a
                         synchronized multi-instruments system list.
          
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_AHOP_data_file

      ViStatus ri3152b_load_AHOP_data_file (ViSession instrHandle,
                                            ViChar _VI_FAR fileName[]);

  Purpose

      Panel Name:   Load AHOP Data File
      
      Description:  This function loads the data from the specified
                    AHop data file.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      fileName

          Variable Type       ViChar []

          Control Name:  File Name
          
          Description:   Determines the name of the file to read.
          
          Valid Range:   List of float amplitude and Real64 dwell
                         times separate by space. (Text file *.ahpv). List
                         of float amplitude separate by space. (Text file
                         *.ahpf)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_AHOP_fixed_data_array

      ViStatus ri3152b_load_AHOP_fixed_data_array (ViSession instrHandle,
                                                   ViInt16 size,
                                                   float amplitudeData[]);

  Purpose

      Panel Name:   Load AHop Fixed Data Array
      
      Description:  This function loads the data from the specified
                    Amplitude Hop Fixed data array.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      size

          Variable Type       ViInt16

          Control Name:  Size
          
          Description:  Pass the size of the Ampl. Hop Fixed  data array
                        you want create.
          
          Valid Range:   1 to 5000

      amplitudeData

          Variable Type       float []

          Control Name:  Amplitude Data
          
          Description:   Specify the array of Amplitude Hop Fixed data you
                         want to use.  The array must have at least as
                         many elements as the value you specify in the
                         Size parameter.
          
          Valid Range:   Array of amplitude values
                        (10E-3 to 16, float - 4 bytes)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_AHOP_var_data_array

      ViStatus ri3152b_load_AHOP_var_data_array (ViSession instrHandle,
                                                 ViInt16 size,
                                                 ViReal64 _VI_FAR amplitudeData[],
                                                 ViReal64 _VI_FAR dwellTimeData[]);

  Purpose

      Panel Name:   Load AHop Var Data Array
      
      Description:  This function downloads the data array that will
                    cause the instrument to hop through the amplitude
                    list. The dwell time for each amplitude list item
                    is variable and is supplied in the variable hop
                    table data array.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      size

          Variable Type       ViInt16

          Control Name:  Size
          
          Description:   Pass the size of the Amplitude data array and
                         Dwell time data array you want create.
          
          Valid Range:   1 to 5000

      amplitudeData

          Variable Type       ViReal64 []

          Control Name:  Amplitude Data
          
          Description:   Specify the array of Amplitude Hop data you
                         want to use.  The array must have at least as
                         many elements as the value you specify in the
                         Size parameter.
          
          Valid Range:   Array of amplitude values
                        (10E-3 to 16, float - 4 bytes)

      dwellTimeData

          Variable Type       ViReal64 []

          Control Name:  Dwell Time Data
          
          Description:   Specify the array of Dwell Time data you want to
                         use. The array must have at least as many
                         elements as the value you specify in the Size
                         parameter.
          
          Valid Range:   Array of dwell time values (200E-9 to 20, double
                         - 8 bytes)
          
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_and_shift_arb_data

      ViStatus ri3152b_load_and_shift_arb_data (ViSession instrHandle,
                                                ViInt16 segmentNumber,
                                                ViUInt16 _VI_FAR dataPointArray[],
                                                ViInt32 number_ofPoints);

  Purpose

      Control Name:   Load/Shift Arbitrary Data
      
      Description:    Selects the arbitrary data segment and
                      loads data into the segment.  The data
                      is shifted left 4 places prior to loading
                      to convert DAC values in the range 0 to
                      4095 into values used by the instrument
                      hardware.
      
      NOTE:   This function is provided solely as an alternative
              to "ri3152b_load_arb_data()".  This function is
              provided for those languages, such as Visual BASIC,
              which do NOT support the VISA data type ViUInt16 [].
              For most users, the "ri3152b_load_arb_data()" is
              preferred because of the increased data download
              throughput.
      
              Only for 12BIT data.
      
              Use function ri3152b_set_wave_resolution before
              download data.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      segmentNumber

          Variable Type       ViInt16

          Control Name:  Segment Number
          
          Description:   Select the segment number.
          
          Valid Range:
           1 to 10E3  (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:       1

      dataPointArray

          Variable Type       ViUInt16 []

          Control Name:   Data Points []
          
          Description:    Select an array of data to be down loaded to
                          a specific memory segment.
          
          Data Range:     each point in the array represents a single
                          DAC value.  The DAC values range from:
          
                                0x0000 (0 decimal)      minimum to
                                0x0FFF (4095 decimal)   maximum
          
                          The middle of the range voltage is thus
                          produced with a value of 0x0800 (2048 decimal)
          
                          The value of 0x0000 represents the minimum
                          (most negative) voltage to generate, and the
                          value of 0x0FFF represents the maximum (most
                          positive) voltage to generate.  The minimum
                          and maximum voltages are determined by the
                          "amplitude" and "offset" values in the
                          ri3152b_output_arb_waveform() function.
          
                          This function shifts the value of each data
                          point left 4 places prior to loading.  This
                          is done to accommodate the 3152B hardware.
                          This causes a performance penalty over
                          the function "ri3152b_load_arb_data()".
          
                          Only for 12BIT data.

      number_ofPoints

          Variable Type       ViInt32

          Control Name:  Number of points ( Segment Size )
          
          Description:   Select the segment size.  It should match
                         the number of data points in the Data Points
                         panel.
          
          Valid Range:
           /* for 3152A */
           10 to 64536  (64K memory)
           10 to 262144 (256K memory)
           10 to 523288 (512K memory)
          
           /* for 3152B */
           16 to 1E6  (1M memory)
           16 to 2E6  (2M memory)
           16 to 4E6  (4M memory)
          
          Default:
           16  (3152B)
           10  (3152A)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_arb_data

      ViStatus ri3152b_load_arb_data (ViSession instrHandle,
                                      ViInt16 segmentNumber,
                                      ViUInt16 _VI_FAR dataPointArray[],
                                      ViInt32 number_ofPoints);

  Purpose

      Panel Name:     Load Arbitrary Data
      
      Description:    Selects the arbitrary data segment and
                      loads data into the segment
      
      NOTE:   This function uses a data type of ViUInt16 [],
              which is NOT supported by some languages, such as
              Visual BASIC.  Users of languages which do not
              support the data type of ViUInt16 [] should use
              the function ri3152b_load_and_shift_arb_data(), which
              uses a data type of ViInt16[].
      
              Use function ri3152b_set_wave_resolution before
              download data.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      segmentNumber

          Variable Type       ViInt16

          Control Name:  Segment Number
          
          Description:   Select the segment number.
          
          Valid Range:
           1 to 10000 (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:  1

      dataPointArray

          Variable Type       ViUInt16 []

          Control Name:   Data Points []
          
          Description:    Select an array of data to be down loaded to
                          a specific memory segment.
          
          Data Range:     each point in the array represents a single
                          DAC value.  The least significant 4 bits of
                          the DAC value should be set to 0.  The
                          DAC value thus ranges from:
          
                                0x0000 (0 decimal)      minimum to
                                0xFFFF (65535 decimal)  maximum in
                                                        increments of
                                0x0010 (16 decimal)
          
                          The middle of the range voltage is thus
                          produced with a value of 0x8000 (32768 decimal)
          
                          The value of 0x0000 represents the minimum
                          (most negative) voltage to generate, and the
                          value of 0xFFF0 represents the maximum (most
                          positive) voltage to generate.  The minimum
                          and maximum voltages are determined by the
                          "amplitude" and "offset" values in the
                          ri3152b_output_arb_waveform() function.
          

      number_ofPoints

          Variable Type       ViInt32

          Control Name:  Number of points ( Segment Size )
          
          Description:   Select the segment size.  It should match
                         the number of data points in the Data Points
                         panel.
          
          Valid Range:
           /* for 3152A */
           10 to 64536  (64K memory)
           10 to 262144 (256K memory)
           10 to 523288 (512K memory)
          
           /* for 3152B */
           16 to 1E6  (1M memory)
           16 to 2E6  (2M memory)
           16 to 4E6  (4M memory)
          
          Default:
           16  (3152B)
           10  (3152A)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_arb_FM_data

      ViStatus ri3152b_load_arb_FM_data (ViSession instrHandle,
                                         ViInt32 waveformSize,
                                         ViReal64 _VI_FAR waveformDataArray[]);

  Purpose

      Panel Name:   Load Arb FM Data
      
      Description:  Load FM Arbitrary data.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      waveformSize

          Variable Type       ViInt32

          Control Name:  Waveform Size.
          
          Description:   Pass the size of the FM arbitrary waveform you
                         want create.
          
                         The size must meet the following restrictions:
          
                         - The size is less than or equal to the
                           maximum waveform size the instrument
                           allows.
          
                         - The size is greater than or equal to
                           the minimum waveform size the instrument
                           allows.
          
          
          Valid Range:
          
          Default Value: None

      waveformDataArray

          Variable Type       ViReal64 []

          Control Name:  Waveform Data Array []
          
          Description:   Specify the array of data you want to use for the
                         new FM arbitrary waveform.  The array must have
                         at least as many elements as the value you
                         specify in the Waveform Size parameter.
          
                         You must normalize the data points in the array
                         to be between
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_ascii_file

      ViStatus ri3152b_load_ascii_file (ViSession instrHandle,
                                        ViInt16 segmentNumber,
                                        ViChar _VI_FAR fileName[],
                                        ViInt32 number_ofPoints);

  Purpose

      Control Name:   Load ASCII file
      
      Description:    Selects the arbitrary data segment and
                      loads data from an ASCII data file into
                      the waveform generator.
      
      NOTE:           Use function ri3152b_set_wave_resolution before
                      download data.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      segmentNumber

          Variable Type       ViInt16

          Control Name:  Segment Number
          
          Description:   Select the segment number.
          
          Valid Range:
           1 to 10E3  (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:       1

      fileName

          Variable Type       ViChar []

          Control Name:   File Name
          
          Description:    Determines the name of the file to read
          
          Valid Range:    The file should be composed of ASCII numbers
                          separated by one or more white space characters
                          (including end-of-line).  Each number must be
                          in the range -2048 to +2047 (12bit) or -32768
                          to +32767 (16-bit).  The value of
                          -2048 will produce the minimum (most negative)
                          voltage, and the value of 2047 will produce the
                          maximum (most positive) voltage.  The minimum
                          and maximum voltages are determined by the
                          "amplitude" and "offset" parameters of the
                          "ri3152b_output_arb_waveform()" function.
          
          

      number_ofPoints

          Variable Type       ViInt32

          Control Name:  Number of points ( Segment Size )
          
          Description:   Select the segment size.  This should match
                         the number of points defined for the segment.
                         This should be less than or equal to the
                         number of points in the file; if there are
                         less data points than are specified with this
                         parameter, then the number of points in
                         the file will be used.
          
          Valid Range:
           /* for 3152A */
           10 to 64536  (64K memory)
           10 to 262144 (256K memory)
           10 to 523288 (512K memory)
          
           /* for 3152B Modern mode */
           16 to 1E6  (1M memory)
           16 to 2E6  (2M memory)
           16 to 4E6  (4M memory)
          
           /* for 3152B Legacy mode */
           10 to 523288
          
          Default:
           16  (3152B Modern mode)
           10  (3152B Legacy mode)
           10  (3152A)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_ASK_data_array

      ViStatus ri3152b_load_ASK_data_array (ViSession instrHandle,
                                            ViInt16 size,
                                            unsigned char ASKData[]);

  Purpose

      Panel Name:   Load ASK Data Array
      
      Description:  This function loads the data from the specified
                    ASK data array.
                    Sample ASK Data Array 0 1 1 1 0 1 0 0 0 1

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      size

          Variable Type       ViInt16

          Control Name:  Size
          
          Description:   Pass the size of the ASK data array you want
                         create.
          
          Valid Range:   1 to 1000

      ASKData

          Variable Type       unsigned char []

          Control Name:  ASK Data
          
          Description:   Specify the array of ASK data you want to use.
                         The array must have at least as many elements as
                         the value you specify in the Size parameter.
          
          Valid Range:   Array of values: 0 or 1 (1 bytes)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_ASK_data_file

      ViStatus ri3152b_load_ASK_data_file (ViSession instrHandle,
                                           ViChar _VI_FAR fileName[]);

  Purpose

      Panel Name:   Load ASK Data File
      
      Description:  This function loads the data from the specified
                    ASK data file.
                    Sample ASK Data Array 0 1 1 1 0 1 0 0 0 1

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      fileName

          Variable Type       ViChar []

          Control Name:  File Name
          
          Description:   Determines the name of the file to read.
          
          Valid Range:   List of bytes ASK data separate by space.(Text
                         file *.ask).
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_FHOP_data_file

      ViStatus ri3152b_load_FHOP_data_file (ViSession instrHandle,
                                            ViChar _VI_FAR fileName[]);

  Purpose

      Panel Name:   Load FHOP Data File
      
      Description:  This function loads the data from the specified
                    Hop data file.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      fileName

          Variable Type       ViChar []

          Control Name:  File Name
          
          Description:   Determines the name of the file to read.
          
          Valid Range:   List of Real64 hop frequencies and Real64 dwell
                         times separate by space. (Text file *.hop)
                         List of Real64 hop frequencies separate by space.
                         (Text file *.hpf)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_FM_Arb_waveform_from_file

      ViStatus ri3152b_load_FM_Arb_waveform_from_file (ViSession instrHandle,
                                                       ViChar _VI_FAR fileName[]);

  Purpose

      Panel Name:  Load FM Arb Waveform From File
      
      Description: This function load an FM arbitrary waveform.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      fileName

          Variable Type       ViChar []

          Control Name:  File Name
          
          Description:   The path of the FM arbitrary waveform you want
                         to download.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_freq_hop_fixed_data_array

      ViStatus ri3152b_load_freq_hop_fixed_data_array (ViSession instrHandle,
                                                       ViInt16 size,
                                                       ViReal64 _VI_FAR freqData[]);

  Purpose

      Panel Name:   Load Freq Hop Fixed Data Array
      
      Description:  This function loads the data from the specified
                    Freq Hop Fixed data array.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      size

          Variable Type       ViInt16

          Control Name:  Size
          
          Description:   Pass the size of the Freq Hop Fixed  data array
                         you want create.
          
          Valid Range:   1 to 5000

      freqData

          Variable Type       ViReal64 []

          Control Name:  Freq Data
          
          Description:   Specify the array of Freq Hop Fixed data you want
                         to use.  The array must have at least as many
                         elements as the value you specify in the Size
                         parameter.
          
          Valid Range:   Array of frequency values (10E-3 to 100E6,
                         double,8 bytes)
          
          
          
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_freq_hop_var_data_array

      ViStatus ri3152b_load_freq_hop_var_data_array (ViSession instrHandle,
                                                     ViReal64 _VI_FAR freqData[],
                                                     ViReal64 _VI_FAR dwellTimeData[],
                                                     ViInt16 size);

  Purpose

      Panel Name:   Load Freq Hop Var Data Array
      
      Description:  This function loads the data from the specified
                    Freq data array and Dwell time data array.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      freqData

          Variable Type       ViReal64 []

          Control Name:  Freq Data
          
          Description:   Specify the array of Freq  data you want
                         to use.  The array must have at least as many
                         elements as the value you specify in the Size
                         parameter.
          
          Valid Range:   Array of frequency values (10E-3 to 100E6,
                         double,8 bytes)

      dwellTimeData

          Variable Type       ViReal64 []

          Control Name:  Dwell Time Data
          
          Description:   Specify the array of Dwell Time data you want to
                         use.  The array must have at least as many
                         elements as the value you specify in the Size
                         parameter.
          
          Valid Range:   Array of dwell time values (200E-9 to 20, double
                         - 8 bytes)
          
          
          
          

      size

          Variable Type       ViInt16

          Control Name:  Size
          
          Description:   Pass the size of the Freq data array and Dwell
                         time data array you want create.
          
          Valid Range:   1 to 5000

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_FSK_data_array

      ViStatus ri3152b_load_FSK_data_array (ViSession instrHandle,
                                            ViInt16 size,
                                            unsigned char FSKData[]);

  Purpose

      Panel Name:   Load FSK Data Array
      
      Description:  This function loads the data from the data
                    array.Loads the data stream that will
                    cause the instrument to hop from carrier to
                    shifted frequency and visa versa. Data format is
                    a string of "0" and "1" which define when the
                    output generates carrier frequency and when it
                    shifts frequency to the FSK value. "0" defines
                    carrier frequency,"1" defines shifted frequency.
                    Note that if you intend to program marker
                    position, you must do it before you load the FSK
                    data list.Below you can see how an FSK data table
                    is constructed. The sample below shows a list of
                    10 shifts. The instrument will step through this
                    list, outputting either carrier or shifted
                    frequencies, depending on the data list: Zero
                    will generate carrier frequency and One will
                    generate shifted frequency. Note that the waveform
                    is always sinewave and that the last cycle is
                    always completed.
                    Sample FSK Data Array 0 1 1 1 0 1 0 0 0 1

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      size

          Variable Type       ViInt16

          Control Name:  Size
          
          Description:   Pass the size of the FSK data array you want
                         create.
          
          Valid Range:   1 to 4000

      FSKData

          Variable Type       unsigned char []

          Control Name:  FSK Data
          
          Description:   Specify the array of FSK data you want to use.
                         The array must have at least as many elements as
                         the value you specify in the Size parameter.
          
          Valid Range:   Array of values: 0 or 1 (1 bytes)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_FSK_data_file

      ViStatus ri3152b_load_FSK_data_file (ViSession instrHandle,
                                           ViChar _VI_FAR fileName[]);

  Purpose

      Panel Name:   Load FSK Data File
      
      Description:  This function loads the data from the specified
                    FSK data file.Loads the data stream that will
                    cause the instrument to hop from carrier to
                    shifted frequency and visa versa. Data format is
                    a string of "0" and "1" which define when the
                    output generates carrier frequency and when it
                    shifts frequency to the FSK value. "0" defines
                    carrier frequency,"1" defines shifted frequency.
                    Note that if you intend to program marker
                    position, you must do it before you load the FSK
                    data list.Below you can see how an FSK data table
                    is constructed. The sample below shows a list of
                    10 shifts. The instrument will step through this
                    list, outputting either carrier or shifted
                    frequencies, depending on the data list: Zero
                    will generate carrier frequency and One will
                    generate shifted frequency. Note that the waveform
                    is always sinewave and that the last cycle is
                    always completed.
                    Sample FSK Data Array 0 1 1 1 0 1 0 0 0 1

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      fileName

          Variable Type       ViChar []

          Control Name:  File Name
          
          Description:   Determines the name of the file to read.
          
          Valid Range:   List of bytes FSK data separate by space.(Text
                         file *.fsk).

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_PSK_data_array

      ViStatus ri3152b_load_PSK_data_array (ViSession instrHandle,
                                            ViInt16 size,
                                            unsigned char PSKData[]);

  Purpose

      Panel Name:   Load PSK Data Array
      
      Description:  This function loads the data from the specified
                    PSK data array.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      size

          Variable Type       ViInt16

          Control Name:  Size
          
          Description:   Pass the size of the PSK data array you want
                         create.
          
          Valid Range:   1 to 4000

      PSKData

          Variable Type       unsigned char []

          Control Name:  PSK Data
          
          Description:   Specify the array of PSK data you want to use.
                         The array must have at least as many elements as
                         the value you specify in the Size parameter.
          
          Valid Range:   Array of values: 0 or 1 (1 bytes)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_PSK_data_file

      ViStatus ri3152b_load_PSK_data_file (ViSession instrHandle,
                                           ViChar _VI_FAR fileName[]);

  Purpose

      Panel Name:   Load PSK Data File
      
      Description:  This function loads the data from the specified
                    PSK data.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      fileName

          Variable Type       ViChar []

          Control Name:  File Name
          
          Description:   Determines the name of the file to read.
          
          Valid Range:   List of bytes PSK data separate by space.(Text
                         file *.psk)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_segment_binary

      ViStatus ri3152b_load_segment_binary (ViSession instrHandle,
                                            ViInt16 number_ofSegments,
                                            ViInt32 _VI_FAR waveSize[]);

  Purpose

      Panel Name: Load Segment Binary
      
      Description:
      
      This loads the segment binary data using the fast binary download method.
      This function performs the bit / byte manipulations necessary to generate
      the segment table in the proper format.  With this function, the
      programmer merely states the number of segments to download and the size
      (in points) of each wave.
      
      The traditional ASCII method of downloading one waveform at a time is
      inefficient in cases where multiple waveforms have to be loaded in the
      shortest possible time.
      
      There are provisions built into the Model 3152B that allow fast binary
      downloads of waveform data.  Using this method, the memory is first
      loaded with one long waveform that is made of many smaller waveforms,
      then, using fast binary access, the segment table is downloaded to the
      generator.  The segment table contains start address and size information
      for each segment.
      
      There are a few things to consider when you prepare segment tables:
      
      1. Data for each segment must have 5 bytes.
      
      2. The number of bytes in a complete segment table must divide
         by 5.  The Model 3152B has no control over data sent to its
         port during data transfer.  Therefore, wrong data and/or
         incorrect number of bytes will cause erroneous memory
         partition.
      
      3. The start address for segment 1 is 0x1F4.
      
      4. Compute the start address for segment n" using the following
         equation:
      
         ADD(n) = ADD(n-1) + (SIZE(n-1)/2)
      
         For example, say you have 2 segments, the first is 10,000
         points.  The start address for segment number 2 is as
         follows:
      
         ADD(2) = ADD(1) + (SIZE(1)/2)
         = 500 + 10000/2 = 500 + 5000; and in hex
         = 0x1F4 + 0x1388 = 0x157C
      
      5. The segment size is entered using the actual size of the
         segment.  Do not divide the segment size as was done for the
         address.  For a size of 10,000: use 0x2710
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      number_ofSegments

          Variable Type       ViInt16

          Control Name: Number of Segments
          
          Description: This sets the number of segments for the segment
                       table.
          

      waveSize

          Variable Type       ViInt32[]

          Control Name:  Wave Size
          
          Description: This stores the array of wave sizes for the Segment
                       Table.
          
          Valid Range: Each entry must be 1 to the maximum value = memory
                       size.
          
          Size of array must be the same as the Number of Segments control
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message()" will return
                          a string describing the error.
          
           VISA Errors:
                  These error codes are defined by the VISA I/O
                  library function standards.  These errors are defined
                  in the header file "visa.h".  These error codes are
                  typically in the range 0xBFFF0000L to 0xBFFFFFFFL
                  (-1073807360 to -1073741825).
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message() function.  The
                  ri3152A_error_message() function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_sequence_binary

      ViStatus ri3152b_load_sequence_binary (ViSession instrHandle,
                                             ViInt16 number_ofSteps,
                                             ViInt16 _VI_FAR segmentNumbers[],
                                             ViInt32 _VI_FAR repeatSegment[]);

  Purpose

      Panel Name:    Load Sequence Binary
      
      Description:   Load a sequence of binary data.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      number_ofSteps

          Variable Type       ViInt16

          Control Name:  Number of steps
          
          Description:   Select the number of steps in a sequence.
          
          Valid Range:   1 to 4096
          
          Default:       1

      segmentNumbers

          Variable Type       ViInt16 []

          Control Name:  Segment Numbers []
          
          Description:   Select the number of segments in a sequence.
          
          Valid Range:
           1 to 10000 (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:       1

      repeatSegment

          Variable Type       ViInt32 []

          Control Name:  Repeat Segment []
          
          Description:   Select the number of repeat segment in a
                         sequence.
          
          Valid Range:   1 to 1E6  (1000000)
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_wavecad_file

      ViStatus ri3152b_load_wavecad_file (ViSession instrHandle,
                                          ViChar _VI_FAR waveCADFileName[]);

  Purpose

      Control Name:  Load WaveCAD (version 2.0) file
      
      Description:   Loads all data from a WaveCAD file (*.CAD).
                     This includes loading all waveform data
                     files specified in the WaveCAD file.
      
                     The instrument configuration is programmed
                     as a result of this function.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      waveCADFileName

          Variable Type       ViChar []

          Control Name:  File Name
          
          Description:   Determines the name of the file to read
          
          Valid Range:   The file must be one created by WaveCAD version
                         2.0 for the Racal 3152A.
          
          Default:       "C:\\wcad20\\rac3152A\\sample.cad"

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_load_wavecad_wave_file

      ViStatus ri3152b_load_wavecad_wave_file (ViSession instrHandle,
                                               ViInt16 segmentNumber,
                                               ViChar _VI_FAR waveCADWaveformFileName[]);

  Purpose

      Control Name:  Load WaveCAD Waveform File
      
      Description:   Loads the data from the specified WaveCAD
                     waveform data file into the specified
                     segment.
      
      NOTE:          Use function ri3152b_set_wave_resolution before
                     download data.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      segmentNumber

          Variable Type       ViInt16

          Control Name:  Segment Number
          
          Description:   Select the segment number.
          
          Valid Range:
           1 to 10E3  (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:  1

      waveCADWaveformFileName

          Variable Type       ViChar []

          Control Name:  File Name
          
          Description:   Determines the name of the file to read.
          
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_mode_query

      ViStatus ri3152b_mode_query (ViSession instrHandle,
                                   ViPInt16 presentWaveformMode,
                                   ViPInt16 presentStandardWaveform);

  Purpose

      Panel Name:    Mode Query
      
      Description:   Returns the current mode in which the
                     waveform generator is operating.  If it
                     is generating in the "Standard" mode, this
                     function also returns which waveform is
                     presently selected.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      presentWaveformMode

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Present Waveform Mode
          
          Description:   Reads the presently selected Waveform
                         Operating Mode.  The reply will be one
                         of the following:
          
                         RI3152B_MODE_STD (0)   - Standard Waveform
                         RI3152B_MODE_ARB (1)   - Arbitrary Waveform
                         RI3152B_MODE_SWEEP (2) - Sweep Waveform
                         RI3152B_MODE_SEQ (3)   - Sequence Waveform
                         RI3152B_MODE_MOD (4)   - Modulation
                         RI3152B_MODE_DPUL (5)  - Digital Pulse
                         RI3152B_MODE_HALF (6)  - Half Cycle
                         RI3152B_MODE_COUN (7)  - Counter
          
          

      presentStandardWaveform

          Variable Type       ViInt16 (passed by reference)

          Control Name: Present Standard Waveform
          
          Description:  Indicates the presently selected standard
                        waveform.  This will be one of the
                        following:
          
                        RI3152B_NON_STANDARD (0) - Not in Standard Mode
                        RI3152B_SINE (1)         - Sine Wave
                        RI3152B_TRIANGLE (2)     - Triangle Wave
                        RI3152B_SQUARE (3)       - Square Wave
                        RI3152B_PULSE (4)        - Pulse Wave
                        RI3152B_RAMP (5)         - Ramp Wave
                        RI3152B_SINC (6)         - Sine(x)/x Wave
                        RI3152B_GAUSSIAN (7)     - Gaussian Wave
                        RI3152B_EXPONENTIAL (8)  - Exponential
                        RI315B_DC (9)            - DC Waveform
                        RI315B_NOISE (10)        - Noise Waveform
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_opc

      ViStatus ri3152b_opc (ViSession instrHandle);

  Purpose

      Panel Name:    Operation Complete
      
      Description:   Set the "operation complete"  bit (bit 0) in the
                     Standard Event register after the previous
                     commands have been executed.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_operating_mode

      ViStatus ri3152b_operating_mode (ViSession instrHandle,
                                       ViInt16 operatingMode);

  Purpose

      Panel Name:    Operating Mode
      
      Description:   Selects one of the four operating modes:
                     Continuous, Trigger, Gated, Burst.
      
                     For a description of these operating modes,
                     see the "Operating Mode" parameter description.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      operatingMode

          Variable Type       ViInt16

          Control Name:  Operating Mode
          
          Description:   Selects one of the modes: CONT, TRIG, GATED
                         and BURST.
          
          Valid Range:
           RI3152B_MODE_CONT   - The signal is generated continuously.
          
           RI3152B_MODE_TRIG   - The signal is generated once for each
                                 trigger received.
          
           RI3152B_MODE_GATED  - The signal is generated as long as the
                                 trigger is asserted.  Once started, a
                                 complete cycle of the signal will be
                                 produced.
          
           RI3152B_MODE_BURST  - N cycles are produced each time a trigger
                                 is received.  N is set by the function
                                 ri3152b_burst_mode().
          
          Default:       RI3152B_MODE_CONT
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_option_query

      ViStatus ri3152b_option_query (ViSession instrHandle,
                                     ViChar _VI_FAR option[]);

  Purpose

      Panel Name:    Option Query
      
      Description:   Queries the instrument's memory
                     (512K or 256K or 64K) - for 3152A
      
                     (1M or 2M or 4M) - for 3152B
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

      option

          Variable Type       ViChar []

          Control Name:  Option
          
          Description:   Holds the option returned
                         by the instrument.
          
          Valid Range:
          /* for 3152A */
           0 -  64K
           1 - 256K
           2 -  512K
          
          /* for 3152B */
           0 -1M
           1 - 2M
           2 - 4M
          
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output

      ViStatus ri3152b_output (ViSession instrHandle, ViBoolean outputSwitch);

  Purpose

      Panel Name:    Output ON/OFF
      
      Description:   Turns the output signal ON or OFF.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      outputSwitch

          Variable Type       ViBoolean

          Control Name:  Output Switch
          
          Description:   Turns the output signal On or Off.
          
          Valid Range:   RI3152B_OUTPUT_ON -  (1)
                         RI3152B_OUTPUT_OFF - (0)
          
          Default:       RI3152B_OUTPUT_OFF  (note:  instrument *RST
                                              condition is OFF)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_arb_waveform

      ViStatus ri3152b_output_arb_waveform (ViSession instrHandle,
                                            ViInt16 segmentNumber,
                                            ViReal64 samplingClock,
                                            ViReal64 amplitude,
                                            ViReal64 offset,
                                            ViInt16 clockSource);

  Purpose

      Panel Name:  Output Arbitrary Waveform
      
      Description:   Select a Segment Number and its Sampling Clock,
                     Amplitude, Offset and Clock Source.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      segmentNumber

          Variable Type       ViInt16

          Control Name:  Segment Number
          
          Description:   Select the segment number.
          
          Valid Range:
           1 to 10E3  (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:       1

      samplingClock

          Variable Type       ViReal64

          Control Name:  Sampling Clock
          
          Description:   Selects the Sampling Clock for selected segment.
          
          Valid Range:
           10.0E-6 to 250.0E6   (3152B)
           100.0E-3 to 100.0E6  (3152A)
          
          Default: 1.0E6

      amplitude

          Variable Type       ViReal64

          Control Name:  Amplitude
          
          Description:   Selects the amplitude (Volts peak-to-peak).
          
          Valid Range:   0.01 to 16.0 V
          
          Default:       5.0 V
          
          Note:          The following relationship must hold between the
                         amplitude and the offset:
          
                         (Amplitude / 2) + ABS(offset) <= 8.0

      offset

          Variable Type       ViReal64

          Control Name:  Offset
          
          Description:   Selects the offset level.
          
          Valid Range:
          -7.992 to +7.992   (3152B)
          -7.19 to +7.19     (3152A)
          
          Default:       0.0 V

      clockSource

          Variable Type       ViInt16

          Control Name:  Clock Source
          
          Description:   Select one of the Trigger Sources: INTERNAL
                         EXTERNAL, ECLTRG0 or LBUS.  ECLTRG0 is used to
                         synchronize two instruments.
          
          Valid Range:   RI3152B_CLK_SOURCE_INT     (0)
                         RI3152B_CLK_SOURCE_EXT     (1)
                         RI3152B_CLK_SOURCE_ECLTRG0 (2)
                         RI3152B_CLK_SOURCE_LBUS    (3)
          
          Default:       RI3152B_CLK_SOURCE_INT

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_eclt

      ViStatus ri3152b_output_eclt (ViSession instrHandle,
                                    ViBoolean outputECLTSwitch);

  Purpose

      Panel Name:    Output ECLT
      
      Description:   Turns the output ECLT0 ON or OFF.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      outputECLTSwitch

          Variable Type       ViBoolean

          Control Name:  Output ECLT0 Switch
          
          Description:   Turns the output ECLT0 On or Off.
          
          Valid Range:   VI_TRUE   (1)
                         VI_FALSE  (0)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_ECLT1

      ViStatus ri3152b_output_ECLT1 (ViSession instrHandle,
                                     ViBoolean outputECLT1Switch);

  Purpose

      Panel Name:    Output ECLT1
      
      Description:   Turns the output ECLT1 ON or OFF.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      outputECLT1Switch

          Variable Type       ViBoolean

          Control Name:  Output ECLT1 Switch
          
          Description:   Turns the output ECLT1 On or Off.
          
          Valid Range:   VI_TRUE   (1)
                         VI_FALSE  (0)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_sequence_waveform

      ViStatus ri3152b_output_sequence_waveform (ViSession instrHandle,
                                                 ViReal64 samplingClock,
                                                 ViReal64 amplitude,
                                                 ViReal64 offset,
                                                 ViInt16 sequenceMode);

  Purpose

      Panel Name:     Output Sequence Waveform
      
      Description:    Select a Sequence Number and its Sampling Clock,
                      Amplitude and Offset.  Also begins generating
                      the sequence.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      samplingClock

          Variable Type       ViReal64

          Control Name:  Sampling Clock
          
          Description:   Selects the sampling clock used for each
                         point in the sequence.
          
          Valid Range:
           10.0E-6 to 250.0E6   (3152B)
           100.0E-3 to 100.0E6  (3152A)
          
          Default: 1.0E6

      amplitude

          Variable Type       ViReal64

          Control Name:   Amplitude
          
          Description:    Selects the amplitude (Volts peak-to-peak).
          
          Valid Range:    0.01 to 16.0 V
          
          Default:        5.0 V
          
          Note:           The following relationship must hold between the
                          amplitude and the offset:
          
                               (Amplitude / 2) + ABS(offset) <= 8.0

      offset

          Variable Type       ViReal64

          Control Name:  Offset
          
          Description:   Selects the offset level.
          
          Valid Range:
          -7.992 to +7.992   (3152B)
          -7.19 to +7.19     (3152A)
          
          Default:  0.0 V

      sequenceMode

          Variable Type       ViInt16

          Control Name:  Sequence Mode
          
          Description:   Selects an Sequence Mode.
          
          Valid Range:   RI3152B_SEQ_MODE_AUTO  (0)
                         RI3152B_SEQ_MODE_TRIG  (1)
                         RI3152B_SEQ_MODE_STEP  (2)
                         RI3152B_SEQ_MODE_MIX   (3)
          
          Default:       RI3152B_SEQ_MODE_AUTO

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_shunt

      ViStatus ri3152b_output_shunt (ViSession instrHandle,
                                     ViBoolean outputShunt);

  Purpose

      Panel Name:    Output Shunt ON/OFF
      
      Description:   Turns the output shunt ON or OFF to bypass or not
                     to bypass the 50 Ohm resistor.
      
      Note: Not supported in 3152B
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      outputShunt

          Variable Type       ViBoolean

          Control Name:  Output Shunt
          
          Description:   Turns the output shunt On or Off
          
          Valid Range:   RI3152B_OUTPUT_ON -  VI_TRUE   (1)
                         RI3152B_OUTPUT_OFF - VI_FALSE  (0)
          
          Default:       RI3152B_OUTPUT_OFF  (note:  instrument *RST
                                              condition is OFF)
          
          Note: Not supported in 3152B

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_sync

      ViStatus ri3152b_output_sync (ViSession instrHandle,
                                    ViInt16 SYNCPulseSource,
                                    ViInt32 BITSYNCPoint, ViInt32 outputWidth,
                                    ViInt16 LCOMSegment);

  Purpose

      Panel Name:    Output SYNC Pulse
      
      Description:   Programs how the SYNC pulse is generated.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      SYNCPulseSource

          Variable Type       ViInt16

          Control Name:  SYNC Pulse Source
          
          Description:   Selects the source (cause) of the SYNC
                         Pulse generated to the SYNC output of the
                         waveform generator
          
          Valid Range:
           RI3152B_SYNC_OFF (0) - Turns off the SYNC output pulse
          
           RI3152B_SYNC_BIT (1) - Generates a SYNC Pulse every time
                                  a semgent is output in arbitrary (USER)
                                  mode or sequenced modes
          
           RI3152B_SYNC_LCOM (2) - Generates a SYNC Pulse when in
                                   sequenced mode.  This generates a pulse
                                   when the selected segment (see the LCOM
                                   Segment parameter) appears for the
                                   first time in the sequence.
          
           RI3152B_SYNC_SSYN (3) - Generates a SYNC Pulse at intervals
                                   that are synchronized to the internal
                                   clock generator.
          
           RI3152B_SYNC_HCLOCK (4) - Generates a SYNC pulse at a rate
                                     equal to 1/2 the sample clock period.
          
           RI3152B_SYNC_PULSE  (5) -
          
           RI3152B_SYNC_ZEROCROSS (6) -
          
          Default:  RI3152B_SYNC_OFF

      BITSYNCPoint

          Variable Type       ViInt32

          Control Name:   BIT SYNC Point
          
          Description:    Selects the point within the signal at which
                          the Sync Pulse will be output when the BIT SYNC
                          Mode is selected.  This also indicates point
                          within the selected LCOM Segment when the LCOM
                          Mode is selected.  This has no meaning when
                          the other SYNC modes are used.
          
          Valid Range:
           0 to (memory size - 1) (3152B Modern mode)
           0 to 500000 (3152B Legacy mode)
           0 to (memory size - 1) (3152A)
          
           (and must be less than the
            number of points in the signal), MUST BE AN EVEN NUMBER
          
          Default:
           0  (3152B Modern mode)
           2  (3152B Legacy mode)
           2  (3152A)

      outputWidth

          Variable Type       ViInt32

          Control Name:  Output Width
          
          Description:   Specifies the output sync width.
          
          Valid Range:
           4 to n-8  (3152B Modern mode)  (where n - memory size)
           2 to 500  (3152B Legacy mode)
           2 to 500  (3152A)
          
          Default:
           4    (3152B Modern mode)
           2    (3152B Legacy mode)
           2    (3152A)

      LCOMSegment

          Variable Type       ViInt16

          Control Name:  LCOM Segment
          
          Description:   Selects the segment within the sequence at
                         which the SYNC pulse will be generated.
          
          Valid Range:
           1 to 10E3  (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:  1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_sync_enable

      ViStatus ri3152b_output_sync_enable (ViSession instrHandle,
                                           ViBoolean outputSyncState);

  Purpose

      Panel Name:    Output Sync Enable
      
      Description:   Turns ON (1) or OFF (0) the output sync.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      outputSyncState

          Variable Type       ViBoolean

          Control Name:  Output Sync State
          
          Description:   Sets the output sync ON (1) or OFF (0)
          
          Default:       OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_sync_pos

      ViStatus ri3152b_output_sync_pos (ViSession instrHandle,
                                        ViInt32 syncPosition);

  Purpose

      Panel Name:    Output Sync Position
      
      Description:   Selects the sync position.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      syncPosition

          Variable Type       ViInt32

          Control Name:  Sync Position
          
          Description:   Selects the point within the signal at which
                         the Sync Pulse will be output.
          
          Valid Range:
           0 to (memory size - 1)  (3152B Modern mode)
           0 to 500000  (3152B Legacy mode)
           0 to (memory size - 1)  (3152A)
           (and must be less than the number of points in
            the signal), MUST BE AN EVEN NUMBER
          
          Default:
           0  (3152B Modern mode)
           2  (3152B Legacy mode)
           2  (3152A)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_sync_query

      ViStatus ri3152b_output_sync_query (ViSession instrHandle,
                                          ViPInt16 outputSyncState);

  Purpose

      Panel Name:  Output Sync Query
      
      Description: Determines whether the output sync is ON or OFF.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      outputSyncState

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Output Sync State
          
          Description:   Determines whether the output sync is ON or OFF.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_sync_setup

      ViStatus ri3152b_output_sync_setup (ViSession instrHandle,
                                          ViInt16 SYNCPulseSource,
                                          ViInt32 BITSYNCPoint,
                                          ViInt32 outputWidth,
                                          ViInt16 LCOMSegment);

  Purpose

      Panel Name:    Output SYNC Setup
      
      Description:   Programs the SYNC output pulse without no output
                     enable.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      SYNCPulseSource

          Variable Type       ViInt16

          Control Name:  SYNC Pulse Source
          
          Description:   Selects the source (cause) of the SYNC
                         Pulse generated to the SYNC output of the
                         waveform generator
          
          Valid Range:
           RI3152B_SYNC_OFF (0) - Turns off the SYNC output pulse
          
           RI3152B_SYNC_BIT (1) - Generates a SYNC Pulse every time
                                  a semgent is output in arbitrary (USER)
                                  mode or sequenced modes
          
           RI3152B_SYNC_LCOM (2) - Generates a SYNC Pulse when in
                                   sequenced mode.  This generates a pulse
                                   when the selected segment (see the LCOM
                                   Segment parameter) appears for the
                                   first time in the sequence.
          
           RI3152B_SYNC_SSYN (3) - Generates a SYNC Pulse at intervals
                                   that are synchronized to the internal
                                   clock generator.
          
           RI3152B_SYNC_HCLOCK (4) - Generates a SYNC pulse at a rate
                                     equal to 1/2 the sample clock period.
          
           RI3152B_SYNC_PULSE  (5) -
          
           RI3152B_SYNC_ZEROCROSS (6) -
          
          Default:  RI3152B_SYNC_OFF

      BITSYNCPoint

          Variable Type       ViInt32

          Control Name:   BIT SYNC Point
          
          Description:    Selects the point within the signal at which
                          the Sync Pulse will be output when the BIT SYNC
                          Mode is selected.  This also indicates point
                          within the selected LCOM Segment when the LCOM
                          Mode is selected.  This has no meaning when
                          the other SYNC modes are used.
          
          Valid Range:
           0 to (memory size - 1) (3152B Modern mode)
           0 to 500000 (3152B Legacy mode)
           0 to (memory size - 1) (3152A)
           (and must be less than the
            number of points in the signal), MUST BE AN EVEN NUMBER
          
          Default:
           0  (3152B Modern mode)
           2  (3152B Legacy mode)
           2  (3152A)

      outputWidth

          Variable Type       ViInt32

          Control Name:  Output Width
          
          Description:   Specifies the output sync width.
          
          Valid Range:
           4 to n-8  (3152B Modern mode)  (where n - memory size)
           2 to 500  (3152B Legacy mode)
           2 to 500  (3152A)
          
          Default:
           4    (3152B Modern mode)
           2    (3152B Legacy mode)
           2    (3152A)

      LCOMSegment

          Variable Type       ViInt16

          Control Name:  LCOM Segment
          
          Description:   Selects the segment within the sequence at
                         which the SYNC pulse will be generated.
          
          Valid Range:
           1 to 10E3  (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_sync_width

      ViStatus ri3152b_output_sync_width (ViSession instrHandle,
                                          ViInt32 syncWidth);

  Purpose

      Panel Name:    Output Sync Width
      
      Description:   Selects the sync width to control the rising
                     edge of the sync pulse.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      syncWidth

          Variable Type       ViInt32

          Control Name:  Sync Width
          
          Description:   Selects a sync width to control the rising edge
                         of the sync pulse.
          
          Valid Range:
           4 to n-8  (3152B Modern mode)  (where n - memory size)
           2 to 500  (3152B Legacy mode)
           2 to 500  (3152A)
          
          Default:
           4  (3152B Modern mode)
           2  (3152B Legacy mode)
           2  (3152A)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_output_trigger

      ViStatus ri3152b_output_trigger (ViSession instrHandle,
                                       ViInt16 outputTriggerSource,
                                       ViInt16 outputTriggerLine,
                                       ViInt32 BITTriggerPoint,
                                       ViInt16 LCOMSegment,
                                       ViInt32 outputWidth);

  Purpose

      Panel Name:    Output Trigger
      
      Description:   Programs the mode of operation and the
                     Trigger Line on which the output trigger
                     is generated.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      outputTriggerSource

          Variable Type       ViInt16

          Control Name:  Output Trigger Source
          
          Description:   Selects the source of the trigger output
                         from the instrument waveform generator.
          
          Valid Range:
           RI3152B_TRIGGER_INTERNAL (0) - Generates a trigger signal at
                                          intervals set by the internal
                                          trigger generator (see the
                                          Trigger Rate Panel
                                          [ri3152b_trigger_rate()])
          
           RI3152B_TRIGGER_EXTERNAL (1) - Generates a trigger signal every
                                          time a trigger is applied to the
                                          front panel TRIG IN connector
          
           RI3152B_TRIGGER_BIT (2) - Generates a trigger signal at a
                                     particular point in the waveform.
                                     The trigger point in the waveform can
                                     be programmed using the BIT Trigger
                                     Point parameter of this function.
          
           RI3152B_TRIGGER_LCOM (3) - Generates a trigger signal in
                                      SEQuence mode.  This trigger signal
                                      is generated when the selected BIT
                                      Trigger point of the selected LCOM
                                      Segment is reached.
          
          Default:  RI3152B_TRIGGER_BIT

      outputTriggerLine

          Variable Type       ViInt16

          Control Name:  Output Trigger Line
          
          Description:   Select one of the VXI backplane TTLTRG lines
                         to receive the trigger signal.
          
          Valid Range:   RI3152B_TRIGGER_TTLTRG0  - (2)
                         RI3152B_TRIGGER_TTLTRG1  - (3)
                         RI3152B_TRIGGER_TTLTRG2  - (4)
                         RI3152B_TRIGGER_TTLTRG3  - (5)
                         RI3152B_TRIGGER_TTLTRG4  - (6)
                         RI3152B_TRIGGER_TTLTRG5  - (7)
                         RI3152B_TRIGGER_TTLTRG6  - (8)
                         RI3152B_TRIGGER_TTLTRG7  - (9)
                         RI3152B_TRIGGER_ECLTRG1  - (11)
                         RI3152B_TRIGGER_NONE     - (13)
          
          Default:       RI3152B_TRIGGER_NONE

      BITTriggerPoint

          Variable Type       ViInt32

          Control Name:   BIT Trigger Point
          
          Description:    Selects the point within the signal at which
                          the trigger will be output when the BIT Trigger
                          Mode is selected.  This is also the point
                          within the LCOM Segment when the LCOM Trigger
                          Mode is selected.
          
                          This has no meaning when the other trigger
                          modes are used.
          
          Valid Range:
           0 to (memory size - 1)  (3152B Modern mode)
           0 to 500000  (3152B Legacy mode)
           0 to (memory size - 1)  (3152A)
          
           (must be less than the number of
            points in the signal).  MUST BE AN EVEN NUMBER.
          
          Default:
           0 (3152B Modern mode)
           2 (3152B Legacy mode)
           2 (3152A)

      LCOMSegment

          Variable Type       ViInt16

          Control Name:  LCOM Segment
          
          Description:   Selects the segment within the sequence at
                         which the trigger pulse will be generated.
                         This is used only in the LCOM Trigger Mode.
          
          Valid Range:
           1 to 10E3  (3152B Modern mode)
           1 to 4096  (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:       1

      outputWidth

          Variable Type       ViInt32

          Control Name:  Output Width
          
          Description:   Specifies the pulse width.
          
          Valid Range:
           4 to n-8  (3152B Modern mode)  (where n - memory size)
           2 to 500  (3152B Legacy mode)
           2 to 500  (3152A)
          
          Default:
           4  (3152B Modern mode)
           2  (3152B Legacy mode)
           2  (3152A)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_phase_lock_loop

      ViStatus ri3152b_phase_lock_loop (ViSession instrHandle,
                                        ViBoolean phaseLockLoop);

  Purpose

      Panel Name:    3152 Phase Lock Loop (PLL) On/Off
      
      Description:   Determines whether the instrument is in the Phase
                     Lock Loop Mode.
      
                     This mode only works in Arbitrary and Standard
                     Waveform mode.
      
      Note: Not supported in 3151A, 3151B

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      phaseLockLoop

          Variable Type       ViBoolean

          Control Name:  Phase Lock Loop
          
          Description:   Determines whether the phase lock loop (PLL)
                         mode is turned ON or OFF.
          
          Valid Range:   RI3152B_PLL_ON -  VI_TRUE   (1)
                         RI3152B_PLL_OFF - VI_FALSE  (0)
          
          Default:       RI3152B_PLL_OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_phase_lock_null

      ViStatus ri3152b_phase_lock_null (ViSession instrHandle);

  Purpose

      Panel Name:    Phase Lock Null
      
      Description:   Set phase lock to null.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_phase_master

      ViStatus ri3152b_phase_master (ViSession instrHandle);

  Purpose

      Panel Name:    Phase Master Mode
      
      Description:   Selects the phase master mode of operation
                     for this instrument waveform generator.
      
                     Note that both the master and slave MUST have
                     the same number of points defined for the
                     active signal (standard, user, or sequence
                     mode) for the phase lock to work correctly.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_phase_query

      ViStatus ri3152b_phase_query (ViSession instrHandle,
                                    ViPReal64 phaseOffset,
                                    ViPInt16 phaseState,
                                    ViPInt16 phaseSource);

  Purpose

      Panel Name:    Phase Query
      
      Description:   Provides the phase offset, phase lock, and phase
                     source.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          
           Variable Type: ViSession

      phaseOffset

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Phase Offset
          
          Description:   Holds the phase offset for the instrument.

      phaseState

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Phase State
          
          Description:   Holds the phase state for the instrument.

      phaseSource

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Phase Source
          
          Description:   Holds the phase source for the instrument
          
          Valid Range:   RI3152B_PHASE_SOURCE_MASTER (0)
                         RI3152B_PHASE_SOURCE_SLAVE (1)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_phase_slave

      ViStatus ri3152b_phase_slave (ViSession instrHandle, ViReal64 phase);

  Purpose

      Panel Name:    Phase Slave Mode
      
      Description:   Selects the phase slave mode of operation
                     for this instrument waveform generator.  This
                     instrument will be clocked (via ECLTRG0 and
                     ECLTRG1) by the instrument defined to be the
                     master.
      
                     This allows a phase offset to be added to
                     the slave instrument.
      
                     Note that both the master and slave MUST have
                     the same number of points defined for the
                     active signal (standard, user, or sequence
                     mode) for the phase lock to work correctly.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      phase

          Variable Type       ViReal64

          Control Name:  Phase
          
          Description:   Holds the phase offset for the slave instrument
          
          Valid Range:   0 to 360.  360 is an absolute maximum.  The
                         usable maximum value is:
          
                               360 - (2880 / n), where n is the number
                               of points in the memory which comprises
                               the waveform.  The minimum number of
                               points is 10.
          
                          Ensuring that the programmed phase is within
                          range is left to the user.  Note:  for standard
                          waveforms, the number of points is:
          
                               1000  (if frequency < 100 KHz).
                                     (Sine, Square, Triangle, Pulse,
                                      Ramp)
          
                                500  (if frequency < 100 KHz)
                                     (Sinc, Gaussian, Exponential)
          
                               100E6 / frequency  (if frequency > 100 KHz)
                                     (all waveforms)
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_pll_fine_phase

      ViStatus ri3152b_pll_fine_phase (ViSession instrHandle,
                                       ViReal64 finePhase);

  Purpose

      Panel Name:    Phase Lock Fine Phase Adjust
      
      Description:   Provides the fine phase adjustment
                     for the instrument Phase Lock Loop Mode.
      
                     This is useful when the coarse phase adjust-
                     ment does not provide enough granularity,
                     especially with the Arbitrary Waveform Mode
                     of operation.
      
      Note: Not supported in 3151A, 3151B
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          
           Variable Type: ViSession

      finePhase

          Variable Type       ViReal64

          Control Name:  Fine Phase
          
          Description:   Holds the fine phase offset for the instrument
          
          Valid Range:   -36.0 to 36.0
          
          Note: Not supported in 3151A, 3151B
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_pll_phase

      ViStatus ri3152b_pll_phase (ViSession instrHandle, ViReal64 phase);

  Purpose

      Panel Name:    Phase Lock Phase Adjust
      
      Description:   Provides the (coarse) phase adjustment
                     for the instrument Phase Lock Loop Mode.
                     When the 3152B is outputting "Standard"
                     waveforms,this function will calculate the number
                     of points in the standard waveform and adjust
                     both the coarse and fine phase adjustment
                     to achieve the desired phase adjustment
                     as requested in the "Phase" parameter.
      
                     When the 3152B is outputting the "Arbitrary"
                     waveforms, this function will ONLY adjust
                     the coarse phase.  The fine phase adjustment
                     must be performed using the "Phase Lock Fine
                     Adjust" (ri3152b_pll_fine_phase) function.
      
      Note: Not supported in 3151A, 3151B
      
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          
           Variable Type: ViSession

      phase

          Variable Type       ViReal64

          Control Name:   Phase
          
          Description:    Holds the phase offset for the instrument
          
          Valid Range:    -180.0 to +180.0
          
          Accuracy:       The accuracy depends on the number of samples
                          in the presently selected waveform.  The
                          accuracy is:
          
                              360 / N
          
                          Where N (= the Number of Samples) is determined
                          by the presently selected waveform mode
                          and standard waveform type:
          
                              If the generator is in Arbitrary Waveform
                              Mode, the Number of Samples is the number
                              of points allocated in the present segment.
          
                              If the generator is in Standard Waveform
                              Mode, the Number of Samples (N) is a
                              function of the Waveform Type and the
                              Frequency (F):
          
                              SINE and SQUARE:
          
                                 F <= 200 KHz,           N = 500 Points
                                 200 KHz < F <= 10 MHz,  N = 100 MHz / F
                                 F > 10 MHz,             N = 10
          
                              RAMP, PULSE, GAUSSIAN, and EXPONENTIAL:
          
                                 F <= 100 KHz,           N = 1000 Points
                                 F >  100 KHz,           N = 100 MHz / F
          
                              TRIANGLE and SINC
          
                                 F <= 200 KHz,           N = 500 Points
                                 F >  200 KHz,           N = 100 MHz / F
          
                              These calculations are used in generating
                              the coarse and fine phase adjustments for
                              the standard waveforms.  Only the coarse
                              adjustment is made for Arbitrary Mode
                              waveforms.
          
          Note: Not supported in 3151A, 3151B
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_pll_query

      ViStatus ri3152b_pll_query (ViSession instrHandle,
                                  ViPBoolean phaseLockState,
                                  ViPReal64 coarsePhase, ViPReal64 finePhase,
                                  ViPReal64 extFrequency);

  Purpose

      Panel Name:    Query Phase Lock Loop
      
      Description:   Returns the on/off state of the 3152B Phase Lock
                     Mode, the coarse phase adjustment, the
                     fine phase adjustment, and the external
                     frequency to which the 3152B is locked.
      
      Note: Not supported in 3151A, 3151B
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      phaseLockState

          Variable Type       ViBoolean (passed by reference)

          Control Name:  Phase Lock State
          
          Description:   Indicates whether the phase lock loop
                         mode is ON or OFF for the 3152B.
          
                         VI_TRUE (1)  = Phase Lock Loop ON
                         VI_FALSE (0) = Phase Lock Loop OFF
          
          Note: Not supported in 3151A, 3151B
          
          

      coarsePhase

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Coarse Phase
          
          Description:   Indicates the coarse phase adjustment
                         for a 3152B in phase lock loop mode.
          
          Note: Not supported in 3151A, 3151B
          

      finePhase

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Fine Phase
          
          Description:   Indicates the fine phase adjustment
                         for a 3152B in phase lock loop mode.
          
          Note: Not supported in 3151A, 3151B
          

      extFrequency

          Variable Type       ViReal64 (passed by reference)

          Control Name:  External Frequency
          
          Description:   Indicates the external frequency to which
                         we are locked, as measured by the 3152B.
          
          Note: Not supported in 3151A, 3151B
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_poll

      ViStatus ri3152b_poll (ViSession instrHandle, ViPInt16 statusByte);

  Purpose

      Panel Name:    Poll instrument
      
      Description:   Reads the status byte from the instrument.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      statusByte

          Variable Type       ViInt16 (passed by reference)

          Control Name:       Status Byte
          
          Description:        Returns the IEEE-488.2 defined Status
                              Byte.  The bit assignments are as follows
                              (bit 0 is LSB)
          
          
                      Bit #     Meaning
                      ----------------------------------
                        0       Not used, always 0
                        1       Not used, always 0
                        2       Not used, always 0
                        3       Not used, always 0
                        4       Message Available (MAV).  The device
                                has output which may be read by the
                                system controller
                        5       Standard Event Status Bit (ESB).  One or
                                more of the enabled Standard Event Status
                                Register bits are set.  For an explanation
                                of the Standard Event Status Register, see
                                the description for "ri3152b_read_ESR"
                        6       Master Status Summary (MSS).  This bit
                                indicates that one or more of the enabled
                                Status Byte bits are set.  For further
                                details on enabling Status Byte bits,
                                consult the "ri3152b_set_ESE" function.
                        7       Not used, always 0
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_pulse_data_query

      ViStatus ri3152b_pulse_data_query (ViSession instrHandle,
                                         ViPReal64 delay, ViPReal64 width,
                                         ViPReal64 rise, ViPReal64 fall);

  Purpose

      Panel Name:    Query Pulse Data
      
      Description:   Returns the pulse data.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      delay

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Delay
          
          Description:   Reads the current pulse delay.
          

      width

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Width
          
          Description:    Reads the current pulse width.
          

      rise

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Rise
          
          Description:    Reads the current pulse rise.
          

      fall

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Fall
          
          Description:    Reads the current pulse fall.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_pulse_wave

      ViStatus ri3152b_pulse_wave (ViSession instrHandle, ViReal64 frequency,
                                   ViReal64 amplitude, ViReal64 offset,
                                   ViReal64 delayTime, ViReal64 riseTime,
                                   ViReal64 highTime, ViReal64 fallTime);

  Purpose

      Panel Name:    Pulse Wave
      
      Description:   Select Frequency, Amplitude, Offset, Delay Time
                     Rise Time, High Time and Fall Time.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequency

          Variable Type       ViReal64

          Control Name:  Frequency
          
          Description:   Selects the frequency of the Pulse Wave.
          
          Valid Range:
          10.0E-3 to 100.0E6  (3152B)
          100.0E-6 to 1.0E6   (3152A)
          
          Default:
          1.0E6   (3152B)
          100.0E3 (3152A)
          
          
          

      amplitude

          Variable Type       ViReal64

          Control Name:  Amplitude
          
          Description:   Selects the amplitude (Volts peak-to-peak).
          
          Valid Range:   10.0E-3 to 16.0 V
          
          Default:       5.0 V
          
          Note:          The following relationship must hold between the
                         amplitude and the offset:
          
                               (Amplitude / 2) + ABS(offset) <= 8.0

      offset

          Variable Type       ViReal64

          Control Name:  Offset
          
          Description:   Programs the offset for the presently
                         selected waveform.
          
          Valid Range:
          -7.992 to +7.992   (3152B)
          -7.19 to +7.19     (3152A)
          
          Default:  0.0

      delayTime

          Variable Type       ViReal64

          Control Name:  Delay Time
          
          Description:   Select the Time Delay value.
          
          Valid Range: 0 to 99.999 (3152B)
                       0 to 99.9   (3152A)
          
          Note: the total of delay + rise_time + high_time
                + fall_time  must be less than 100%.
          
          Default: 10.0
          

      riseTime

          Variable Type       ViReal64

          Control Name:  Rise Time
          
          Description:   Select the Rise Time value.
          
          Valid Range:    0 to 99.999  (3152B)
                          0 to 99.9    (3152A)
          
          Note: The total of delay + rise_time + high_time
                + fall_time  must be less than 100%.
          
          Default:  10% of the period

      highTime

          Variable Type       ViReal64

          Control Name:  High Time
          
          Description:   Select the High Time value.
          
          Valid Range:   0 to 99.999 (3152B)
                         0 to 99.9   (3152A)
          
          Note: The total of delay + rise_time + high_time
                + fall_time  must be less than 100%.
          
          Default: 10% of the period

      fallTime

          Variable Type       ViReal64

          Control Name:  Fall Time
          
          Description:   Select the Fall Time value.
          
          Valid Range:   0 to 99.999  (3152B)
                         0 to 99.9    (3152A)
          
          Note: The total of delay + rise_time + high_time
                + fall_time  must be less than 100%.
          
          Default:  10% of the period

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_3D_marker

      ViStatus ri3152b_query_3D_marker (ViSession instrHandle,
                                        ViPInt32 _3DMarker);

  Purpose

      Panel Name:   Query 3D Marker
      
      Description:  Reads the current 3D Marker value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      _3DMarker

          Variable Type       ViInt32 (passed by reference)

          Control Name:  3D Marker
          
          Description:   Reads the current 3D Marker value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_3D_sample_rate

      ViStatus ri3152b_query_3D_sample_rate (ViSession instrHandle,
                                             ViPReal64 _3DSampleRate);

  Purpose

      Panel Name:   Query 3D Sample Rate
      
      Description:  Reads the current 3D Sample Rate value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      _3DSampleRate

          Variable Type       ViReal64 (passed by reference)

          Control Name:  3D Sample Rate
          
          Description:   Reads the current 3D Sample Rate value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_active_channel

      ViStatus ri3152b_query_active_channel (ViSession instrHandle,
                                             ViPInt16 activeChannel);

  Purpose

      Panel Name:   Query Active Channel
      
      Description:  Reads the current active channel.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      activeChannel

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Active Channel
          
          Description:   Reads the current active channel.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_active_segment

      ViStatus ri3152b_query_active_segment (ViSession instrHandle,
                                             ViPInt16 segmentNumber);


  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      segmentNumber

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Number of Sequence
          
          Description:   Returns the current number of active sequence.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_active_sequence

      ViStatus ri3152b_query_active_sequence (ViSession instrHandle,
                                              ViPInt16 number_ofSequence);

  Purpose

      Panel Name:    Query Active Sequence
      
      Description:   Returns the current active sequence.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      number_ofSequence

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Number of Sequence
          
          Description:   Returns the current number of active sequence.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_AHOP_dwell_mode

      ViStatus ri3152b_query_AHOP_dwell_mode (ViSession instrHandle,
                                              ViPInt16 dweelMode);

  Purpose

      Panel Name:   Query AHOP Dwell Mode
      
      Description:  Returns the current AHOP Dwell Mode value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      dweelMode

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Dwell Mode
          
          Description:   Reads the current Dwell Mode value.
          
          Valid Range:   RI3152B_AHOP_DWELL_MODE_FIX  - (0)
                         RI3152B_AHOP_DWELL_MODE_VAR  - (1)
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_AHOP_dwell_time

      ViStatus ri3152b_query_AHOP_dwell_time (ViSession instrHandle,
                                              ViPReal64 dwellTime);

  Purpose

      Panel Name:   Query AHOP Dwell Time
      
      Description:  Reads the current AHOP Dwell Time value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      dwellTime

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Dwell Time
          
          Description:   Reads the current AHOP Dwell Time value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_AHOP_marker

      ViStatus ri3152b_query_AHOP_marker (ViSession instrHandle,
                                          ViPInt16 AHOPMarker);

  Purpose

      Panel Name:   Query AHOP Marker
      
      Description:  Reads the current AHOP Marker value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      AHOPMarker

          Variable Type       ViInt16 (passed by reference)

          Control Name:  AHOP Marker
          
          Description:   Reads the current AHOP Marker value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_AM_modulation_waveform

      ViStatus ri3152b_query_AM_modulation_waveform (ViSession instrHandle,
                                                     ViPInt16 AMModulationWaveform);

  Purpose

      Panel Name:   Query AM Modulation Waveform
      
      Description:  Returns the current AM Modulation Waveform.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      AMModulationWaveform

          Variable Type       ViInt16 (passed by reference)

          Control Name:  AM Modulation Waveform
          
          Description:   Returns the current AM Modulation Waveform.
          
          Valid Range:   RI3152B_AM_WAVE_SINE - (0)
                         RI3152B_AM_WAVE_TRI  - (1)
                         RI3152B_AM_WAVE_SQU  - (2)
                         RI3152B_AM_WAVE_RAMP - (3)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_amplitude

      ViStatus ri3152b_query_amplitude (ViSession instrHandle,
                                        ViPReal64 amplitude);

  Purpose

      Panel Name:    Amplitude Query
      
      Description:   Returns the amplitude setting for the currently
                     active standard function.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      amplitude

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Amplitude
          
          Description:    Reads the current amplitude.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_ASK_baud

      ViStatus ri3152b_query_ASK_baud (ViSession instrHandle,
                                       ViPReal64 ASKBaud);

  Purpose

      Panel Name:  Query ASK Baud
      
      Description: Reads the current ASK Baud value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      ASKBaud

          Variable Type       ViReal64 (passed by reference)

          Control Name:  ASK Baud
          
          Description:   Reads the current ASK Baud value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_ASK_marker

      ViStatus ri3152b_query_ASK_marker (ViSession instrHandle,
                                         ViPInt16 ASKMarker);

  Purpose

      Panel Name:  Query ASK Marker
      
      Description: Returns the current ASK Marker value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      ASKMarker

          Variable Type       ViInt16 (passed by reference)

          Control Name:  ASK Marker
          
          Description:   Returns the current ASK Marker value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_ASK_shifted_amplitude

      ViStatus ri3152b_query_ASK_shifted_amplitude (ViSession instrHandle,
                                                    ViPReal64 shiftedAmplitude);

  Purpose

      Panel Name:  Query ASK Shifted Amplitude
      
      Description: Reads the current Shifted Amplitude value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      shiftedAmplitude

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Shifted Amplitude
          
          Description:   Reads the current Shifted Amplitude value.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_ASK_start_amplitude

      ViStatus ri3152b_query_ASK_start_amplitude (ViSession instrHandle,
                                                  ViPReal64 startAmplitude);

  Purpose

      Panel Name:   Query ASK Start Amplitude
      
      Description:  Returns the current value for ASK Start Amplitude.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      startAmplitude

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Start Amplitude
          
          Description:   Returns the curent value for the ASK Start
                         Amplitude.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_carrier_baseline

      ViStatus ri3152b_query_carrier_baseline (ViSession instrHandle,
                                               ViPInt16 carrierBaseline);

  Purpose

      Panel Name:    Query Carrier Baseline
      
      Description:   Returns the current type of carrier baseline.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      carrierBaseline

          Variable Type       ViInt16 (passed by reference)

          Control Name: Carrier Baseline
          
          Description:  Returns the current type of carrier baseline.
          
          Valid Range:  RI3152B_CARR_BASELINE_CARR - (0)
                        RI3152B_CARR_BASELINE_DC   - (1)
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_carrier_frequency

      ViStatus ri3152b_query_carrier_frequency (ViSession instrHandle,
                                                ViPReal64 frequency);

  Purpose

      Panel Name:    Query Carrier Frequency
      
      Description:   Returns the current value of CW frequency.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      frequency

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Frequency
          
          Description:   Reads the current value of CW frequency.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_counter_display_mode

      ViStatus ri3152b_query_counter_display_mode (ViSession instrHandle,
                                                   ViPInt16 counterDisplayMode);

  Purpose

      Panel Name:  Query Counter Display Mode
      
      Description: Reads the current Counter Display Mode value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      counterDisplayMode

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Counter Display Mode
          
          Description:   Reads the current Counter Display Mode value.
          
          Valid Range:   RI3152B_COUNT_DISP_MODE_NORMAL  - (0)
                         RI3152B_COUNT_DISP_MODE_HOLD    - (1)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_counter_function

      ViStatus ri3152b_query_counter_function (ViSession instrHandle,
                                               ViPInt16 counterFunction);

  Purpose

      Panel Name:  Query Counter Function
      
      Description: Reads the current Counter Function value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      counterFunction

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Counter Function
          
          Description:   Reads the current Counter Function value.
          
          Valid Range:   RI3152B_COUNT_FUNC_FREQ  - (0)
                         RI3152B_COUNT_FUNC_PER   - (1)
                         RI3152B_COUNT_FUNC_APER  - (2)
                         RI3152B_COUNT_FUNC_PULSE - (3)
                         RI3152B_COUNT_FUNC_TOT  -  (4)
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_counter_gate_time

      ViStatus ri3152b_query_counter_gate_time (ViSession instrHandle,
                                                ViPReal64 counterGateTime);

  Purpose

      Panel Name:  Query Counter Gate Time
      
      Description: Reads the current Counter Gate Time value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      counterGateTime

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Counter Gate Time
          
          Description:   Reads the current Counter Gate Time value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_couple

      ViStatus ri3152b_query_couple (ViSession instrHandle, ViPInt16 state);

  Purpose

      Panel Name:    Query Couple
      
      Description:   Reads the current Couple State.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:      State
          
          Description:       Returns the couple state.
                             0 = couple is off
                             1 = couple is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_couple_delay

      ViStatus ri3152b_query_couple_delay (ViSession instrHandle,
                                           ViPReal64 coupleDelay);

  Purpose

      Panel Name:    Query Couple Delay
      
      Description:   Reads the current Couple Delay value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      coupleDelay

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Couple Delay
          
          Description:   Reads the current Couple Delay value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_couple_mode

      ViStatus ri3152b_query_couple_mode (ViSession instrHandle,
                                          ViPInt16 coupleMode);

  Purpose

      Panel Name:   Query Couple Mode
      
      Description:  Reads the current Couple Mode value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      coupleMode

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Couple Mode
          
          Description:   Reads the current Couple Mode value.
          
          Valid Range:   RI3152B_COUPLE_MODE_MASTER - (0)
                         RI3152B_COUPLE_MODE_SLAVE  - (1)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_couple_path

      ViStatus ri3152b_query_couple_path (ViSession instrHandle,
                                          ViPInt16 couplePath);

  Purpose

      Panel Name:   Query Couple Path
      
      Description:  Reads the current Couple Path value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      couplePath

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Couple Path
          
          Description:   Reads the current Couple Path value.
          
          Valid Range:   RI3152B_COUPLE_PATH_ADJ  - (0)
                         RI3152B_COUPLE_PATH_ECLT - (1)
                         RI3152B_COUPLE_PATH_LBUS - (2)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_dc_level

      ViStatus ri3152b_query_dc_level (ViSession instrHandle,
                                       ViPReal64 DCLevel);

  Purpose

      Panel Name:    Query DC Level
      
      Description:   Returns the value of DC level.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      DCLevel

          Variable Type       ViReal64 (passed by reference)

          Control Name:   DC Level
          
          Description:    Reads the current DC Level value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_double_pulse_delay

      ViStatus ri3152b_query_double_pulse_delay (ViSession instrHandle,
                                                 ViPReal64 doublePulseDelay);

  Purpose

      Panel Name:   Query Double Pulse Delay
      
      Description:  Reads the current Double Pulse Delay value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      doublePulseDelay

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Double Pulse Delay
          
          Description:   Reads the current Double Pulse Delay value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_double_pulse_state

      ViStatus ri3152b_query_double_pulse_state (ViSession instrHandle,
                                                 ViPInt16 state);

  Purpose

      Panel Name:   Query Double Pulse State
      
      Description:  Reads the current Double Pulse State value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:  State
          
          Description:   Reads the current Double Pulse State value.
          
          Valid Values:  1 - Double Pulse is ON
                         0 - Double Pulse is OFF
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_FHOP_dwell_mode

      ViStatus ri3152b_query_FHOP_dwell_mode (ViSession instrHandle,
                                              ViPInt16 dweelMode);

  Purpose

      Panel Name:   Query FHOP Dwell Mode
      
      Description:  Reads the current FHOP Dwell Mode value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      dweelMode

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Dwell Mode
          
          Description:   Reads the current Dwell Mode value.
          
          Valid Range:   RI3152B_FHOP_DWELL_MODE_FIX  - (0)
                         RI3152B_FHOP_DWELL_MODE_VAR  - (1)
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_FHOP_dwell_time

      ViStatus ri3152b_query_FHOP_dwell_time (ViSession instrHandle,
                                              ViPReal64 dwellTime);

  Purpose

      Panel Name:   Query FHOP Dwell Time
      
      Description:  Reads the current FHOP Dwell Time value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      dwellTime

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Dwell Time
          
          Description:   Reads the current FHOP Dwell Time value.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_FHOP_marker

      ViStatus ri3152b_query_FHOP_marker (ViSession instrHandle,
                                          ViPInt16 FHOPMarker);

  Purpose

      Panel Name:   Query FHOP Marker
      
      Description:  Reads the current FHOP Marker value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FHOPMarker

          Variable Type       ViInt16 (passed by reference)

          Control Name:  FHOP Marker
          
          Description:   Reads the current FHOP Marker value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_filter

      ViStatus ri3152b_query_filter (ViSession instrHandle, ViPInt16 filter);

  Purpose

      Control Name:  Query Filter
      
      Description:   Reads the current Filter value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      filter

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Filter
          
          Description:   Reads the current Filter value.
          
          Valid Range:   RI3152B_FILTER_20MHZ  - (1)
                         RI3152B_FILTER_25MHZ  - (2)
                         RI3152B_FILTER_50MHZ  - (3)
                         RI3152B_FILTER_60MHZ  - (4)
                         RI3152B_FILTER_120MHZ - (5)
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_filter_state

      ViStatus ri3152b_query_filter_state (ViSession instrHandle,
                                           ViPInt16 state);

  Purpose

      Panel Name:    Query Filter
      
      Description:   Reads the current Filter State.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:       State
          
          Description:        Returns the filter state.
                              0 = filter is off
                              1 = filter is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_FM_frequency_deviation

      ViStatus ri3152b_query_FM_frequency_deviation (ViSession instrHandle,
                                                     ViPReal64 frequencyDeviation);

  Purpose

      Panel Name:   Query FM Frequency Deviation
      
      Description:  Returns the current value of  the frequency
                    deviation.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequencyDeviation

          Variable Type       ViReal64 (passed by reference)

          Control Name:      Frequency Deviation
          
          Description:       Reads the current value of the frequency
                             deviation.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_FM_marker

      ViStatus ri3152b_query_FM_marker (ViSession instrHandle,
                                        ViPReal64 FMMarker);

  Purpose

      Panel Name:   Query FM Marker
      
      Description:  Returns the current value of marker frequency
                    position.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FMMarker

          Variable Type       ViReal64 (passed by reference)

          Control Name:  FM Marker
          
          Description:   Returns the current value of marker frequency
                         position.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_FM_modulation_frequency

      ViStatus ri3152b_query_FM_modulation_frequency (ViSession instrHandle,
                                                      ViPReal64 FMModulationFrequency);

  Purpose

      Panel Name:   Query FM Modulation Frequency
      
      Description:  Returns the current value of the FM modulation
                    frequency.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FMModulationFrequency

          Variable Type       ViReal64 (passed by reference)

          Control Name:  FM Modulation Frequency
          
          Description:   Returns the current value of the FM modulation
                         frequency.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_FM_modulation_SCLK

      ViStatus ri3152b_query_FM_modulation_SCLK (ViSession instrHandle,
                                                 ViPReal64 FMModulationSCLK);

  Purpose

      Panel Name:   Query FM Modulation SCLK
      
      Description:  Returns the current value of FM modulation SCLK.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FMModulationSCLK

          Variable Type       ViReal64 (passed by reference)

          Control Name:  FM Modulation SCLK
          
          Description:   Returns the current value of FM modulation SCLK.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_FM_modulation_waveform

      ViStatus ri3152b_query_FM_modulation_waveform (ViSession instrHandle,
                                                     ViPInt16 FMModulationWaveform);

  Purpose

      Panel Name:   Query FM Modulation Waveform
      
      Description:  Returns the current waveform of FM modulation.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FMModulationWaveform

          Variable Type       ViInt16 (passed by reference)

          Control Name:  FM Modulation Waveform
          
          Description:   Returns the current waveform of FM modulation.
          
          Valid Range:   RI3152B_FM_WAVE_SINE - (0)
                         RI3152B_FM_WAVE_TRI  - (1)
                         RI3152B_FM_WAVE_SQU  - (2)
                         RI3152B_FM_WAVE_RAMP - (3)
                         RI3152B_FM_WAVE_ARB  - (4)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_format_instrument

      ViStatus ri3152b_query_format_instrument (ViSession instrHandle,
                                                ViPInt16 formatInstrument);

  Purpose

      Panel Name:   Query Format Instrument
      
      Description:  Reads the current Format Instrument value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      formatInstrument

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Format Instrument
          
          Description:   Reads the current Format Instrument value.
          
          Valid Range:   RI3152B_FORMAT_INSTR_LEG - (0)
                         RI3152B_FORMAT_INSTR_MOD - (1)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_FSK_baud

      ViStatus ri3152b_query_FSK_baud (ViSession instrHandle,
                                       ViPReal64 FSKBaud);

  Purpose

      Panel Name:   Query FSK Baud
      
      Description:  Returns the current value of FSK Baud.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FSKBaud

          Variable Type       ViReal64 (passed by reference)

          Control Name:  FSK Baud
          
          Description:   Returns the current value of FSK Baud.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_FSK_marker

      ViStatus ri3152b_query_FSK_marker (ViSession instrHandle,
                                         ViPInt16 FSKMarker);

  Purpose

      Panel Name:   Query FSK Marker
      
      Description:  Returns the current value of FSK Marker.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FSKMarker

          Variable Type       ViInt16 (passed by reference)

          Control Name:  FSK Marker
          
          Description:   Returns the current value of FSK marker.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_FSK_shifted_frequency

      ViStatus ri3152b_query_FSK_shifted_frequency (ViSession instrHandle,
                                                    ViPReal64 shiftedFrequency);

  Purpose

      Panel Name:   Query FSK Shifted Frequency
      
      Description:  Returns the current value of FSK Shifted
                    Frequency.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      shiftedFrequency

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Shifted Frequency
          
          Description:   Returns the current value of FSK Shifted
                         Frequency.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_gate_mode

      ViStatus ri3152b_query_gate_mode (ViSession instrHandle,
                                        ViPInt16 gateMode);

  Purpose

      Panel Name:    Query Gate Mode
      
      Description:   Returns the current value of gate mode.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      gateMode

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Gate Mode
          
          Description:   Returns the current value of gate mode.
          
          Valid Range:   RI3152B_GATE_MODE_LEV  - (0)
                         RI3152B_GATE_MODE_TRAN - (1)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_half_cycle_delay

      ViStatus ri3152b_query_half_cycle_delay (ViSession instrHandle,
                                               ViPReal64 halfCycleDelay);

  Purpose

      Panel Name:  Query Half Cycle Delay
      
      Description: Returns the current interval of which the output
                   idles between half cycles.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      halfCycleDelay

          Variable Type       ViReal64 (passed by reference)

          Control Name: Half Cycle Delay
          
          Description:  Returns the current delay time interval between
                        half cycles.
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_half_cycle_duty_cycle

      ViStatus ri3152b_query_half_cycle_duty_cycle (ViSession instrHandle,
                                                    ViPReal64 halfCycleDutyCycle);

  Purpose

      Panel Name:  Query Half Cycle Duty Cycle
      
      Description: Returns the current Duty Cycle value for the Half
                   Cycle Square.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      halfCycleDutyCycle

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Half Cycle Duty Cycle
          
          Description:   Returns the current Duty Cycle value for the Half
                         Cycle Square.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_half_cycle_frequency

      ViStatus ri3152b_query_half_cycle_frequency (ViSession instrHandle,
                                                   ViPReal64 halfCycleFrequency);

  Purpose

      Panel Name:  Query Half Cycle Frequency
      
      Description: Returns the current frequency value of the half
                   cycle waveforms.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      halfCycleFrequency

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Half Cycle Frequency
          
          Description:   Returns the current frequency value of the half
                         cycle waveform.
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_half_cycle_phase

      ViStatus ri3152b_query_half_cycle_phase (ViSession instrHandle,
                                               ViPReal64 halfCyclePhase);

  Purpose

      Panel Name:  Query Half Cycle Phase
      
      Description: Returns the current phase value for the Half Cycle
                   Waves: Sine and Triangle.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      halfCyclePhase

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Half Cycle Phase
          
          Description:   Returns the current phase value for the Half
                         Cycle Waves: Sine and Triangle.
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_half_cycle_waveform

      ViStatus ri3152b_query_half_cycle_waveform (ViSession instrHandle,
                                                  ViPInt16 halfCycleWaveform);

  Purpose

      Panel Name:    Query Half cycle waveform
      
      Description:   Returns the current half cycle waveform.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      halfCycleWaveform

          Variable Type       ViInt16 (passed by reference)

          Control Name : Half Cycle Waveform
          
          Description :  Returns the current half cycle waveform.
          
          Valid Range :  RI3152B_HALF_CYCLE_WAVE_SINE - (0)
                         RI3152B_HALF_CYCLE_WAVE_TRI  - (1)
                         RI3152B_HALF_CYCLE_WAVE_SQU  - (2)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_instrument_data

      ViStatus ri3152b_query_instrument_data (ViSession instrHandle,
                                              ViChar _VI_FAR commandBuffer[],
                                              ViChar _VI_FAR readBuffer[],
                                              ViInt32 count_toRead);

  Purpose

      Panel Name:    Query Instrument Data
      
      Description:   This function writes a user-specified string to
                     the instrument and then reads data from the
                     instrument.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

      commandBuffer

          Variable Type       ViChar []

          Control Name:  Command Buffer
          
          Description:   Pass the string to be written to the instrument.

      readBuffer

          Variable Type       ViChar []

          Control Name:  Read Buffer
          
          Description:   After this function executes, this parameter
                         contains the data that was read from the
                         instrument.
          

      count_toRead

          Variable Type       ViInt32

          Control Name:  Count to Read
          
          Description:   Pass the maximum number of bytes to read from the
                         instruments.
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_load_impedance

      ViStatus ri3152b_query_load_impedance (ViSession instrHandle,
                                             ViPReal64 impedance);

  Purpose

      Panel Name:    Query Load Impedance
      
      Description:   Returns the output impedance.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      impedance

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Impedance
          
          Description:   Returns the impedance value.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_modulation_type

      ViStatus ri3152b_query_modulation_type (ViSession instrHandle,
                                              ViPInt16 modulationType);

  Purpose

      Panel Name:    Query Modulation Type
      
      Description:   Returns one of the Modulation type:
                     OFF, AM, FM, SWE, FSK, ASK, PSK, FHOP,
                     AHOP, 3D.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      modulationType

          Variable Type       ViInt16 (passed by reference)

          Control Name: Modulation Type
          
          Description:  Returns one of the Modulation type.
          
          Valid Range:  RI3152B_MOD_TYPE_OFF  - (0)
                        RI3152B_MOD_TYPE_AM   - (1)
                        RI3152B_MOD_TYPE_FM   - (2)
                        RI3152B_MOD_TYPE_SWE  - (3)
                        RI3152B_MOD_TYPE_FSK  - (4)
                        RI3152B_MOD_TYPE_ASK  - (5)
                        RI3152B_MOD_TYPE_PSK  - (6)
                        RI3152B_MOD_TYPE_FHOP - (7)
                        RI3152B_MOD_TYPE_AHOP - (8)
                        RI3152B_MOD_TYPE_3D   - (9)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_offset

      ViStatus ri3152b_query_offset (ViSession instrHandle, ViPReal64 offset);

  Purpose

      Panel Name:    Offset Query
      
      Description:   Returns the Offset setting for the currently
                     active standard function.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      offset

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Offset
          
          Description:    Reads the current Offset.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_operating_mode

      ViStatus ri3152b_query_operating_mode (ViSession instrHandle,
                                             ViPInt16 operatingMode);

  Purpose

      Panel Name:    Operating Mode Query
      
      Description:   Returns the current operating mode.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      operatingMode

          Variable Type       ViInt16 (passed by reference)

          Control Name:      Operating Mode
          
          Description:       Reads the presently selected
                             Operating Mode.  The reply will be one
                             of the following:
          
                             RI3152B_MODE_CONT (0)
                             RI3152B_MODE_TRIG (1)
                             RI3152B_MODE_GATE (2)
                             RI3152B_MODE_BURST(3)
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_output

      ViStatus ri3152b_query_output (ViSession instrHandle, ViPInt16 state);

  Purpose

      Panel Name:    Output Query
      
      Description:   Reads the output state.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:       State
          
          Description:        Returns the output state.
                              0 = output is off
                              1 = output is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_output_eclt

      ViStatus ri3152b_query_output_eclt (ViSession instrHandle,
                                          ViPInt16 state);

  Purpose

      Panel Name:    Output ECLT Query
      
      Description:   Reads the state of output ECLT0.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:  State
          
          Description:   Returns the output ECLT0 state.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_output_ECLT1

      ViStatus ri3152b_query_output_ECLT1 (ViSession instrHandle,
                                           ViPInt16 state);

  Purpose

      Panel Name:    Output ECLT Query
      
      Description:   Reads the state of output ECLT1.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:  State
          
          Description:   Returns the output ECLT1 state.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_output_load_calibration

      ViStatus ri3152b_query_output_load_calibration (ViSession instrHandle,
                                                      ViPInt16 outputLoadCalibration);

  Purpose

      Panel Name:    Query Output Load Calibration
      
      Description:   Returns the current output load calibration.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      outputLoadCalibration

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Output Load Calibration
          
          Description:   Reads the current output load calibration.
                         The reply will be one of the following:
          
                         RI3152B_OUTPUT_LOAD_CAL_50 (0) - 50 ohms
          
                         RI3152B_OUTPUT_LOAD_CAL_1M (1) -  1 MOhms
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_output_shunt

      ViStatus ri3152b_query_output_shunt (ViSession instrHandle,
                                           ViPInt16 state);

  Purpose

      Panel Name:    Output Shunt Query
      
      Description:   Reads the output shunt state.
      
      Note: Not supported in 3152B
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:      State
          
          Description:       Returns the output shunt state.
                             0 = shunt is off
                             1 = shunt is on
          
          Note: Not supported in 3152B

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_phase2_source

      ViStatus ri3152b_query_phase2_source (ViSession instrHandle,
                                            ViPInt16 phase2Source);

  Purpose

      Panel Name:   Query Phase2 Source
      
      Description:  Reads the current Phase2 Source value.
      
      Note: Not supported in 3151A, 3151B

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      phase2Source

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Phase2 Source
          
          Description:   Reads the current Phase2 Source value.
          
          Valid Range:   RI3152B_PHASE2_SOURCE_EXT - (0)
                         RI3152B_PHASE2_SOURCE_ECLT0 - (1)
                         RI3152B_PHASE2_SOURCE_TTLT0 - (2)
                         RI3152B_PHASE2_SOURCE_TTLT1 - (3)
                         RI3152B_PHASE2_SOURCE_TTLT2 - (4)
                         RI3152B_PHASE2_SOURCE_TTLT3 - (5)
                         RI3152B_PHASE2_SOURCE_TTLT4 - (6)
                         RI3152B_PHASE2_SOURCE_TTLT5 - (7)
                         RI3152B_PHASE2_SOURCE_TTLT6 - (8)
                         RI3152B_PHASE2_SOURCE_TTLT7 - (9)
          
          Note: Not supported in 3151A, 3151B
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_phase_offset

      ViStatus ri3152b_query_phase_offset (ViSession instrHandle,
                                           ViPInt32 phaseOffset);

  Purpose

      Panel Name:   Query Phase Offset
      
      Description:  Returns the start phase offset between two
                    instruments in units of waveform points.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      phaseOffset

          Variable Type       ViInt32 (passed by reference)

          Control Name: Phase Offset
          
          Description:  Returns the start phase offset between two
                        instruments in units of waveform points.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_PSK_marker

      ViStatus ri3152b_query_PSK_marker (ViSession instrHandle,
                                         ViPInt16 PSKMarker);

  Purpose

      Panel Name:   Query PSK Marker
      
      Description:  Reads the current PSK marker value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      PSKMarker

          Variable Type       ViInt16 (passed by reference)

          Control Name:  PSK Marker
          
          Description:   Reads the current PSK Marker value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_PSK_rate

      ViStatus ri3152b_query_PSK_rate (ViSession instrHandle,
                                       ViPReal64 PSKRate);

  Purpose

      Panel Name:   Query PSK Rate
      
      Description:  Reads the current PSK Rate value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      PSKRate

          Variable Type       ViReal64 (passed by reference)

          Control Name:  PSK Rate
          
          Description:   Reads the current PSK Rate value.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_PSK_shifted_phase

      ViStatus ri3152b_query_PSK_shifted_phase (ViSession instrHandle,
                                                ViPReal64 shiftedPhase);

  Purpose

      Panel Name:   Query PSK Shifted Phase
      
      Description:  Returns the current PSK Shifted Phase value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      shiftedPhase

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Shifted Phase
          
          Description:   Reads the current PSK Shifted Phase value.
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_PSK_start_phase

      ViStatus ri3152b_query_PSK_start_phase (ViSession instrHandle,
                                              ViPReal64 startPhase);

  Purpose

      Panel Name:   Query PSK Start Phase
      
      Description:  Returns the current PSK Start Phase value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      startPhase

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Start Phase
          
          Description:   Reads the current PSK Start Phase value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_pulse_delay

      ViStatus ri3152b_query_pulse_delay (ViSession instrHandle,
                                          ViPReal64 pulseDelay);

  Purpose

      Panel Name:   Query Pulse Delay
      
      Description:  Reads the current Pulse Delay value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      pulseDelay

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Pulse Delay
          
          Description:   Reads the current Pulse Delay value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_pulse_fall_time

      ViStatus ri3152b_query_pulse_fall_time (ViSession instrHandle,
                                              ViPReal64 fallTime);

  Purpose

      Panel Name:  Query Pulse Fall Time
      
      Description: Reads the current Pulse Fall Time value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      fallTime

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Fall Time
          
          Description:   Reads the current Pulse Fall Time value.
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_pulse_high_level

      ViStatus ri3152b_query_pulse_high_level (ViSession instrHandle,
                                               ViPReal64 highLevel);

  Purpose

      Panel Name:   Query Pulse High Level
      
      Description:  Reads the current Pulse High Level value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      highLevel

          Variable Type       ViReal64 (passed by reference)

          Control Name:  High Level
          
          Description:   Reads the current Pulse High Level value.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_pulse_high_time

      ViStatus ri3152b_query_pulse_high_time (ViSession instrHandle,
                                              ViPReal64 highTime);

  Purpose

      Panel Name:   Query Pulse High Time
      
      Description:  Reads the current Pulse High Time value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      highTime

          Variable Type       ViReal64 (passed by reference)

          Control Name:  High Time
          
          Description:   Reads the current Pulse High Time value.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_pulse_low_level

      ViStatus ri3152b_query_pulse_low_level (ViSession instrHandle,
                                              ViPReal64 lowLevel);

  Purpose

      Panel Name:   Query Pulse Low Level
      
      Description:  Reads the current Pulse Low Level value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      lowLevel

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Low Level
          
          Description:   Reads the current Pulse Low Level value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_pulse_period

      ViStatus ri3152b_query_pulse_period (ViSession instrHandle,
                                           ViPReal64 pulsePeriod);

  Purpose

      Panel Name:   Query Pulse Period
      
      Description:  Reads the current Pulse Period value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      pulsePeriod

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Pulse Period
          
          Description:   Reads the current Pulse Period value.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_pulse_polarity

      ViStatus ri3152b_query_pulse_polarity (ViSession instrHandle,
                                             ViPInt16 pulsePolarity);

  Purpose

      Panel Name:   Query Pulse Polarity
      
      Description:  Returns the current Pulse Polarity value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      pulsePolarity

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Pulse Polarity
          
          Description:   Reads the current Pulse Polarity value.
          
          Valid Range:   RI3152B_DPULSE_POL_NORMAL - (0)
                         RI3152B_DPULSE_POL_COMP   - (1)
                         RI3152B_DPULSE_POL_INV    - (2)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_pulse_rise_time

      ViStatus ri3152b_query_pulse_rise_time (ViSession instrHandle,
                                              ViPReal64 riseTime);

  Purpose

      Panel Name:   Query Pulse Rise Time
      
      Description:  Reads the current Pulse Rise Time value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      riseTime

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Rise Time
          
          Description:   Reads the current Pulse Rise Time value.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_re_trigger_delay_state

      ViStatus ri3152b_query_re_trigger_delay_state (ViSession instrHandle,
                                                     ViPInt16 reTrigDelayState);

  Purpose

      Panel Name:    Re-Trigger Delay State Query
      
      Description:   Reads the state of re-trigger delay.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      reTrigDelayState

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Re-Trig Delay State
          
          Description:   Returns the re-trigger delay state.
          
                         0 = Re-Trigger is OFF
                         1 = Re-Trigger is ON

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_re_trigger_time

      ViStatus ri3152b_query_re_trigger_time (ViSession instrHandle,
                                              ViPReal64 reTriggerTime);

  Purpose

      Panel Name:    Query Re-Trigger Time
      
      Description:   Returns the current value of the re-trigger
                     period.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      reTriggerTime

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Re-Trigger Time
          
          Description:   Reads the current value of re-trigger time.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_ref_clock_source

      ViStatus ri3152b_query_ref_clock_source (ViSession instrHandle,
                                               ViPInt16 refClockSource);

  Purpose

      Panel Name:    Query Ref Clock Source
      
      Description:   Returns the reference source for the sample
                     clock generator.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      refClockSource

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Ref Clock Source
          
          Description:   Returns the reference clock.
          
          Valid Range:
           RI3152B_REF_SOURCE_INT (0) - internal clock source
           RI3152B_REF_SOURCE_EXT (1) - external clock source
           RI3152B_REF_SOURCE_CLK10 (2) - clk10 clock source
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_sampling_freq

      ViStatus ri3152b_query_sampling_freq (ViSession instrHandle,
                                            ViPReal64 samplingFrequency);

  Purpose

      Panel Name:    Sampling Frequency Query
      
      Description:   Returns the sampling frequency.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      samplingFrequency

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Sampling Frequency
          
          Description:    Reads the current sampling frequency.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_sequence_sync_type

      ViStatus ri3152b_query_sequence_sync_type (ViSession instrHandle,
                                                 ViPInt16 syncType);

  Purpose

      Panel Name:    Query Sequence Sync Type
      
      Description:   Returns one of the sync types: BIT or  LCOM when
                     instrument is in sequence mode.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      syncType

          Variable Type       ViInt16 (passed by reference)

          Control Name : Sync Type
          
          Description :  Returns one of the sync types: BIT or  LCOM when
                         instrument is in sequence mode.
          
          Valid Range :  RI3152B_SEQ_SYNC_LCOM - (0)
                         RI3152B_SEQ_SYNC_BIT  - (1)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_shared_flag

      ViStatus ri3152b_query_shared_flag (ViSession instrHandle,
                                          ViPInt16 state);

  Purpose

      Panel Name:    Query Shared Flag
      
      Description:   Reads the current Shared Flag State.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:       State
          
          Description:        Returns the shared flag state.
                              0 = shared flag state is off
                              1 = shared flag state is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_sweep_direction

      ViStatus ri3152b_query_sweep_direction (ViSession instrHandle,
                                              ViPInt16 sweepDirecion);

  Purpose

      Panel Name:    Query Sweep Direction
      
      Description:   Returns the current sweep direction.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sweepDirecion

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Sweep Direction
          
          Description:   Reads the current sweep direction.
          
          Valid Range:   RI3152B_SWEEP_DIR_UP   - (0)
                         RI3152B_SWEEP_DIR_DOWN - (1)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_sweep_freq_marker

      ViStatus ri3152b_query_sweep_freq_marker (ViSession instrHandle,
                                                ViPReal64 sweepFreqMarker);

  Purpose

      Panel Name:    Query Sweep Frequency Marker
      
      Description:   Returns the present sweep frequency marker.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sweepFreqMarker

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Sweep Freq Marker
          
          Description:   Reads the current sweep frequency marker.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_sweep_freq_raster

      ViStatus ri3152b_query_sweep_freq_raster (ViSession instrHandle,
                                                ViPReal64 sweepFreqRaster);

  Purpose

      Panel Name:    Query Sweep Frequency Raster
      
      Description:   Returns the present sweep frequency raster.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sweepFreqRaster

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Sweep Freq Raster
          
          Description:    Reads the current sweep frequency raster.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_sweep_freq_start

      ViStatus ri3152b_query_sweep_freq_start (ViSession instrHandle,
                                               ViPReal64 sweepFreqStart);

  Purpose

      Panel Name:    Query Sweep Frequency Start
      
      Description:   Returns the present sweep frequency start.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sweepFreqStart

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Sweep Freq Start
          
          Description:    Reads the current sweep frequency start.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_sweep_freq_stop

      ViStatus ri3152b_query_sweep_freq_stop (ViSession instrHandle,
                                              ViPReal64 sweepFreqStop);

  Purpose

      Panel Name:    Query Sweep Frequency Stop
      
      Description:   Returns the present sweep frequency stop.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sweepFreqStop

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Sweep Freq Stop
          
          Description:    Reads the current sweep frequency stop.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_sweep_spacing

      ViStatus ri3152b_query_sweep_spacing (ViSession instrHandle,
                                            ViPInt16 sweepSpacing);

  Purpose

      Panel Name:    Query Sweep Spacing
      
      Description:   Returns the current sweep spacing.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sweepSpacing

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Sweep Spacing
          
          Description:   Reads the current sweep spacing.
          
          Valid Range:   RI3152B_SWEEP_SPAC_LIN - (0)
                         RI3152B_SWEEP_SPAC_LOG - (1)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_sweep_step

      ViStatus ri3152b_query_sweep_step (ViSession instrHandle,
                                         ViPInt16 sweepStep);

  Purpose

      Panel Name:    Query Sweep Step
      
      Description:   Returns the present sweep step.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sweepStep

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Sweep Step
          
          Description:   Reads the current sweep step.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_sweep_time

      ViStatus ri3152b_query_sweep_time (ViSession instrHandle,
                                         ViPReal64 sweepTime);

  Purpose

      Panel Name:    Query Sweep Time
      
      Description:   Returns the present sweep time.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sweepTime

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Sweep Time
          
          Description:    Reads the current sweep time.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_trigger_delay_time

      ViStatus ri3152b_query_trigger_delay_time (ViSession instrHandle,
                                                 ViPReal64 trigger_delay_time);

  Purpose

      Panel Name:    Query Trigger Delay Time
      
      Description:   Returns the current trigger delay time value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      trigger_delay_time

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Trigger Delay Time
          
          Description:   Returns the current trigger delay time.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_TTLT0_state

      ViStatus ri3152b_query_TTLT0_state (ViSession instrHandle,
                                          ViPInt16 state);

  Purpose

      Panel Name:    Query TTLT0 State
      
      Description:   Reads the output TTLT0 state.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:       State
          
          Description:        Returns the output TTLT0 state.
                              0 = TTLT0 is off
                              1 = TTLT0 is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_TTLT1_state

      ViStatus ri3152b_query_TTLT1_state (ViSession instrHandle,
                                          ViPInt16 state);

  Purpose

      Panel Name:    Query TTLT1 State
      
      Description:   Reads the output TTLT1 state.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:       State
          
          Description:        Returns the output TTLT0 state.
                              0 = TTLT0 is off
                              1 = TTLT0 is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_TTLT2_state

      ViStatus ri3152b_query_TTLT2_state (ViSession instrHandle,
                                          ViPInt16 state);

  Purpose

      Panel Name:    Query TTLT2 State
      
      Description:   Reads the output TTLT2 state.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:       State
          
          Description:        Returns the output TTLT2 state.
                              0 = TTLT2 is off
                              1 = TTLT2 is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_TTLT3_state

      ViStatus ri3152b_query_TTLT3_state (ViSession instrHandle,
                                          ViPInt16 state);

  Purpose

      Panel Name:    Query TTLT3 State
      
      Description:   Reads the output TTLT3 state.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:       State
          
          Description:        Returns the output TTLT3 state.
                              0 = TTLT3 is off
                              1 = TTLT3 is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_TTLT4_state

      ViStatus ri3152b_query_TTLT4_state (ViSession instrHandle,
                                          ViPInt16 state);

  Purpose

      Panel Name:    Query TTLT4 State
      
      Description:   Reads the output TTLT4 state.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:       State
          
          Description:        Returns the output TTLT4 state.
                              0 = TTLT4 is off
                              1 = TTLT4 is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_TTLT5_state

      ViStatus ri3152b_query_TTLT5_state (ViSession instrHandle,
                                          ViPInt16 state);

  Purpose

      Panel Name:    Query TTLT5 State
      
      Description:   Reads the output TTLT5 state.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:       State
          
          Description:        Returns the output TTLT5 state.
                              0 = TTLT5 is off
                              1 = TTLT5 is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_TTLT6_state

      ViStatus ri3152b_query_TTLT6_state (ViSession instrHandle,
                                          ViPInt16 state);

  Purpose

      Panel Name:    Query TTLT6 State
      
      Description:   Reads the output TTLT6 state.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:       State
          
          Description:        Returns the output TTLT6 state.
                              0 = TTLT6 is off
                              1 = TTLT6 is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_TTLT7_state

      ViStatus ri3152b_query_TTLT7_state (ViSession instrHandle,
                                          ViPInt16 state);

  Purpose

      Panel Name:    Query TTLT7 State
      
      Description:   Reads the output TTLT7 state.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      state

          Variable Type       ViInt16 (passed by reference)

          Control Name:       State
          
          Description:        Returns the output TTLT7 state.
                              0 = TTLT7 is off
                              1 = TTLT7 is on

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_query_wave_resolution

      ViStatus ri3152b_query_wave_resolution (ViSession instrHandle,
                                              ViPInt16 waveResolution);

  Purpose

      Panel Name:    Query Wave Resolution
      
      Description:   Reads the current wave resolution value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      waveResolution

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Wave Resolution
          
          Description:   Reads the current Wave Resolution value.
          
          Valid Range:   RI3152B_WAVE_RES_16BIT - (0)
                         RI3152B_WAVE_RES_12BIT - (1)
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_ramp_data_query

      ViStatus ri3152b_ramp_data_query (ViSession instrHandle,
                                        ViPReal64 delay, ViPReal64 rise,
                                        ViPReal64 fall);

  Purpose

      Panel Name:    Query Ramp Data
      
      Description:   Returns the ramp data.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      delay

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Delay
          
          Description:    Reads the current ramp delay.
          

      rise

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Rise
          
          Description:    Reads the current ramp rise.
          

      fall

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Fall
          
          Description:    Reads the current ramp fall.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_ramp_wave

      ViStatus ri3152b_ramp_wave (ViSession instrHandle, ViReal64 frequency,
                                  ViReal64 amplitude, ViReal64 offset,
                                  ViReal64 delayTime, ViReal64 riseTime,
                                  ViReal64 fallTime);

  Purpose

      Panel Name:    Ramp Wave
      
      Description:   Select Frequency, Amplitude, Offset, Delay Time
                     Rise Time and Fall Time.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequency

          Variable Type       ViReal64

          Control Name:  Frequency
          
          Description:   Selects the frequency of the Ramp Wave.
          
          Valid Range:
          10.0E-3 to 100.0E6  (3152B)
          100.0E-6 to 1.0E6   (3152A)
          
          Default:
          1.0E6   (3152B)
          100.0E3 (3152A)
          
          

      amplitude

          Variable Type       ViReal64

          Control Name:  Amplitude
          
          Description:   Selects the amplitude (Volts peak-to-peak).
          
          Valid Range:   10.0E-3 to 16.0 V
          
          Default:       5.0 V
          
          Note:          The following relationship must hold between the
                         amplitude and the offset:
          
                               (Amplitude / 2) + ABS(offset) <= 8.0

      offset

          Variable Type       ViReal64

          Control Name:  Offset
          
          Description:   Programs the offset for the presently
                         selected waveform.
          
          Valid Range:
          -7.992 to +7.992   (3152B)
          -7.19 to +7.19     (3152A)
          
          Default:   0.0

      delayTime

          Variable Type       ViReal64

          Control Name:  Delay Time
          
          Description:   Select the Time Delay value
          
          Valid Range:
          0 to 99.99 (3152B)
          0 to 99.9  (3152A)
          
          Note: the total of delay + rise_time + fall_time
                must be less than 100%.
          
          Default:       10% of the period

      riseTime

          Variable Type       ViReal64

          Control Name:  Rise Time
          
          Description:   Select the Rise Time value.
          
          Valid Range:
           0 to 99.99 (3152B)
           0 to 99.9  (3152A)
          
          Note: The total of delay + rise_time + fall_time
                must be less than 100%.
          
          Default:  10% of the period

      fallTime

          Variable Type       ViReal64

          Control Name:  Fall Time
          
          Description:   Select the Fall Time value.
          
          Valid Range:
           0 to 99.99  (3152B)
           0 to 99.9   (3152A)
          
          Note: the total of delay + rise_time + fall_time
                                must be less than 100%.
          
          Default:   10% of the period

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_read_counter_value

      ViStatus ri3152b_read_counter_value (ViSession instrHandle,
                                           ViPReal64 counterValue);

  Purpose

      Panel Name:  Read Counter Value
      
      Description: This function will interrogate the counter/timer
                   for a reading. Note that the read command must
                   follow a valid gate time interval otherwise reading
                   will not be available and the interface bus will be
                   held until the measurement cycle has been completed
                   and result available to be read.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      counterValue

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Counter Value
          
          Description:   Returns the result of the present measurement
                         function reading.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_read_ESE

      ViStatus ri3152b_read_ESE (ViSession instrHandle, ViPInt16 ESERegister);

  Purpose

      Panel Name:    Read ESE register
      
      Description:   Reads the Standard Event Status Enable (ESE)
                     register.  This register enables bits of the
                     Status Event Status Register (ESR).  When
                     one of the enabled ESR bits becomes set, bit
                     4 of the Status Byte (ESB bit) is set.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          
           Variable Type: ViSession

      ESERegister

          Variable Type       ViInt16 (passed by reference)

          Control Name:   ESE Register
          
          Description:    Holds the present ESE register value as
                          read from the instrument.  For a description
                          of the ESR register, see the description of
                          the "ri3152b_read_ESR" function.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_read_ESR

      ViStatus ri3152b_read_ESR (ViSession instrHandle, ViPInt16 ESRRegister);

  Purpose

      Panel Name:    Read ESR Register
      
      Description:   Reads the IEEE-488.2 defined Standard Event
                     Status Register.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      ESRRegister

          Variable Type       ViInt16 (passed by reference)

          Control Name:      ESR Register
          
          Description:       Returns the IEEE-488.2 defined Standard
                             Event Status Register.  The bit assignments
                             are as follows (bit 0 is LSB)
          
          
                      Bit #     Meaning
                      ----------------------------------
                        0       Operation Complete.  Set to 1 when the
                                *OPC command has been received and all
                                pending operations are complete
                        1       Request Control.  Not used, always 0
                        2       Query Error.  Set to 1 when an attempt
                                to read data from output queue when no
                                data is present.
                        3       Device Dependent Error.  Set when an
                                error in a device function occurs.  For
                                example, setting the voltage to 7.5 and
                                the offset to 4.1 is an illegal
                                combination of legal components.
                        4       Execution Error.  The value of a parameter
                                is outside legal limits.
                        5       Command Error.  The command received by
                                the 3152B is invalid.
                        6       User Request.  Not used, always 0.
                        7       Power On.  Set when the devices power
                                source (VXI chassis) has been turned on.
          
                        Note:  These bits are cleared by the 3152B after
                               the register is read.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_read_SRE

      ViStatus ri3152b_read_SRE (ViSession instrHandle, ViPInt16 SRERegister);

  Purpose

      Panel Name:    Read SRE register
      
      Description:   Reads the Service Request Enable (SRE) register.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      SRERegister

          Variable Type       ViInt16 (passed by reference)

          Control Name:      SRE Register
          
          Description:       Holds the present SRE register value upon
                             return from the function.  The bit
                             assignments correspond to the Status Byte:
          
                      Bit #     Meaning
                      ----------------------------------
                        0       Not used, always 0
                        1       Not used, always 0
                        2       Not used, always 0
                        3       Not used, always 0
                        4       Message Available (MAV).  The device
                                has output which may be read by the
                                system controller
                        5       Standard Event Status Bit (ESB).  One or
                                more of the enabled Standard Event Status
                                Register bits are set.  For an explanation
                                of the Standard Event Status Register, see
                                the description for "ri3152b_read_ESR"
                        6       This is the MSS bit.  Thus, the ESE
                                register should NOT have this bit set.
                        7       Not used, always 0
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_read_status_byte

      ViStatus ri3152b_read_status_byte (ViSession instrHandle,
                                         ViPInt16 statusByte);

  Purpose

      Panel Name:    Read Status Byte
      
      Description:   Reads the status byte from the instrument.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      statusByte

          Variable Type       ViInt16 (passed by reference)

          Control Name:      Status Byte
          
          Description:       Returns the IEEE-488.2 defined Status
                             Byte.  The bit assignments are as follows
                             (bit 0 is LSB)
          
          
                      Bit #     Meaning
                      ----------------------------------
                        0       Not used, always 0
                        1       Not used, always 0
                        2       Not used, always 0
                        3       Not used, always 0
                        4       Message Available (MAV).  The device
                                has output which may be read by the
                                system controller
                        5       Standard Event Status Bit (ESB).  One or
                                more of the enabled Standard Event Status
                                Register bits are set.  For an explanation
                                of the Standard Event Status Register, see
                                the description for "ri3152b_read_ESR"
                        6       Master Status Summary (MSS).  This bit
                                indicates that one or more of the enabled
                                Status Byte bits are set.  For further
                                details on enabling Status Byte bits,
                                consult the "ri3152b_set_SRE" function.
                        7       Not used, always 0
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_reset

      ViStatus ri3152b_reset (ViSession instrHandle);

  Purpose

      Panel Name:    Reset
      
      Description:   Resets the instrument to the power-up state.
      
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_reset_counter

      ViStatus ri3152b_reset_counter (ViSession instrHandle);

  Purpose

      Panel Name:  Reset Counter
      
      Description: This function reset the counter value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_revision_query

      ViStatus ri3152b_revision_query (ViSession instrHandle,
                                       ViChar _VI_FAR driverRevision[],
                                       ViChar _VI_FAR firmwareRevision[]);

  Purpose

      Panel Name:    Revision Query
      
      Description:   Queries the instrument for its firmware revision
                     and also returns the driver revision.
      
                     Revisions are returned in strings.  The revision
                     information for this instrument takes the form
      
                        "<XX>.<Y>"
      
                     where:
      
                        <XX> is a 2-digit number representing the
                             major revision number
      
                        <Y>  is a 1 or 2-digit number representing
                             the minor revision number (typically "1")
      
                      Example:
      
                          "13.1"   13 = major revision, 1 = minor
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

      driverRevision

          Variable Type       ViChar []

           Control Name:  Driver Revision
          
           Description:   Holds a string which defines the
                          current revision of this instrument
                          driver.
          

      firmwareRevision

          Variable Type       ViChar []

          Control Name:  Firmware Revision
          
          Description:   Holds the firmware revision as
                         read from the 3152B *IDN? query.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sclk_source_query

      ViStatus ri3152b_sclk_source_query (ViSession instrHandle,
                                          ViPInt16 sampleClockSource);

  Purpose

      Panel Name:    Sample Clock Source Query
      
      Description:   Reads the value of sample clock source.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sampleClockSource

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Sample Clock Source
          
          Description:   Returns the value of sample clock source.
          
          Valid Range:   RI3152B_CLK_SOURCE_INT     (0)
                         RI3152B_CLK_SOURCE_EXT     (1)
                         RI3152B_CLK_SOURCE_ECLTRG0 (2)
                         RI3152B_CLK_SOURCE_LBUS    (3)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_select_standard_waveform

      ViStatus ri3152b_select_standard_waveform (ViSession instrHandle,
                                                 ViInt16 standardWaveform);

  Purpose

      Panel Name:    Standard Waveform Selection
      
      Description:   Selects one of the standard waveforms:
                     Sine, Triangle, Square, Pulse, Ramp,
                     Gaussian, Sinc, Exponential, Dc, Noise.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      standardWaveform

          Variable Type       ViInt16

          Control Name:  Standard Waveform
          
          Description:   Selects a standard waveform.
          
          Valid Range:   RI3152B_SINE          (1)
                         RI3152B_TRIANGLE      (2)
                         RI3152B_SQUARE        (3)
                         RI3152B_PULSE         (4)
                         RI3152B_RAMP          (5)
                         RI3152B_SINC          (6)
                         RI3152B_GAUSSIAN      (7)
                         RI3152B_EXPONENTIAL   (8)
                         RI3152B_DC            (9)
                         RI3152B_NOISE         (10)
          
          Default:       RI3152B_SINE

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_select_waveform_mode

      ViStatus ri3152b_select_waveform_mode (ViSession instrHandle,
                                             ViInt16 waveformMode);

  Purpose

      Panel Name:    Waveform Mode Selection Switch
      
      Description:   Selects one of the Waveform Modes:
                     Standard, Arbitrary, Sequence, Sweep,
                     Modulation, Digital Pulse, Half Cycle, Counter.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      waveformMode

          Variable Type       ViInt16

          Control Name:  Waveform Mode
          
          Description:   Selects an Waveform Mode.
          
          Valid Range:
           RI3152B_MODE_STD (0)  - This mode provides for generating
                                   standard waveforms, such as Sine Wave,
                                   Square Wave, Triangle Waves, and so on.
          
           RI3152B_MODE_ARB (1)  - This mode outputs a single arbitrary
                                   waveform segment.
          
           RI3152B_MODE_SEQ (3)  - This mode outputs a sequence of one or
                                   more arbitrary segments.  Each segment
                                   is generated one or more times before
                                   moving to a subsequent segment.
          
           RI3152B_MODE_SWEEP (2) -This mode outputs a sequence in sweep
                                   mode.
          
           RI3152B_MODE_MOD (4)   - Modulation output.
          
           RI3152B_MODE_DPUL (5)  - Digital pulse generator function.
          
           RI3152B_MODE_HALF (6)  - Half cycle function
          
           RI3152B_MODE_COUN (7)  - Counter/timer function
          
          Default:  RI3152B_MODE_STD
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_self_test

      ViStatus ri3152b_self_test (ViSession instrHandle, ViPInt16 passFail,
                                  ViChar _VI_FAR selfTestMessage[]);

  Purpose

      Panel Name:    Self-Test
      
      Description:   Performs a self-test of the instrument.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

      passFail

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Pass/Fail
          
          Description:   Flag which indicates pass or
                         failure of the self-test.
          
          Valid Range:   0 - pass
                         1 - fail
          

      selfTestMessage

          Variable Type       ViChar []

          Control Name:  Self-Test Message
          
          Description:   Indicates which self-test failed,
                         if any.  If no self-tests failed,
                         this will be a null string.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sequence_mode_query

      ViStatus ri3152b_sequence_mode_query (ViSession instrHandle,
                                            ViPInt16 sequenceMode);

  Purpose

      Panel Name:    Sequence Mode Query
      
      Description:   Reads the value of sequence mode.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sequenceMode

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Sequence Mode
          
          Description:   Returns the value of sequence mode.
          
          Valid Range:   RI3152B_SEQ_MODE_AUTO  - (0)
                         RI3152B_SEQ_MODE_TRIG  - (1)
                         RI3152B_SEQ_MODE_STEP  - (2)
                         RI3152B_SEQ_MODE_MIX   - (3)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_3D_marker

      ViStatus ri3152b_set_3D_marker (ViSession instrHandle,
                                      ViInt32 _3DMarker);

  Purpose

      Panel Name:   Set 3D Marker
      
      Description:  This function configures the marker for the 3D
                    modulation.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      _3DMarker

          Variable Type       ViInt32

          Control Name:  3D Marker
          
          Description:   Programs a marker for the 3D modulation.
          
          Valid Range:   1 to 30000
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_3D_sample_rate

      ViStatus ri3152b_set_3D_sample_rate (ViSession instrHandle,
                                           ViReal64 _3DSampleRate);

  Purpose

      Panel Name:   Set 3D Sample Rate
      
      Description:  This function configures the sample rate for 3D
                    modulation.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      _3DSampleRate

          Variable Type       ViReal64

          Control Name:  3D Sample Rate
          
          Description:   Pass the sample rate for the 3D modulation.
          
          Valid Range:   1.0 to 2.5E6
          
          Default:       1.0E6

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_active_channel

      ViStatus ri3152b_set_active_channel (ViSession instrHandle,
                                           ViInt16 activeChannel);

  Purpose

      Panel Name:   Set Active Channel
      
      Description:  Sets the active channel.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      activeChannel

          Variable Type       ViInt16

          Control Name:  Active Channel
          
          Description:   Sets the active channel.
          
          Valid Range:   1 to 2
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_active_segment

      ViStatus ri3152b_set_active_segment (ViSession instrHandle,
                                           ViInt16 segmentNumber);

  Purpose

      Control Name:  Set Active Segment
      
      Description:   Set Active Segment

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      segmentNumber

          Variable Type       ViInt16

          Control Name:  Segment Number
          
          Description:   Select the segment number.
          
          Valid Range:
           1 to 10000 (3152B Modern mode)
           1 to 4096 (3152B Legacy mode)
           1 to 4096  (3152A)
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_active_sequence

      ViStatus ri3152b_set_active_sequence (ViSession instrHandle,
                                            ViInt16 number_ofSequence);

  Purpose

      Panel Name:    Set Active Sequence
      
      Description:   Select the active sequence.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      number_ofSequence

          Variable Type       ViInt16

          Control Name:  Number of Sequence
          
          Description:   Select the number of active sequence.
          
          Valid Range:   1 to 10
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_AHOP_dwell_mode

      ViStatus ri3152b_set_AHOP_dwell_mode (ViSession instrHandle,
                                            ViInt16 dwellMode);

  Purpose

      Panel Name:   Set AHOP Dwell Mode
      
      Description:  Selects between fixed or variable dwell-time for
                    the amplitude hops.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      dwellMode

          Variable Type       ViInt16

          Control Name:  Dwell Mode
          
          Description:   Selects between fixed or variable dwell-time for
                         the amplitude hops.
          Valid Range:
           RI3152B_AHOP_DWELL_MODE_FIX -  Selects the fixed dwell time
                                          amplitude hops mode.
          
           RI3152B_AHOP_DWELL_MODE_VAR  - Selects the variable dwell time
                                          amplitude hops mode.
          
          Default:  RI3152B_AHOP_DWELL_MODE_FIX

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_AHOP_dwell_time

      ViStatus ri3152b_set_AHOP_dwell_time (ViSession instrHandle,
                                            ViReal64 dwellTime);

  Purpose

      Panel Name:   Set AHOP Dwell Time
      
      Description:  Programs dwell time for the fixed dwell-time
                    frequency Ahop function. The same dwell time will
                    be valid for each frequency Ahop.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      dwellTime

          Variable Type       ViReal64

          Control Name:  Dwell Time
          
          Description:   Programs dwell time for the fixed dwell-time
                         frequency Ahop function. The same dwell time will
                         be valid for each frequency Ahop.
          
          Valid Range:   200.0E-9 to 20.0
          
          Default:       200.0E-9

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_AHOP_marker

      ViStatus ri3152b_set_AHOP_marker (ViSession instrHandle,
                                        ViInt16 AHOPMarker);

  Purpose

      Panel Name:   Set AHOP Marker
      
      Description:  This function programs where on the amplitude list
                    the instrument will generate a pulse, designated
                    as Hop marker, or index point. The marker pulse is
                    generated at the SYNC output connector.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      AHOPMarker

          Variable Type       ViInt16

          Control Name:  AHOP Marker
          
          Description:   Programs a marker pulse at an index amplitude hop
                         position.
          
          Valid Range:   1 to 5000
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_AM_modulation_waveform

      ViStatus ri3152b_set_AM_modulation_waveform (ViSession instrHandle,
                                                   ViInt16 AMModulationWaveform);

  Purpose

      Panel Name:   Set AM Modulation Waveform
      
      Description:  Pass the waveform you want to use to modulate the
                    output signal.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      AMModulationWaveform

          Variable Type       ViInt16

          Control Name:  AM Modulation Waveform
          
          Description:   Pass the waveform you want to use to modulate the
                         output signal.
          
          Valid Range:   RI3152B_AM_WAVE_SINE - (0)
                         RI3152B_AM_WAVE_TRI  - (1)
                         RI3152B_AM_WAVE_SQU  - (2)
                         RI3152B_AM_WAVE_RAMP - (3)
          
          Default:       RI3152B_AM_WAVE_SINE

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_amplitude

      ViStatus ri3152b_set_amplitude (ViSession instrHandle,
                                      ViReal64 amplitude);

  Purpose

      Panel Name:    Set Amplitude
      
      Description:   Sets the amplitude for the presently selected
                     waveform.  The amplitude may never be greater
                     than 16.0.  Note that the value of
      
                          (Amplitude / 2) + Offset
      
                     must always be between -8.0 and +8.0 volts.
      
                     The user is responsible for ensuring that
                     the combination of the amplitude and the
                     offset does not exceed these values.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      amplitude

          Variable Type       ViReal64

          Control Name:  Amplitude
          
          Description:   Selects the amplitude (Volts peak-to-peak)
          
          Valid Range:   10.0E-3 to 16.0 V
          
          Default:       5.0 V
          
          Note:          The amplitude selects the voltage range for
                          the output (80 mV, 800 mV, or 8 V).  The
                          amplitude and offset must always be within the
                          following ranges:
          
                          if Amplitude <= 0.080
          
                               (Amplitude / 2) + ABS(offset) <= 0.080
          
                          if 0.080 < Amplitude <= 0.800
          
                               (Amplitude / 2) + ABS(offset) <= 0.800
          
                          if 0.800 < Amplitude <= 8.0
          
                               (Amplitude / 2) + ABS(offset) <= 8.0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_ASK_baud

      ViStatus ri3152b_set_ASK_baud (ViSession instrHandle, ViReal64 ASKBaud);

  Purpose

      Panel Name:  Set ASK Baud
      
      Description: This function allows the user to select ASK word
                   rate. The word rate is the interval of which the
                   bit streams in the ASK data array are clocked
                   causing the output amplitude to hop from one level
                   to shifted amplitude level values and visa versa.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      ASKBaud

          Variable Type       ViReal64

          Control Name:  ASK Baud
          
          Description:   Programs the rate of which the frequency shifts
                         from carrier to shifted frequency.
          
          Valid Range:   1.0 to 2.5E6
          
          Default:       10.0E3

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_ASK_marker

      ViStatus ri3152b_set_ASK_marker (ViSession instrHandle,
                                       ViInt16 ASKMarker);

  Purpose

      Panel Name:  Set ASK Marker
      
      Description: This function programs where on the data stream the
                   instrument will generate a pulse, designated as ASK
                   marker, or index point. The marker pulse is
                   generated at the SYNC output connector. Note that
                   if you intend to program marker position, you must
                   do it before you load the ASK data list.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      ASKMarker

          Variable Type       ViInt16

          Control Name:  ASK Marker
          
          Description:   Programs a marker pulse at an index bit position.
          
          Valid Range:   1 to 1000
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_ASK_shifted_amplitude

      ViStatus ri3152b_set_ASK_shifted_amplitude (ViSession instrHandle,
                                                  ViReal64 shiftedAmplitude);

  Purpose

      Panel Name:  Set ASK Shifted Amplitude
      
      Description: Programs Shifted Amplitude for the ASK modulation.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      shiftedAmplitude

          Variable Type       ViReal64

          Control Name:  Shifted Amplitude
          
          Description:   Programs Shifted Amplitude for the ASK
                         modulation.
          
          Valid Range:   0.0 to 16.0
          
          Default:       1.0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_ASK_start_amplitude

      ViStatus ri3152b_set_ASK_start_amplitude (ViSession instrHandle,
                                                ViReal64 startAmplitude);

  Purpose

      Panel Name:   Set ASK Start Amplitude
      
      Description:  Programs Start Amplitude for the ASK modulation.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      startAmplitude

          Variable Type       ViReal64

          Control Name:  Start Amplitude
          
          Description:   Programs Start Amplitude for the ASK modulation.
          
          Valid Range:   0.0 to 16.0
          
          Default:       5.0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_burst_count

      ViStatus ri3152b_set_burst_count (ViSession instrHandle,
                                        ViInt32 number_ofCycles);

  Purpose

      Panel Name:    Set Burst Count
      
      Description:   Select the number of cycles to be output per
                     burst.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      number_ofCycles

          Variable Type       ViInt32

          Control Name:  Number of Cycles
          
          Description:   Select the number of cycles to be output per
                         burst.
          
          Valid Range:   1 to 1000000
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_carrier_baseline

      ViStatus ri3152b_set_carrier_baseline (ViSession instrHandle,
                                             ViInt16 carrierBaseline);

  Purpose

      Panel Name:    Set Carrier Baseline
      
      Description:   This function affects the instrument in
                     triggered, burst and gated run modes only. It
                     defines if the output idles on DC level or on the
                     carrier waveform. The default selection is
                     carrier, which means that when placed in one of
                     the interrupted run modes, the output generates
                     continuous carrier waveform shape until
                     triggered, when the trigger sequence ends, the
                     output resumes generation of continuous carrier
                     waveforms.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      carrierBaseline

          Variable Type       ViInt16

          Control Name:  Carrier Baseline
          
          Description:   Defines if the output idles on DC level or on
                         the carrier waveform.
          
          Valid Range:
           RI3152B_CARR_BASELINE_CARR (0) - This selects the continuous
                                            carrier waveform output before
                                            valid trigger is accepted.
          
           RI3152B_CARR_BASELINE_DC   (1) - This selects the DC output
                                            before valid trigger is
                                            accepted.
          
          Default:  RI3152B_CARR_BASELINE_CARR

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_carrier_frequency

      ViStatus ri3152b_set_carrier_frequency (ViSession instrHandle,
                                              ViReal64 frequency);

  Purpose

      Panel Name:    Set Carrier Frequency
      
      Description:   Will programs the CW frequency. Note that the CW
                     waveform is sine only and its frequency setting
                     is separate to the standard sine waveform. The CW
                     frequency setting is valid for all modulation
                     types.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      frequency

          Variable Type       ViReal64

          Control Name:  Frequency
          
          Description:   Specifies the frequency of the carrier waveform.
          
          Valid Range:   10 to 100.0E6
          
          Default:       1.0E6

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_counter_display_mode

      ViStatus ri3152b_set_counter_display_mode (ViSession instrHandle,
                                                 ViInt16 counterDisplayMode);

  Purpose

      Panel Name:  Set Counter Display Mode
      
      Description: Programs the display time mode for the
                   counter/timer. The two modes are normal for
                   continuous display readings and hold for single
                   reading after arming the counter input.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      counterDisplayMode

          Variable Type       ViInt16

          Control Name:  Counter Display Mode
          
          Description:   Programs the display time mode for the
                         counter/timer. The two modes are normal for
                         continuous display readings and hold for single
                         reading after arming the counter input.
          
          Valid Range:
           RI3152B_COUNT_DISP_MODE_NORMAL  - Select the continuous
                                             reading mode.
          
           RI3152B_COUNT_DISP_MODE_HOLD  -   Select the single reading
                                             mode.
          
          Default: RI3152B_COUNT_DISP_MODE_NORMAL

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_counter_function

      ViStatus ri3152b_set_counter_function (ViSession instrHandle,
                                             ViInt16 counterFunction);

  Purpose

      Panel Name:  Set Counter Function
      
      Description: Programs the measurement function for the
                   counter/timer.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      counterFunction

          Variable Type       ViInt16

          Control Name:  Counter Function
          
          Description:   Programs the measurement function for the
                         counter/timer.
          
          Valid Range:
           RI3152B_COUNT_FUNC_FREQ  -  Select the frequency measurement
                                       function. Frequency is measured
                                       on continuous signal only.
          
           RI3152B_COUNT_FUNC_PER   -  Select the period measurement
                                       function. Period can be measured
                                       on either continuous or
                                       non-repetitive signals.
          
           RI3152B_COUNT_FUNC_APER  -  Select the period averaged
                                       measurement function. Period
                                       averaged can be measured
                                       continuous signals only.
          
           RI3152B_COUNT_FUNC_PULSE -  Select the pulse width
                                       measurement function. Pulse
                                       width can be measured on either
                                       continuous or non-repetitive
                                       signals.
          
           RI3152B_COUNT_FUNC_TOT  -  Select the totalize measurement
                                      function.
          
          Default:  RI3152B_COUNT_FUNC_FREQ

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_counter_gate_time

      ViStatus ri3152b_set_counter_gate_time (ViSession instrHandle,
                                              ViReal64 counterGateTime);

  Purpose

      Panel Name:  Set Counter Gate Time
      
      Description: Programs the gate time interval. In continuous
                   mode, the counter is self-armed and therefore every
                   valid signal at the counter input will open the
                   gate and initiate a measurement cycle. In hold
                   mode, the counter must be armed before the gate can
                   open. Always make sure the programmed gate time
                   interval is larger than the period of the measured
                   signal.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      counterGateTime

          Variable Type       ViReal64

          Control Name:  Counter Gate Time
          
          Description:   Programs the gate time interval. In continuous
                         mode, the counter is self-armed and therefore
                         every valid signal at the counter input will open
                         the gate and initiate a measurement cycle. In
                         hold mode, the counter must be armed before the
                         gate can open. Always make sure the programmed
                         gate time interval is larger than the period of
                         the measured signal.
          
          Valid Range:   100.0E-6 to 1
          
          Default:       1.0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_couple

      ViStatus ri3152b_set_couple (ViSession instrHandle,
                                   ViBoolean coupleSwitch);

  Purpose

      Panel Name:    Set Couple ON/OFF
      
      Description:   This function will turn the couple state on and
                     off.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      coupleSwitch

          Variable Type       ViBoolean

          Control Name:  Couple Switch
          
          Description:   Turns the couple switch On or Off
          
          Valid Range:   VI_TRUE  (1) - Couple State On
                         VI_FALSE (0) - Couple state Off
          
          Default:       VI_FALSE

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_couple_delay

      ViStatus ri3152b_set_couple_delay (ViSession instrHandle,
                                         ViReal64 coupleDelay);

  Purpose

      Panel Name:    Set Couple Delay
      
      Description:   This function configures the couple delay.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      coupleDelay

          Variable Type       ViReal64

          Control Name:  Couple Delay
          
          Description:   Set Couple Delay.
          
          Valid Range:   0.0 to 360.0
          
          Default:       0.0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_couple_mode

      ViStatus ri3152b_set_couple_mode (ViSession instrHandle,
                                        ViInt16 coupleMode);

  Purpose

      Panel Name:   Set Couple Mode
      
      Description:  This function configure the couple mode.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      coupleMode

          Variable Type       ViInt16

          Control Name:  Couple Mode
          
          Description:   Will select master and slave instruments in
                         multi-instrument synchronization mode.
          
          Valid Range:
           RI3152B_COUPLE_MODE_MASTER (0)  - Set the master instrument.
           RI3152B_COUPLE_MODE_SLAVE (1)   - Set the slave instrument.
          
          Default:  RI3152B_COUPLE_MODE_MASTER

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_couple_path

      ViStatus ri3152b_set_couple_path (ViSession instrHandle,
                                        ViInt16 couplePath);

  Purpose

      Panel Name:   Set Couple Path
      
      Description:  This function configure the couple path for
                    multi-instr. synchronization.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      couplePath

          Variable Type       ViInt16

          Control Name:  Couple Path
          
          Description:   Programs the couple path for
                         multi-instr.synchronization.
          
          Valid Range:
           RI3152B_COUPLE_PATH_ADJ  (0) -
           RI3152B_COUPLE_PATH_ECLT (1) -
           RI3152B_COUPLE_PATH_LBUS (2) -
          
          
          Default:  RI3152B_COUPLE_PATH_ADJ

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_dc_level

      ViStatus ri3152b_set_dc_level (ViSession instrHandle, ViReal64 DCLevel);

  Purpose

      Panel Name:    Set DC Level
      
      Description:   Specifies the value of DC level.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      DCLevel

          Variable Type       ViReal64

          Control Name:  DC Level
          
          Description:   Specifies the value of DC level.
          
          Valid Range:   -8.0 to 8.0
          
          Default:       5.0
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_double_pulse_delay

      ViStatus ri3152b_set_double_pulse_delay (ViSession instrHandle,
                                               ViReal64 doublePulseDelay);

  Purpose

      Panel Name:   Set Double Pulse Delay
      
      Description:  Sets the delay between two adjacent pulses when
                    the double pulse state is ON. Note that the sum of
                    all parameters, including the pulse delay time
                    must not exceed the programmed pulse period and
                    therefore, it is recommended that the pulse period
                    be programmed before all other pulse parameters.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      doublePulseDelay

          Variable Type       ViReal64

          Control Name:  Double Pulse Delay
          
          Description:   Sets the delay between two adjacent pulses when
                         the double pulse state is ON. Note that the sum
                         of all parameters, including the pulse delay time
                         must not exceed the programmed pulse period and
                         therefore, it is recommended that the pulse
                         period be programmed before all other pulse
                         parameters.
          
          Valid Range:   0.0 to 1.0E3
          
          Default:       1.0E-3

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_double_pulse_state

      ViStatus ri3152b_set_double_pulse_state (ViSession instrHandle,
                                               ViBoolean doublePulseState);

  Purpose

      Panel Name:   Set Double Pulse State ON/OFF
      
      Description:  Turns the double pulse state on/off.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      doublePulseState

          Variable Type       ViBoolean

          Control Name:  Double Pulse State
          
          Description:   Pass whether you want to enable or disable the
                         double pulse.
          
          Valid Values:
           VI_TRUE (1)  - Enable the double pulse.
           VI_FALSE (0) - Disable the double pulse.
          
          Default: VI_FALSE
          
          
          
          
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_ESE

      ViStatus ri3152b_set_ESE (ViSession instrHandle, ViInt16 ESERegister);

  Purpose

      Panel Name:    set ESE register
      
      Description:   Programs the Standard Event Status Enable (ESE)
                     register.  This register enables bits of the
                     Status Event Status Register (ESR).  When
                     one of the enabled ESR bits becomes set, bit
                     4 of the Status Byte (ESB bit) is set.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      ESERegister

          Variable Type       ViInt16

          Control Name:      ESE Register
          
          Description:       Holds the ESE register value.  Each bit
                             enables the corresponding bit in the
                             ESR Register.  For a description of the
                             ESR register, see the description of the
                             "ri3152b_read_ESR" function.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_exponential_exp

      ViStatus ri3152b_set_exponential_exp (ViSession instrHandle,
                                            ViInt16 exponent);

  Purpose

      Panel Name:    Set Exponential Exponent
      
      Description:   Sets exponential exponent.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      exponent

          Variable Type       ViInt16

          Control Name:  Exponent
          
          Description:   Selects the exponential exponent value.
          
          Valid Range:
           -100 to 100 (3152B)
           -200 to 200 (3152A)
          
          Default: -10
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_FHOP_dwell_mode

      ViStatus ri3152b_set_FHOP_dwell_mode (ViSession instrHandle,
                                            ViInt16 dwellMode);

  Purpose

      Panel Name:   Set FHOP Dwell Mode
      
      Description:  Selects between fixed or variable dwell-time for
                    the frequency hops. Select the fixed option if you
                    want each frequency to dwell equally on each step.
                    The variable option lets you program different
                    dwell times for each frequency hop.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      dwellMode

          Variable Type       ViInt16

          Control Name:  Dwell Mode
          
          Description:   Selects between fixed or variable dwell-time for
                         the frequency hops.
          
          Valid Range:
           RI3152B_FHOP_DWELL_MODE_FIX -  Selects the fixed dwell time
                                          frequency hops mode.
          
           RI3152B_FHOP_DWELL_MODE_VAR  - Select the variable dwell time
                                          frequency hops mode.
          
          Default:  RI3152B_FHOP_DWELL_MODE_FIX

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_FHOP_dwell_time

      ViStatus ri3152b_set_FHOP_dwell_time (ViSession instrHandle,
                                            ViReal64 dwellTime);

  Purpose

      Panel Name:   Set FHOP Dwell Time
      
      Description:  Programs dwell time for the fixed dwell-time
                    frequency hop function. The same dwell time will
                    be valid for each frequency hop.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      dwellTime

          Variable Type       ViReal64

          Control Name:  Dwell Time
          
          Description:   Programs dwell time for the fixed dwell-time
                         frequency hop function. The same dwell time will
                         be valid for each frequency hop.
          
          Valid Range:   200.0E-9 to 20.0
          
          Default:       200.0E-9

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_FHOP_marker

      ViStatus ri3152b_set_FHOP_marker (ViSession instrHandle,
                                        ViInt16 FHOPMarker);

  Purpose

      Panel Name:   Set FHOP Marker
      
      Description:  This function configures the marker for the
                    hopping modulation.
                    This function programs where on the frequency list
                    the instrument will generate a pulse, designated
                    as Hop marker, or index point.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FHOPMarker

          Variable Type       ViInt16

          Control Name:  FHOP Marker
          
          Description:   Programs a marker pulse at an index frequency hop
                         position.
          
          Valid Range:   1 to 5000
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_filter_state

      ViStatus ri3152b_set_filter_state (ViSession instrHandle,
                                         ViBoolean filterSwitch);

  Purpose

      Panel Name:    Set Filter State ON/OFF
      
      Description:   Turns the filter ON or OFF.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      filterSwitch

          Variable Type       ViBoolean

          Control Name:  Filter Switch
          
          Description:   Turns the filter On or Off.
          
          Valid Range:   1 (ON) -  Turns the filter On
                         0 (OFF) - Turns the filter Off
          
          Default:       OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_FM_frequency_deviation

      ViStatus ri3152b_set_FM_frequency_deviation (ViSession instrHandle,
                                                   ViReal64 frequencyDeviation);

  Purpose

      Panel Name:   Set FM Frequency Deviation
      
      Description:  Pass the deviation range around the carrier
                    frequency.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequencyDeviation

          Variable Type       ViReal64

          Control Name:  Frequency Deviation
          
          Description:   Pass the deviation range around the carrier
                         frequency.
          
          Valid Range:   10.0E-3 to 100.0E6
          
          Default:       100.0E3

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_FM_marker

      ViStatus ri3152b_set_FM_marker (ViSession instrHandle,
                                      ViReal64 FMMarker);

  Purpose

      Panel Name:   Set FM Marker
      
      Description:  Programs marker frequency position. FM marker can
                    be placed inside the following range: (carrier
                    frequency ± deviation frequency / 2).

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FMMarker

          Variable Type       ViReal64

          Control Name:  FM Marker
          
          Description:   Specifies the marker frequency position.
          
          Valid Range:   10.0E-3 to 100.0E6
          
          Default:       1.0E6

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_FM_modulation_frequency

      ViStatus ri3152b_set_FM_modulation_frequency (ViSession instrHandle,
                                                    ViReal64 FMModulationFrequency);

  Purpose

      Panel Name:   Set FM Modulation Frequency
      
      Description:  Specifies the frequency of the internal modulating
                    signal.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FMModulationFrequency

          Variable Type       ViReal64

          Control Name:  FM Modulation Frequency
          
          Description:   Specifies the frequency of the internal
                         modulating signal.
          
          Valid Range:   10.0E-3 to 350.0E3
          
          Default:       10.0E3

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_FM_modulation_SCLK

      ViStatus ri3152b_set_FM_modulation_SCLK (ViSession instrHandle,
                                               ViReal64 FMModulationSCLK);

  Purpose

      Panel Name:   Set FM Modulation SCLK
      
      Description:  This function configures the FM sample rate which
                    determines the rate at which the function
                    generator produces FM arbitrary waveforms.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FMModulationSCLK

          Variable Type       ViReal64

          Control Name:  FM Modulation SCLK
          
          Description:   Pass the FM sample rate at which you want the
                         function generator to output FM arbitrary
                         waveform.
          
          Valid Range:   1 to 2.5E6
          
           Default:      1.0E6

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_FM_modulation_waveform

      ViStatus ri3152b_set_FM_modulation_waveform (ViSession instrHandle,
                                                   ViInt16 FMWave);

  Purpose

      Panel Name:   Set FM Modulation Waveform
      
      Description:  Pass the waveform you want to use to modulate the
                    output signal.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FMWave

          Variable Type       ViInt16

          Control Name:  FM Wave
          
          Description:   Pass the waveform you want to use to modulate the
                         output signal.
          
          Valid Range:   RI3152B_FM_WAVE_SINE - (0)
                         RI3152B_FM_WAVE_TRI  - (1)
                         RI3152B_FM_WAVE_SQU  - (2)
                         RI3152B_FM_WAVE_RAMP - (3)
                         RI3152B_FM_WAVE_ARB  - (4)
          
          Default:       RI3152B_FM_WAVE_SINE

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_frequency

      ViStatus ri3152b_set_frequency (ViSession instrHandle,
                                      ViReal64 frequency);

  Purpose

      Panel Name:    Set Frequency
      
      Description:   Sets the frequency for the presently selected
                     waveform frequency.  Although the range is
                     up to 50.0 MHz, the maximum may be as low as
                     1.0 MHz depending on the present waveform.
                     Care should be taken to program below the
                     maximum for the presently selected waveform.
      
                     The maximum frequencies for standard waveforms
                     are as follows: (3152A)
      
                           Waveform          Max Frequency
                           --------------------------------------
                            Sine               50 MHz
                            Square             50 MHz
                            Triangle            1 MHz
                            Pulse               1 MHz
                            Ramp                1 MHz
                            Sinc                1 MHz
                            Exponential         1 MHz
                            Gaussian            1 MHz
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequency

          Variable Type       ViReal64

          Control Name:  Frequency
          
          Description:   Programs the frequency for the presently
                         selected waveform.
          
          Valid Range:
          100.0E-6 to 100.0E6 (3152B)
          100.0E-6 to 50.0E6 (3152A)
          
          Default:  1.0E6

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_FSK_baud

      ViStatus ri3152b_set_FSK_baud (ViSession instrHandle, ViReal64 FSKBaud);

  Purpose

      Panel Name:   Set FSK Baud
      
      Description:  Allows the user to select FSK word rate. The word
                    rate is the interval of which the bit streams in
                    the FSK data array are clocked causing the output
                    frequency to hop from carrier to shifted frequency
                    values and visa versa.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FSKBaud

          Variable Type       ViReal64

          Control Name:  FSK Baud
          
          Description:   Allows the user to select FSK word rate.
          
          Valid Range:   1 to 10.0E6
          
          Default:       10.0E3

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_FSK_marker

      ViStatus ri3152b_set_FSK_marker (ViSession instrHandle,
                                       ViInt16 FSKMarker);

  Purpose

      Panel Name:   Set FSK Marker
      
      Description:  Programs where on the data stream the
                    instrument will generate a pulse, designated as
                    FSK marker, or index point. The marker pulse is
                    generated at the SYNC output connector. Note that
                    if you intend to program marker position, you must
                    do it before you load the FSK data list.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      FSKMarker

          Variable Type       ViInt16

          Control Name:  FSK Marker
          
          Description:   Programms the FSK Marker.
          
          Valid Range:   1 to 4000

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_FSK_shifted_frequency

      ViStatus ri3152b_set_FSK_shifted_frequency (ViSession instrHandle,
                                                  ViReal64 shiftedFrequency);

  Purpose

      Panel Name:   Set FSK Shifted Frequency
      
      Description:  Specifies the shifted frequency. The frequency
                    shifts when the pointer in the data array points
                    to "1".
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      shiftedFrequency

          Variable Type       ViReal64

          Control Name:  Shifted Frequency
          
          Description:   Specifies the shifted frequency. The frequency
                         shifts when the pointer in the data array points
                         to "1".
          
          Valid Range:   10.0E-3 to 100.0E6
          
          
          Default:       100.0E3

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_gate_mode

      ViStatus ri3152b_set_gate_mode (ViSession instrHandle,
                                      ViInt16 gateMode);

  Purpose

      Panel Name:    Set Gate Mode
      
      Description:   This command will select if the instrument will
                     gate between transitions or on level changes.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      gateMode

          Variable Type       ViInt16

          Control Name:  Gate Mode
          
          Description:   Will select if the instrument will gate between
                         transitions or on level changes.
          
          Valid Range:
           RI3152B_GATE_MODE_LEV  (0) -  Selects the mode where level
                                         change at the trigger input
                                         causes the output to turn the
                                         gate on and off.
          
           RI3152B_GATE_MODE_TRAN (1) -  Will select transitions as the
                                         gating signals. First transition
                                         turns the gate on and second
                                         turns the gate off.
          
          Default:  RI3152B_GATE_MODE_LEV

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_gauss_exp

      ViStatus ri3152b_set_gauss_exp (ViSession instrHandle,
                                      ViInt16 exponent);

  Purpose

      Panel Name:    Set Gauss Exponent
      
      Description:   Sets gauss exponent.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      exponent

          Variable Type       ViInt16

          Control Name:  Exponent
          
          Description:   Selects the gauss exponent value
          
          Valid Range:   1 to 200

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_half_cycle_delay

      ViStatus ri3152b_set_half_cycle_delay (ViSession instrHandle,
                                             ViReal64 halfCycleDelay);

  Purpose

      Panel Name:  Set Half Cycle Delay
      
      Description: Programs the interval of which the output idles
                   between half cycles.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      halfCycleDelay

          Variable Type       ViReal64

          Control Name: Half Cycle Delay
          
          Description:  Sets the delay time interval between half cycles.
          
          Valid Range:  200.0e-9 - 20.0
          
          Default:      1.0E-6
          
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_half_cycle_duty_cycle

      ViStatus ri3152b_set_half_cycle_duty_cycle (ViSession instrHandle,
                                                  ViReal64 halfCycleDutyCycle);

  Purpose

      Panel Name:  Set Half Cycle Duty Cycle
      
      Description: Programs the Duty Cycle for the Half Cycle Square.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      halfCycleDutyCycle

          Variable Type       ViReal64

          Control Name:  Half Cycle Duty Cycle
          
          Description:   Programs the Duty Cycle for the Half Cycle
                         Square.
          
          Valid Range:   0.0 to 99.99
          
          Default:       50.0
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_half_cycle_frequency

      ViStatus ri3152b_set_half_cycle_frequency (ViSession instrHandle,
                                                 ViReal64 halfCycleFrequency);

  Purpose

      Panel Name:  Set Half Cycle Frequency
      
      Description: Programs the frequency of the half cycle waveforms.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      halfCycleFrequency

          Variable Type       ViReal64

          Control Name:  Half Cycle Frequency
          
          Description:   Sets the frequency of the half cycle waveform.
          
          Valid Range:   10.0E-3 to 1.0E6
          
          Default:       1.0E6
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_half_cycle_phase

      ViStatus ri3152b_set_half_cycle_phase (ViSession instrHandle,
                                             ViReal64 halfCyclePhase);

  Purpose

      Panel Name:  Set Half Cycle Phase
      
      Description: Programs the phase for the Half Cycle Waves: Sine
                   and Triangle.
      
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      halfCyclePhase

          Variable Type       ViReal64

          Control Name:  Half Cycle Phase
          
          Description:   Programs the phase for the Half Cycle Waves:
                         Sine and Triangle.
          
          Valid Range:   0.0 to 360.0
          
          Default:       0.0
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_half_cycle_waveform

      ViStatus ri3152b_set_half_cycle_waveform (ViSession instrHandle,
                                                ViInt16 halfCycleWaveform);

  Purpose

      Panel Name:    Set Half cycle waveform
      
      Description:   Defines the type of half cycle waveform that will
                     be available at the output connector.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      halfCycleWaveform

          Variable Type       ViInt16

          Control Name : Half Cycle Waveform
          
          Description : Defines the type of half cycle waveform that will
                        be available at the output connector.
          
          Valid Range :
           RI3152B_HALF_CYCLE_WAVE_SINE  - Selects the half cycle sine
                                           waveform.
          
          RI3152B_HALF_CYCLE_WAVE_TRI - Selects the half cycle
                                        triangular waveform.
          
          RI3152B_HALF_CYCLE_WAVE_SQU  - Selects the half cycle square
                                         waveform.
          
          Default Value: RI3152B_HALF_CYCLE_WAVE_SINE
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_load_impedance

      ViStatus ri3152b_set_load_impedance (ViSession instrHandle,
                                           ViReal64 impedance);

  Purpose

      Panel Name:    Set Load Impedance
      
      Description:   Configures the output impedance.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      impedance

          Variable Type       ViReal64

          Control Name:  Impedance
          
          Description:   Pass the impedance value you want the function
                         generator to use.
          
          Valid Range:   50.0 to 1.0E6
          
          Default:       50.0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_modulation_type

      ViStatus ri3152b_set_modulation_type (ViSession instrHandle,
                                            ViInt16 modulationType);

  Purpose

      Panel Name:    Set Modulation Type
      
      Description:   Selects one of the Modulation type:
                     OFF, AM, FM, SWE, FSK, ASK, PSK, FHOP,
                     AHOP, 3D.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      modulationType

          Variable Type       ViInt16

          Control Name:  Modulation Type
          
          Description:   Selects one of the Modulation type.
          
          Valid Range:   RI3152B_MOD_TYPE_OFF  - (0)
                         RI3152B_MOD_TYPE_AM   - (1)
                         RI3152B_MOD_TYPE_FM   - (2)
                         RI3152B_MOD_TYPE_SWE  - (3)
                         RI3152B_MOD_TYPE_FSK  - (4)
                         RI3152B_MOD_TYPE_ASK  - (5)
                         RI3152B_MOD_TYPE_PSK  - (6)
                         RI3152B_MOD_TYPE_FHOP - (7)
                         RI3152B_MOD_TYPE_AHOP - (8)
                         RI3152B_MOD_TYPE_3D   - (9)
          
          Default:       RI3152B_MOD_TYPE_OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_offset

      ViStatus ri3152b_set_offset (ViSession instrHandle, ViReal64 offset);

  Purpose

      Panel Name:    Set Offset
      
      Description:   Sets the offset for the presently selected
                     waveform.  Note that the value of
      
                          (Amplitude / 2) + Offset
      
                     must always be between -8.0 and +8.0 volts.
      
                     The user is responsible for ensuring that
                     the combination of the amplitude and the
                     offset does not exceed these values.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      offset

          Variable Type       ViReal64

          Control Name:  Offset
          
          Description:   Programs the offset for the presently
                         selected waveform.
          
          Valid Range:
          -7.992 to +7.992   (3152B)
          -7.19 to +7.19     (3152A)
          
          Default:       0.0
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_output_load_calibration

      ViStatus ri3152b_set_output_load_calibration (ViSession instrHandle,
                                                    ViInt16 loadCalibration);

  Purpose

      Panel Name:    Set Output Load Calibration
      
      Description:   Will set the output load calibration.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      loadCalibration

          Variable Type       ViInt16

          Control Name:  Load Calibration
          
          Description:   Selects an Output Load Calibration.
          
          Valid Range:
           RI3152B_OUTPUT_LOAD_CAL_50 (0) - 50 ohms
          
           RI3152B_OUTPUT_LOAD_CAL_1M (1) - 1 MOhms
          
          Default:  RI3152B_OUTPUT_LOAD_CAL_50
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_output_trigger_source

      ViStatus ri3152b_set_output_trigger_source (ViSession instrHandle,
                                                  ViInt16 outputTriggerSource);

  Purpose

      Panel Name:    Set Output Trigger Source
      
      Description:   Sets the output trigger source.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      outputTriggerSource

          Variable Type       ViInt16

          Control Name:  Output Trigger Source
          
          Description:   Selects the source of the trigger output
                         from the instrument waveform generator.
          
          Valid Range:
           RI3152B_TRIGGER_INTERNAL (0) - Generates a trigger signal at
                                          intervals set by the internal
                                          trigger generator (see the
                                          Trigger Rate Panel
                                          [ri3152b_trigger_rate()])
          
           RI3152B_TRIGGER_EXTERNAL (1) - Generates a trigger signal every
                                          time a trigger is applied to the
                                          front panel TRIG IN connector
          
           RI3152B_TRIGGER_BIT (2) - Generates a trigger signal at a
                                     particular point in the waveform.
                                     The trigger point in the waveform can
                                     be programmed using the BIT Trigger
                                     Point parameter of this function.
          
           RI3152B_TRIGGER_LCOM (3) - Generates a trigger signal in
                                      SEQuence mode.  This trigger signal
                                      is generated when the selected BIT
                                      Trigger point of the selected LCOM
                                      Segment is reached.
          
          Default:  RI3152B_TRIGGER_BIT

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_phase2_source

      ViStatus ri3152b_set_phase2_source (ViSession instrHandle,
                                          ViInt16 phase2Source);

  Purpose

      Panel Name:   Set Phase2 Source
      
      Description:  Set Phase2 source.
      
      Note: Not supported in 3151A, 3151B

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      phase2Source

          Variable Type       ViInt16

          Control Name:  Phase2 Source
          
          Description:   Set Phase2 Source.
          
          Valid Range:   RI3152B_PHASE2_SOURCE_EXT - (0)
                         RI3152B_PHASE2_SOURCE_ECLT0 - (1)
                         RI3152B_PHASE2_SOURCE_TTLT0 - (2)
                         RI3152B_PHASE2_SOURCE_TTLT1 - (3)
                         RI3152B_PHASE2_SOURCE_TTLT2 - (4)
                         RI3152B_PHASE2_SOURCE_TTLT3 - (5)
                         RI3152B_PHASE2_SOURCE_TTLT4 - (6)
                         RI3152B_PHASE2_SOURCE_TTLT5 - (7)
                         RI3152B_PHASE2_SOURCE_TTLT6 - (8)
                         RI3152B_PHASE2_SOURCE_TTLT7 - (9)
          
          Default:       RI3152B_PHASE2_SOURCE_EXT
          
          Note: Not supported in 3151A, 3151B

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_phase_offset

      ViStatus ri3152b_set_phase_offset (ViSession instrHandle,
                                         ViInt32 phaseOffset);

  Purpose

      Panel Name:   Set Phase Offset
      
      Description:  Will programs the start phase offset between two
                    instruments in units of waveform points.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      phaseOffset

          Variable Type       ViInt32

          Control Name: Phase Offset
          
          Description:  Will programs the start phase offset between two
                        instruments in units of waveform points.
          
          Valid Range:  0 to maximum memory size - 1
          
          Default:      0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_PSK_marker

      ViStatus ri3152b_set_PSK_marker (ViSession instrHandle,
                                       ViInt16 PSKMarker);

  Purpose

      Panel Name:   Set PSK Marker
      
      Description:  This function programs where on the data stream
                    the instrument will generate a pulse, designated
                    as PSK marker, or index point.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      PSKMarker

          Variable Type       ViInt16

          Control Name:  PSK Marker
          
          Description:   Programs where on the data stream the instrument
                         will generate a pulse, designated as PSK marker,
                         or index point.
          
          Valid Range:   1 to 4000
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_PSK_rate

      ViStatus ri3152b_set_PSK_rate (ViSession instrHandle, ViReal64 PSKRate);

  Purpose

      Panel Name:   Set PSK Rate
      
      Description:  This function allows the user to select PSK word
                    rate. The word rate is the interval of which the
                    bit streams in the PSK data array are clocked
                    causing the output phase to hop from start to
                    shifted phase values and visa versa.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      PSKRate

          Variable Type       ViReal64

          Control Name:  PSK Rate
          
          Description:   Programs the  PSK word rate. The word rate is
                         the interval of which the bit streams in the PSK
                         data array are clocked causing the output phase
                         to hop from start to shifted phase values and
                         visa versa.
          
          Valid Range:   1.0 to 10.0E6
          
          Default:       10.0E3

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_PSK_shifted_phase

      ViStatus ri3152b_set_PSK_shifted_phase (ViSession instrHandle,
                                              ViReal64 shiftedPhase);

  Purpose

      Panel Name:   Set PSK Shifted Phase
      
      Description:  Programs the shift phase for the carrier waveform.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      shiftedPhase

          Variable Type       ViReal64

          Control Name:  Shifted Phase
          
          Description:   Programs the shift phase for the carrier
                         waveform.
          
          Valid Range:   0.0 to 360.0
          
          Default:       180.0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_PSK_start_phase

      ViStatus ri3152b_set_PSK_start_phase (ViSession instrHandle,
                                            ViReal64 startPhase);

  Purpose

      Panel Name:   Set PSK Start Phase
      
      Description:  Programs the start phase for the carrier
                    waveform.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      startPhase

          Variable Type       ViReal64

          Control Name:  Start Phase
          
          Description:   Programs the start phase for the carrier
                         waveform.
          
          Valid Range:   0.0 to 360.0
          
          Default:       0.0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_pulse_data

      ViStatus ri3152b_set_pulse_data (ViSession instrHandle, ViReal64 delay,
                                       ViReal64 width, ViReal64 rise,
                                       ViReal64 fall);

  Purpose

      Panel Name:    Set Pulse
      
      Description:   Sets the pulse data.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      delay

          Variable Type       ViReal64

          Control Name:  Delay
          
          Description:   Programs the pulse delay.
          
          Valid Range: 0 to 99.999 (3152B)
                       0 to 99.9   (3152A)
          
          Note: the total of delay + rise_time + high_time
                + fall_time  must be less than 100%.
          
          Default: 10.0
          

      width

          Variable Type       ViReal64

          Control Name:  Width
          
          Description:   Programs the pulse width.
          
          Valid Range:   0 to 99.999 (3152B)
                         0 to 99.9   (3152A)
          
          Note: The total of delay + rise_time + high_time
                + fall_time  must be less than 100%.
          
          Default: 10% of the period

      rise

          Variable Type       ViReal64

          Control Name:  Rise
          
          Description:   Programs the pulse rise.
          
          Valid Range:    0 to 99.999  (3152B)
                          0 to 99.9    (3152A)
          
          Note: The total of delay + rise_time + high_time
                + fall_time  must be less than 100%.
          
          Default:  10% of the period

      fall

          Variable Type       ViReal64

          Control Name:  Fall
          
          Description:   Programs the pulse fall.
          
          Valid Range:   0 to 99.999  (3152B)
                         0 to 99.9    (3152A)
          
          Note: The total of delay + rise_time + high_time
                + fall_time  must be less than 100%.
          
          Default:  10% of the period

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_pulse_delay

      ViStatus ri3152b_set_pulse_delay (ViSession instrHandle,
                                        ViReal64 pulseDelay);

  Purpose

      Panel Name:   Set Pulse Delay
      
      Description:  This function programs the delayed interval of
                    which the output idles on the low level amplitude
                    until the first transition to high level
                    amplitude.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      pulseDelay

          Variable Type       ViReal64

          Control Name:  Pulse Delay
          
          Description:   Sets the delay time interval in units of seconds.
                         Note that the sum of all parameters, including
                         the pulse delay time must not exceed the
                         programmed pulse period and therefore, it is
                         recommended that the pulse period be programmed
                         first and then all other pulse parameters.
          
          Valid Range:   0.0 to 10.0
          
          Default:       0.0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_pulse_fall_time

      ViStatus ri3152b_set_pulse_fall_time (ViSession instrHandle,
                                            ViReal64 fallTime);

  Purpose

      Panel Name:  Set Pulse Fall Time
      
      Description: Programs the Fall Time for the Pulse Output.
                   Note that the sum of all parameters, including the
                   fall time must not exceed the programmed pulse
                   period and therefore, it is recommended that the
                   pulse period be programmed before all other pulse
                   parameters.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      fallTime

          Variable Type       ViReal64

          Control Name:  Fall Time
          
          Description:   Programs the Fall Time for the Pulse Output.
                         Note that the sum of all parameters, including
                         the fall time must not exceed the programmed
                         pulse period and therefore, it is recommended
                         that the pulse period be programmed before all
                         other pulse parameters.
          
          Valid Range:   0 to 1.0E3
          
          Default:       1.0E-3

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_pulse_high_level

      ViStatus ri3152b_set_pulse_high_level (ViSession instrHandle,
                                             ViReal64 highLevel);

  Purpose

      Panel Name:   Set Pulse High Level
      
      Description:  Sets the pulse high level. Note that the high
                    level setting must be higher than the low level
                    setting.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      highLevel

          Variable Type       ViReal64

          Control Name:  High Level
          
          Description:   Sets the pulse high level. Note that the high
                         level setting must be higher than the low level
                         setting.
          
          Valid Range:   -7.992 to 8.0
          
          Default:       5.0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_pulse_high_time

      ViStatus ri3152b_set_pulse_high_time (ViSession instrHandle,
                                            ViReal64 highTime);

  Purpose

      Panel Name:   Set Pulse High Time
      
      Description:  Sets the width of the high time for the pulse
                    shape.
                    Note that the sum of all parameters, including the
                    high time must not exceed the programmed pulse
                    period and therefore, it is recommended that the
                    pulse period be programmed before all other pulse
                    parameters.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      highTime

          Variable Type       ViReal64

          Control Name:  High Time
          
          Description:   Sets the width of the high time for the pulse
                         shape.Note that the sum of all parameters,
                         including the high time must not exceed the
                         programmed pulse period and therefore, it is
                         recommended that the pulse period be programmed
                         before all other pulse parameters.
          
          Valid Range:   0 to 1.0E3
          
          Default:       1.0E-3

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_pulse_low_level

      ViStatus ri3152b_set_pulse_low_level (ViSession instrHandle,
                                            ViReal64 lowLevel);

  Purpose

      Panel Name:   Set Pulse Low Level
      
      Description:  Sets the pulse low level. Note that the low level
                    setting must be smaller than the high level
                    setting.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      lowLevel

          Variable Type       ViReal64

          Control Name:  Low Level
          
          Description:   Sets the pulse low level. Note that the low level
                         setting must be smaller than the high level
                         setting.
          
          Valid Range:   -8.0 to 7.992
          
          Default:       0.0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_pulse_period

      ViStatus ri3152b_set_pulse_period (ViSession instrHandle,
                                         ViReal64 pulsePeriod);

  Purpose

      Panel Name:   Set Pulse Period
      
      Description:  Programs the Period for the Pulse Output.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      pulsePeriod

          Variable Type       ViReal64

          Control Name:  Pulse Period
          
          Description:   Programs the Period for the Pulse Output.
          
          Valid Range:   80.0E-9 to maximum memory size
          
          Default:       10.0E-3

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_pulse_polarity

      ViStatus ri3152b_set_pulse_polarity (ViSession instrHandle,
                                           ViInt16 pulsePolarity);

  Purpose

      Panel Name:   Set Pulse Polarity
      
      Description:  This function programs the polarity of the pulse
                    in reference to the base line level.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      pulsePolarity

          Variable Type       ViInt16

          Control Name:  Pulse Polarity
          
          Description:   Programs the polarity of the pulse in reference
                         to the base line level.
          
          Valid Range:
           RI3152B_DPULSE_POL_NORMAL - The pulse is generated exactly as
                                       programmed.
          
           RI3152B_DPULSE_POL_COMP   -  The pulse is inverted about its
                                        mid amplitude level.
          
           RI3152B_DPULSE_POL_INV - The pulse is inverted about the 0
                                    level base line.
          
          Default: RI3152B_DPULSE_POL_NORMAL

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_pulse_rise_time

      ViStatus ri3152b_set_pulse_rise_time (ViSession instrHandle,
                                            ViReal64 riseTime);

  Purpose

      Panel Name:   Set Pulse Rise Time
      
      Description:  Programs the Rise Time for the Pulse Output.
                    Note that the sum of all parameters, including the
                    rise time must not exceed the programmed pulse
                    period and therefore, it is recommended that the
                    pulse period be programmed before all other pulse
                    parameters.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      riseTime

          Variable Type       ViReal64

          Control Name:  Rise Time
          
          Description:   Programs the Rise Time for the Pulse Output.
                         Note that the sum of all parameters, including
                         the rise time must not exceed the programmed
                         pulse period and therefore, it is recommended
                         that the pulse period be programmed before all
                         other pulse parameters.
          
          Valid Range:   0 to 1.0E3
          
          Default:       1.0E-3

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152A_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152A_error_message function.  The
                  ri3152A_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_ramp_data

      ViStatus ri3152b_set_ramp_data (ViSession instrHandle, ViReal64 delay,
                                      ViReal64 rise, ViReal64 fall);

  Purpose

      Panel Name:    Set Rampe Data
      
      Description:   Sets the ramp data.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      delay

          Variable Type       ViReal64

          Control Name:  Delay
          
          Description:   Programs the ramp delay.
          
          Valid Range:
          0 to 99.99 (3152B)
          0 to 99.9  (3152A)
          
          Note: The total of delay + rise_time + fall_time
                must be less than 100%.
          
          Default:  10% of the period

      rise

          Variable Type       ViReal64

          Control Name:  Rise
          
          Description:   Programs the ramp rise.
          
          Valid Range:
           0 to 99.99 (3152B)
           0 to 99.9  (3152A)
          
          Note: The total of delay + rise_time + fall_time
                must be less than 100%.
          
          Default:  10% of the period

      fall

          Variable Type       ViReal64

          Control Name:  Fall
          
          Description:   Programs the ramp fall.
          
          Valid Range:
           0 to 99.99  (3152B)
           0 to 99.9   (3152A)
          
          Note: The total of delay + rise_time + fall_time
                must be less than 100%.
          
          Default:   10% of the period

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_re_trigger_delay_state

      ViStatus ri3152b_set_re_trigger_delay_state (ViSession instrHandle,
                                                   ViBoolean reTriggerDelayState);

  Purpose

      Panel Name:   Set Re-Trigger Delay State
      
      Description:  Determines whether the re-trigger delay state is
                    turned ON or OFF.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      reTriggerDelayState

          Variable Type       ViBoolean

          Control Name:  Re-Trigger Delay State
          
          Description:   Determines whether the re-trigger delay state is
                         turned ON or OFF:
          
                         TRUE = Turn re-trigger delay on
                         FALSE = Turn re-trigger delay off

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_re_trigger_time

      ViStatus ri3152b_set_re_trigger_time (ViSession instrHandle,
                                            ViReal64 reTriggerTime);

  Purpose

      Panel Name:    Set Re-Trigger Time
      
      Description:   Programs the re-trigger period.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      reTriggerTime

          Variable Type       ViReal64

          Control Name:  Re-Trigger Time
          
          Description:   Programs the re-trigger period.
          
          Valid Range:   100.0E-9 to 20
          
          Default:       100.0E-9

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_ref_clock_source

      ViStatus ri3152b_set_ref_clock_source (ViSession instrHandle,
                                             ViInt16 refClockSource);

  Purpose

      Panel Name:    Set Ref Clock Source
      
      Description:   Will select the reference source for the sample
                     clock generator.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      refClockSource

          Variable Type       ViInt16

          Control Name:  Ref Clock Source
          
          Description:   Pass the reference clock source you want the
                         function generator to use.
          
          Valid Range:
           RI3152B_REF_SOURCE_INT - internal clock source
           RI3152B_REF_SOURCE_EXT - external clock source
           RI3152B_REF_SOURCE_CLK10 - clk10 clock source
          
          Default: RI3152B_REF_SOURCE_INT (Modern Mode)
                   RI3152B_REF_SOURCE_CLK10 (Legacy Mode)
          
          
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sequence_sync_type

      ViStatus ri3152b_set_sequence_sync_type (ViSession instrHandle,
                                               ViInt16 syncType);

  Purpose

      Panel Name:    Set Sequence Type
      
      Description:   Selects one of the sync types: BIT or  LCOM when
                     instrument is in sequence mode.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      syncType

          Variable Type       ViInt16

          Control Name : Sync Type
          
          Description :  Selects one of the sync types: BIT or  LCOM when
                         instrument is in sequence mode.
          
          Valid Range :  RI3152B_SEQ_SYNC_LCOM - (0)
                         RI3152B_SEQ_SYNC_BIT  - (1)
          
          Default Value: RI3152B_SEQ_SYNC_LCOM
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_shared_flag_on_off

      ViStatus ri3152b_set_shared_flag_on_off (ViSession instrHandle,
                                               ViBoolean sharedFlag);

  Purpose

      Panel Name:    Set Shared Flag ON/OFF
      
      Description:   Turns the shared flag ON or OFF.
                     If shared flag is ON - download waveforms
                     will be during shared memory.
                     If shared flag is OFF - download waveforms
                     will be during trace data

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sharedFlag

          Variable Type       ViBoolean

          Control Name:  Shared Flag
          
          Description:   Turns the shared flag On or Off.
          
          Valid Range:   1 (ON) -  Turns the shared flag On
                         0 (OFF) - Turns the shared flag Off
          
          Default:       ON

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sinc_ncycle

      ViStatus ri3152b_set_sinc_ncycle (ViSession instrHandle,
                                        ViInt16 NCycle);

  Purpose

      Panel Name:    Set Sinc Cycle
      
      Description:   Sets Sinc Cycle.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      NCycle

          Variable Type       ViInt16

          Control Name:  NCycle
          
          Description:   Set number of cycles for the sinc wave.
          
          Valid Range:   4 to 100 cycles
          
          Default:       10
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sine_wave_phase

      ViStatus ri3152b_set_sine_wave_phase (ViSession instrHandle,
                                            ViInt16 phase);

  Purpose

      Panel Name:    Set Sine Wave Phase
      
      Description:   Sets Sine Phase.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      phase

          Variable Type       ViInt16

          Control Name:  Phase
          
          Description:   Selects the starting phase of the Sine Wave.
          
          Valid Range:   0 to 360 degrees
          
          Default:       0 degrees

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sine_wave_power

      ViStatus ri3152b_set_sine_wave_power (ViSession instrHandle,
                                            ViInt16 power);

  Purpose

      Panel Name:    Set Sine Wave Power
      
      Description:   Sets Sine Power.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      power

          Variable Type       ViInt16

          Control Name:  Power
          
          Description:   Selects the sine power.
          
          Valid Range:   1 to 9
          
          Default:       1

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_square_dcycle

      ViStatus ri3152b_set_square_dcycle (ViSession instrHandle,
                                          ViInt16 dutyCycle);

  Purpose

      Panel Name:    Square Duty Cycle
      
      Description:   Sets square duty cycle.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      dutyCycle

          Variable Type       ViInt16

          Control Name:  Duty Cycle
          
          Description:   Set the duty cycle.
          
          Valid Range:
           0 to 99.99  (3152B)
           1% to 99.9%   (3152A)
          
          Default: 50

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_SRE

      ViStatus ri3152b_set_SRE (ViSession instrHandle, ViInt16 SRERegister);

  Purpose

      Panel Name:    Set SRE register
      
      Description:   Programs the Service Request Enable (SRE)
                     register.  This register enables bits of the
                     Status Byte to generate a Service Request
                     (SRQ if GPIB/VXI or VXI Request True Event)
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      SRERegister

          Variable Type       ViInt16

          Control Name:      SRE Register
          
          Description:       Holds the SRE register value.  Each bit
                             enables the corresponding bit in the
                             Status Byte:
          
                      Bit #     Meaning
                      ----------------------------------
                        0       Not used, always 0
                        1       Not used, always 0
                        2       Not used, always 0
                        3       Not used, always 0
                        4       Message Available (MAV).  The device
                                has output which may be read by the
                                system controller
                        5       Standard Event Status Bit (ESB).  One or
                                more of the enabled Standard Event Status
                                Register bits are set.  For an explanation
                                of the Standard Event Status Register, see
                                the description for "ri3152b_read_ESR"
                        6       This is the MSS bit.  Thus, the ESE
                                register should NOT have this bit set.
                        7       Not used, always 0
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sweep_direction

      ViStatus ri3152b_set_sweep_direction (ViSession instrHandle,
                                            ViInt16 sweepDirection);

  Purpose

      Panel Name:    Set Sweep Direction
      
      Description:   Set the sweep direction to up or down.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sweepDirection

          Variable Type       ViInt16

          Control Name:  Sweep Direction
          
          Description:   Set the sweep direction.
          
          Valid Range:
           RI3152B_SWEEP_DIR_UP (0)   - Sweep from start to stop
                                        frequency.
          
           RI3152B_SWEEP_DIR_DOWN (1) - Sweep from stop to start
                                        frequency.
          
          Default:  RI3152B_SWEEP_DIR_UP

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sweep_freq_marker

      ViStatus ri3152b_set_sweep_freq_marker (ViSession instrHandle,
                                              ViReal64 sweepFreqMarker);

  Purpose

      Panel Name:    Set Sweep Frequency Marker
      
      Description:   Set the sweep frequency marker.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          
           Variable Type: ViSession

      sweepFreqMarker

          Variable Type       ViReal64

          Control Name:  Sweep Freq Marker
          
          Description:   Holds the sweep frequency marker.
          
          Valid Range:
           10.0 to 100.0E6 (3152B)
           10E-3 to 10E6   (3152A)
          
          Default: 505.0E3
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sweep_freq_raster

      ViStatus ri3152b_set_sweep_freq_raster (ViSession instrHandle,
                                              ViReal64 sweepFreqRaster);

  Purpose

      Panel Name:    Set Sweep Frequency Raster
      
      Description:   Set the sweep frequency raster.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          
           Variable Type: ViSession

      sweepFreqRaster

          Variable Type       ViReal64

          Control Name:  Sweep Freq Raster
          
          Description:   Holds the sweep frequency raster.
          
          Valid Range:
           10.0E-6 to 250.0E6  (3152B)
           100E-3 to 100E6     (3152A)
          
          Default: 1.0E6
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sweep_freq_start

      ViStatus ri3152b_set_sweep_freq_start (ViSession instrHandle,
                                             ViReal64 sweepFreqStart);

  Purpose

      Panel Name:    Set Sweep Frequency Start
      
      Description:   Set the sweep frequency start.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          
           Variable Type: ViSession

      sweepFreqStart

          Variable Type       ViReal64

          Control Name:  Sweep Freq Start
          
          Description:   Holds the sweep frequency start.
          
          Valid Range:
           10.0 to 100.0E6  (3152B)
           1E-3 to 9E6      (3152A)
          
          Default:
           10.0E3  (3152B)
           100.0E3 (3152A)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sweep_freq_stop

      ViStatus ri3152b_set_sweep_freq_stop (ViSession instrHandle,
                                            ViReal64 sweepFreqStop);

  Purpose

      Panel Name:    Set Sweep Frequency Stop
      
      Description:   Set the sweep frequency stop.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          
           Variable Type: ViSession

      sweepFreqStop

          Variable Type       ViReal64

          Control Name:  Sweep Freq Stop
          
          Description:   Holds the sweep frequency stop.
          
          Valid Range:
           10.0 to 100.0E6 (3152B)
           10E-3 to 10E6   (3152A)
          
          Default: 1.0E6
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sweep_spacing

      ViStatus ri3152b_set_sweep_spacing (ViSession instrHandle,
                                          ViInt16 sweepSpacing);

  Purpose

      Panel Name:    Set Sweep Spacing
      
      Description:   Set the sweep spacing to LOG or LIN.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sweepSpacing

          Variable Type       ViInt16

          Control Name:  Sweep Spacing
          
          Description:   Set the sweep spacing.
          
          Valid Range:
           RI3152B_SWEEP_SPAC_LIN (0) - Output frequency is sweep linearly
                                        between the start frequency and
                                        the stop frequency.
          
           RI3152B_SWEEP_SPAC_LOG (1) - Output frequency is sweep
                                        logarithmically between the start
                                        frequency and the stop frequency.
          
          Default:  RI3152B_SWEEP_SPAC_LIN
          
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sweep_step

      ViStatus ri3152b_set_sweep_step (ViSession instrHandle,
                                       ViInt16 sweepStep);

  Purpose

      Panel Name:   Set Sweep Step
      
      Description:  Set the sweep step.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          
           Variable Type: ViSession

      sweepStep

          Variable Type       ViInt16

          Control Name:  Sweep Step
          
          Description:   Holds the sweep step.
          
          Valid Range:
           10 to 2000  (3152B)
           10 to 1000  (3152A)
          
          Default: 20
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_sweep_time

      ViStatus ri3152b_set_sweep_time (ViSession instrHandle,
                                       ViReal64 sweepTime);

  Purpose

      Panel Name:   Set Sweep Time
      
      Description:  Set the sweep time.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          
           Variable Type: ViSession

      sweepTime

          Variable Type       ViReal64

          Control Name:  Sweep Time
          
          Description:   Holds the sweep time.
          
          Valid Range:
           1.4E-6 to 40.0  (3152B)
           100E-3 to 10E3  (3152A)
          
          Default: 1.0E-3
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_triangle_phase

      ViStatus ri3152b_set_triangle_phase (ViSession instrHandle,
                                           ViInt16 phase);

  Purpose

      Panel Name:    Set Triangle Phase
      
      Description:   Sets Triangle Phase.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      phase

          Variable Type       ViInt16

          Control Name:  Phase
          
          Description:   Selects the starting phase of the Triangle Wave.
          
          Valid Range:   0 to 360 degrees
          
          Default:       0 degrees

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_triangle_power

      ViStatus ri3152b_set_triangle_power (ViSession instrHandle,
                                           ViInt16 power);

  Purpose

      Panel Name:    Set Triangle Power
      
      Description:   Sets Triangle Power.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      power

          Variable Type       ViInt16

          Control Name:  Power
          
          Description:   Selects the starting power of the Triangle Wave.
          
          Valid Range:   1 to 9

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_trigger_delay_time

      ViStatus ri3152b_set_trigger_delay_time (ViSession instrHandle,
                                               ViReal64 triggerDelayTime);

  Purpose

      Panel Name:    Set Trigger Delay Time
      
      Description:   Sets the time that will elapse from a valid
                     trigger signal to the initiation of the first
                     output waveform.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      triggerDelayTime

          Variable Type       ViReal64

          Control Name:  Trigger Delay Time
          
          Description:   Pass the time that will elapse from a valid
                         trigger signal to the initiation of the first
                         output waveform.
          
          Valid Range:   100.0e-9 - 20.0
          
          Default:       100.0E-9

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_trigger_level

      ViStatus ri3152b_set_trigger_level (ViSession instrHandle,
                                          ViReal64 triggerLevel);

  Purpose

      Panel Name:    Set Trigger Level
      
      Description:   Sets the threshold level at the trigger input
                     connector.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      triggerLevel

          Variable Type       ViReal64

          Control Name:  Trigger Level
          
          Description:   Contains the trigger level value.
          
          Valid Range:   -10 V to 10 V
          
          Default:
           0.0 (Legacy Mode)
           1.6 (Modern Mode)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_TTLT0_On_Off

      ViStatus ri3152b_set_TTLT0_On_Off (ViSession instrHandle,
                                         ViBoolean TTLT0);

  Purpose

      Panel Name:    Set TTLT0 ON/OFF
      
      Description:   Turns the output TTLT0 ON or OFF.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      TTLT0

          Variable Type       ViBoolean

          Control Name:  TTLT0
          
          Description:   Turns the output TTLT0 On or Off.
          
          Valid Range:   0 (OFF) - 1 (ON)
          
          Default:       OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_TTLT1_On_Off

      ViStatus ri3152b_set_TTLT1_On_Off (ViSession instrHandle,
                                         ViBoolean TTLT1);

  Purpose

      Panel Name:    Set TTLT1 ON/OFF
      
      Description:   Turns the output TTLT1 ON or OFF.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      TTLT1

          Variable Type       ViBoolean

          Control Name:  TTLT1
          
          Description:   Turns the output TTLT1 On or Off.
          
          Valid Range:   0 (OFF) - 1 (ON)
          
          Default:       OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_TTLT2_On_Off

      ViStatus ri3152b_set_TTLT2_On_Off (ViSession instrHandle,
                                         ViBoolean TTLT2);

  Purpose

      Panel Name:    Set TTLT2 ON/OFF
      
      Description:   Turns the output TTLT2 ON or OFF.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      TTLT2

          Variable Type       ViBoolean

          Control Name:  TTLT2
          
          Description:   Turns the output TTLT2 On or Off.
          
          Valid Range:   0 (OFF) - 1 (ON)
          
          Default:       OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_TTLT3_On_Off

      ViStatus ri3152b_set_TTLT3_On_Off (ViSession instrHandle,
                                         ViBoolean TTLT3);

  Purpose

      Panel Name:    Set TTLT3 ON/OFF
      
      Description:   Turns the output TTLT3 ON or OFF.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      TTLT3

          Variable Type       ViBoolean

          Control Name:  TTLT3
          
          Description:   Turns the output TTLT3 On or Off.
          
          Valid Range:   0 (OFF) - 1 (ON)
          
          Default:       OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_TTLT4_On_Off

      ViStatus ri3152b_set_TTLT4_On_Off (ViSession instrHandle,
                                         ViBoolean TTLT4);

  Purpose

      Panel Name:    Set TTLT4 ON/OFF
      
      Description:   Turns the output TTLT4 ON or OFF.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      TTLT4

          Variable Type       ViBoolean

          Control Name:  TTLT4
          
          Description:   Turns the output TTLT4 On or Off.
          
          Valid Range:   0 (OFF) - 1 (ON)
          
          Default:       OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_TTLT5_On_Off

      ViStatus ri3152b_set_TTLT5_On_Off (ViSession instrHandle,
                                         ViBoolean TTLT5);

  Purpose

      Panel Name:    Set TTLT5 ON/OFF
      
      Description:   Turns the output TTLT5 ON or OFF.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      TTLT5

          Variable Type       ViBoolean

          Control Name:  TTLT5
          
          Description:   Turns the output TTLT5 On or Off.
          
          Valid Range:   0 (OFF) - 1 (ON)
          
          Default:       OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_TTLT6_On_Off

      ViStatus ri3152b_set_TTLT6_On_Off (ViSession instrHandle,
                                         ViBoolean TTLT6);

  Purpose

      Panel Name:    Set TTLT6 ON/OFF
      
      Description:   Turns the output TTLT6 ON or OFF.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      TTLT6

          Variable Type       ViBoolean

          Control Name:  TTLT6
          
          Description:   Turns the output TTLT6 On or Off.
          
          Valid Range:   0 (OFF) - 1 (ON)
          
          Default:       OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_TTLT7_On_Off

      ViStatus ri3152b_set_TTLT7_On_Off (ViSession instrHandle,
                                         ViBoolean TTLT7);

  Purpose

      Panel Name:    Set TTLT7 ON/OFF
      
      Description:   Turns the output TTLT7 ON or OFF.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      TTLT7

          Variable Type       ViBoolean

          Control Name:  TTLT7
          
          Description:   Turns the output TTLT7 On or Off.
          
          Valid Range:   0 (OFF) - 1 (ON)
          
          Default:       OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_set_wave_resolution

      ViStatus ri3152b_set_wave_resolution (ViSession instrHandle,
                                            ViInt16 waveResolution);

  Purpose

      Panel Name:    Set Wave Resolution
      
      Description:   Sets resolution for the waveform.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      waveResolution

          Variable Type       ViInt16

          Control Name:  Wave Resolution
          
          Description:   Sets resolution for the waveform.
          
          Valid Range:   RI3152B_WAVE_RES_16BIT - (1)
                         RI3152B_WAVE_RES_12BIT - (0)
          
          
          Default:       RI3152B_WAVE_RES_12BIT

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_share_memory_query

      ViStatus ri3152b_share_memory_query (ViSession instrHandle,
                                           ViPInt16 shareMemoryMode,
                                           ViPInt16 shareMemoryState);

  Purpose

      Panel Name:    Query Share memory
      
      Description:   Returns the value of Share Memory mode and
                     Share Memory State.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      shareMemoryMode

          Variable Type       ViInt16 (passed by reference)

          Control Name:   Share Memory Mode
          
          Description:    Returns the value of Share Memory mode
          
          Valid Range:    0 = Read
                          1 = Write

      shareMemoryState

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Share Memory State
          
          Description:   Returns the value of Share Memory State.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sinc_ncycle_query

      ViStatus ri3152b_sinc_ncycle_query (ViSession instrHandle,
                                          ViPInt16 sincNCycle);

  Purpose

      Panel Name:  Query Sinc NCycle
      
      Description: Returns the current sinc number of cycles value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sincNCycle

          Variable Type       ViInt16 (passed by reference)

          Control Name:   Sinc NCycle
          
          Description:    Holds the current sinc number of cycles.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sinc_wave

      ViStatus ri3152b_sinc_wave (ViSession instrHandle, ViReal64 frequency,
                                  ViReal64 amplitude, ViReal64 offset,
                                  ViInt16 number_ofCycles);

  Purpose

      Panel Name:    Sinc Wave
      
      Description:   Select Frequency, Amplitude, Offset and the
                     Number of Cycles.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequency

          Variable Type       ViReal64

          Control Name:  Frequency
          
          Description:   Selects the frequency of the Sinc Wave.
          
          Valid Range:
          10.0E-3 to 100.0E6   (3152B)
          100.0E-6 to 1.0E6    (3152A)
          
          Default:  1.0E6
          
          
          

      amplitude

          Variable Type       ViReal64

          Control Name:  Amplitude
          
          Description:   Selects the amplitude (Volts peak-to-peak).
          
          Valid Range:   10.0E-3 to 16.0 V
          
          Default:       5.0 V
          
          Note:          The following relationship must hold between the
                         amplitude and the offset:
          
                               (Amplitude / 2) + ABS(offset) <= 8.0

      offset

          Variable Type       ViReal64

          Control Name:  Offset
          
          Description:   Programs the offset for the presently
                         selected waveform.
          
          Valid Range:
          -7.992 to +7.992   (3152B)
          -7.19 to +7.19     (3152A)
          
          Default:  0.0

      number_ofCycles

          Variable Type       ViInt16

          Control Name:  Number of Cycles
          
          Description:   Select the number of cycles for the Sinc Wave.
          
          Valid Range:   4 to 100
          
          Default:       10

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sine_phase_query

      ViStatus ri3152b_sine_phase_query (ViSession instrHandle,
                                         ViPInt16 sinePhase);

  Purpose

      Panel Name:    Query Sine Phase
      
      Description:   Returns the current sine phase value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sinePhase

          Variable Type       ViInt16 (passed by reference)

          Control Name:   Sine Phase
          
          Description:    Reads the current sine phase value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sine_power_query

      ViStatus ri3152b_sine_power_query (ViSession instrHandle,
                                         ViPInt16 sinePower);

  Purpose

      Panel Name:    Query Sine Power
      
      Description:   Returns the current sine power value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sinePower

          Variable Type       ViInt16 (passed by reference)

          Control Name:   Sine Power
          
          Description:    Reads the current sine power value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sine_wave

      ViStatus ri3152b_sine_wave (ViSession instrHandle, ViReal64 frequency,
                                  ViReal64 amplitude, ViReal64 offset,
                                  ViInt16 phase, ViInt16 power_Sinex);

  Purpose

      Panel Name:    Sine Wave
      
      Description:   select Frequency, Amplitude, Offset, Phase
                     and the Power of sine(x).

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequency

          Variable Type       ViReal64

          Control Name:  Frequency
          
          Description:   Selects the frequency of the Sine Wave.
          
          Valid Range:
          10.0E-3 to 100.0E6 (3152B)
          100.0E-6 to 50.0E6 (3152A)
          
          Default:
          1.0E6   (3152B)
          100.0E3 (3152A)
          
          
          

      amplitude

          Variable Type       ViReal64

          Control Name:  Amplitude
          
          Description:   Selects the amplitude (Volts peak-to-peak).
          
          Valid Range:   10.0E-3 to 16.0 V
          
          Default:       5.0 V
          
          Note:          The following relationship must hold between the
                          amplitude and the offset:
          
                               (Amplitude / 2) + ABS(offset) <= 8.0

      offset

          Variable Type       ViReal64

          Control Name:  Offset
          
          Description:   Programs the offset for the presently
                         selected waveform.
          
          Valid Range:
          -7.992 to +7.992   (3152B)
          -7.19 to +7.19     (3152A)
          
          Default:  0.0

      phase

          Variable Type       ViInt16

          Control Name:  Phase
          
          Description:   Selects the starting phase of the Sine Wave.
          
          Valid Range:   0 to 360 degrees
          
          Default:       0 degrees

      power_Sinex

          Variable Type       ViInt16

          Control Name:   Power (Sine^x)
          
          Description:    Selects the exponentional power of the Sine
                          function (sine^1, sine^2, sine^3, ..., sine^9).
          
          Valid Range:    1 to 9  (RI3152B_POWER_1 to RI3152B_POWER_9)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_square_dcycle_query

      ViStatus ri3152b_square_dcycle_query (ViSession instrHandle,
                                            ViPInt16 dutyCycle);

  Purpose

      Panel Name:    Query Square Duty Cycle
      
      Description:   Returns the current square duty cycle.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      dutyCycle

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Duty Cycle
          
          Description:   Reads the current square duty cycle.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_square_wave

      ViStatus ri3152b_square_wave (ViSession instrHandle, ViReal64 frequency,
                                    ViReal64 amplitude, ViReal64 offset,
                                    ViInt16 dutyCycle);

  Purpose

      Panel Name:    Square Wave
      
      Description:   Select Frequency, Amplitude, Offset and
                     Duty Cycle.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequency

          Variable Type       ViReal64

          Control Name:  Frequency
          
          Description:   Selects the frequency of the Square Wave.
          
          Valid Range:
          10.0E-3 to 100.0E6  (3152B)
          100.0E-6 to 50.0E6  (3152A)
          
          Default:  1.0E6
          
          
          
          
          

      amplitude

          Variable Type       ViReal64

          Control Name:  Amplitude
          
          Description:   Selects the amplitude (Volts peak-to-peak).
          
          Valid Range:   10.0E-3 to 16.0 V
          
          Default:       5.0 V
          
          Note:          The following relationship must hold between the
                         amplitude and the offset:
          
                           (Amplitude / 2) + ABS(offset) <= 8.0

      offset

          Variable Type       ViReal64

          Control Name:  Offset
          
          Description:   Programs the offset for the presently
                         selected waveform.
          
          Valid Range:
          -7.992 to +7.992   (3152B)
          -7.19 to +7.19     (3152A)
          
          Default:  0.0

      dutyCycle

          Variable Type       ViInt16

          Control Name:  Duty Cycle
          
          Description:   Selects the percentage of Duty Cycle.
          
          Valid Range:
           0 to 99.99  (3152B)
           1% to 99.9%   (3152A)
          
          Default:  50%
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_status_query

      ViStatus ri3152b_status_query (ViSession instrHandle,
                                     ViPReal64 voltage_value,
                                     ViPReal64 frequency_value,
                                     ViPReal64 offset_value,
                                     ViPInt16 filter_type);

  Purpose

      Panel Name:    Status Query
      
      Description:   Returns the current value of the Voltage,
                     Frequency, Offset and Filter.
      
                     Note: value returned for Filter type is
                           described in the following table.
      
                        Value              Representation
                        ------             --------------
                          0                 All filters are off
                          1                 20MHz filter ON
                          2                 25MHz filter ON
                          3                 50MHz filter ON
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      voltage_value

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Voltage value
          
          Description:    Reads the current voltage amplitude
                          (Volts peak-to-peak).
          

      frequency_value

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Frequency value
          
          Description:    Reads the current frequency value.
          

      offset_value

          Variable Type       ViReal64 (passed by reference)

          Control Name:   Offset value
          
          Description:    Reads the current offset value.
          

      filter_type

          Variable Type       ViInt16 (passed by reference)

          Control Name:   Filter type
          
          Description:    Reads the current Filter type.
          
                          Note: value returned for Filter type is
                          described in the following table.
          
                          Value              Representation
                          ------             --------------
                           0                 All filters are off
                           1                 20MHz filter ON
                           2                 25MHz filter ON
                           3                 50MHz filter ON
                           4                 60MHz filter ON
                           5                 120MHz filter ON
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sweep_func_query

      ViStatus ri3152b_sweep_func_query (ViSession instrHandle,
                                         ViPInt16 presentSweepFunction);

  Purpose

      Panel Name:    Query Sweep Function
      
      Description:   Returns the current sweep function.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      presentSweepFunction

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Present Sweep Function
          
          Description:   Reads the present sweep function.
          
          Valid Range:   RI3152B_SINE     - (1)
                         RI3152B_TRIANGLE - (2)
                         RI3152B_SQUARE   - (3)
          
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sweep_function

      ViStatus ri3152b_sweep_function (ViSession instrHandle,
                                       ViInt16 sweepMode);

  Purpose

      Panel Name:  Sweep Function
      
      Description: Selects one of the sweep function: Sine, Triangle
                   or Square.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      sweepMode

          Variable Type       ViInt16

          Control Name:  Sweep Mode
          
          Description:   Selects a sweep mode.
          
          Valid Range:   RI3152B_SINE     - (1)
                         RI3152B_TRIANGLE - (2)
                         RI3152B_SQUARE   - (3)
          
          Default:       RI3152B_SINE

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sync_pos_query

      ViStatus ri3152b_sync_pos_query (ViSession instrHandle,
                                       ViPInt32 syncPosition);

  Purpose

      Panel Name:    Sync Position Query
      
      Description:   Returns the current value of the output sync
                     position.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      syncPosition

          Variable Type       ViInt32 (passed by reference)

          Control Name:  Sync Position
          
          Description:   Reads the current output sync position.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sync_source_query

      ViStatus ri3152b_sync_source_query (ViSession instrHandle,
                                          ViChar _VI_FAR syncSource[]);

  Purpose

      Panel Name:    Sync Source Query
      
      Description:   Returns the current value of the output sync
                     source.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      syncSource

          Variable Type       ViChar []

          Control Name:  Sync Source
          
          Description:   Reads the current output sync source.
          
          Valid Range:   RI3152B_SYNC_OFF       - (0)
                         RI3152B_SYNC_BIT       - (1)
                         RI3152B_SYNC_LCOM      - (2)
                         RI3152B_SYNC_SSYN      - (3)
                         RI3152B_SYNC_HCLOCK    - (4)
                         RI3152B_SYNC_PULSE     - (5)
                         RI3152B_SYNC_ZEROCROSS - (6)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_sync_width_query

      ViStatus ri3152b_sync_width_query (ViSession instrHandle,
                                         ViPInt32 syncWidth);

  Purpose

      Panel Name:    Sync Width Query
      
      Description:   Returns the current value of the output sync
                     width.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      syncWidth

          Variable Type       ViInt32 (passed by reference)

          Control Name:  Sync Width
          
          Description:   Reads the current output sync width.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_triangle_phase_query

      ViStatus ri3152b_triangle_phase_query (ViSession instrHandle,
                                             ViPInt16 trianglePhase);

  Purpose

      Panel Name:    Query Triangle Phase
      
      Description:   Returns the current triangle phase value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      trianglePhase

          Variable Type       ViInt16 (passed by reference)

          Control Name:      Triangle Phase
          
          Description:       Reads the current triangle phase value

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_triangle_power_query

      ViStatus ri3152b_triangle_power_query (ViSession instrHandle,
                                             ViPInt16 trianglePower);

  Purpose

      Panel Name:    Query Triangle Power
      
      Description:   Returns the current triangle power value.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      trianglePower

          Variable Type       ViInt16 (passed by reference)

          Control Name:  Triangle Power
          
          Description:   Reads the current triangle power value.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_triangular_wave

      ViStatus ri3152b_triangular_wave (ViSession instrHandle,
                                        ViReal64 frequency,
                                        ViReal64 amplitude, ViReal64 offset,
                                        ViInt16 phase,
                                        ViInt16 power_Triangularx);

  Purpose

      Panel Name:    Triangular Wave
      
      Description:   Select Frequency, Amplitude, Offset, Phase
                     and the Power of triangle(x).

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      frequency

          Variable Type       ViReal64

          Control Name:  Frequency
          
          Description:   Selects the frequency of the Triangular Wave.
          
          Valid Range:
          10.0E-3 to 100.0E6 (3152B)
          100.0E-6 to 1.0E6  (3152A)
          
          Default:  1.0E6
          
          
          

      amplitude

          Variable Type       ViReal64

          Control Name:  Amplitude
          
          Description:   Selects the amplitude (Volts peak-to-peak).
          
          Valid Range:   10.0E-3 to 16.0 V
          
          Default:       5.0 V
          
          Note:          The following relationship must hold between the
                         amplitude and the offset:
          
                               (Amplitude / 2) + ABS(offset) <= 8.0

      offset

          Variable Type       ViReal64

          Control Name:  Offset
          
          Description:   Programs the offset for the presently
                         selected waveform.
          
          Valid Range:
          -7.992 to +7.992   (3152B)
          -7.19 to +7.19     (3152A)
          
          Default:   0.0

      phase

          Variable Type       ViInt16

          Control Name:  Phase
          
          Description:   Selects the starting phase of the Triangle Wave.
          
          Valid Range:   0 to 360 degrees
          
          Default:       0 degrees

      power_Triangularx

          Variable Type       ViInt16

          Control Name:  Power
          
          Description:   Selects the exponentional power of the triangle
                         function (triangle^1, triangle^2, ...triangle^9).
          
          Valid Range:   1 to 9  (RI3152B_POWER_1 to RI3152B_POWER_9)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trig_delay_state

      ViStatus ri3152b_trig_delay_state (ViSession instrHandle,
                                         ViBoolean triggerDelayState);

  Purpose

      Panel Name:    Set Trigger Delay State
      
      Description:   Enable/disable trigger delay feature.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      triggerDelayState

          Variable Type       ViBoolean

          Control Name:  Trigger Delay State
          
          Description:   Determines whether the trigger delay state is
                         turned ON or OFF:
          
                         TRUE = Turn trigger delay on
                         FALSE = Turn trigger delay off
          
          Default:       OFF

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trig_delay_state_query

      ViStatus ri3152b_trig_delay_state_query (ViSession instrHandle,
                                               ViPInt16 trigDelayState);

  Purpose

      Panel Name:    Trigger Delay State Query
      
      Description:   Reads the state of trigger delay.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      trigDelayState

          Variable Type       ViInt16 (passed by reference)

          Control Name:   Trig Delay State
          
          Description:    Returns the trigger delay state.
          
                          0 = Trigger is OFF
                          1 = Trigger is ON

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trigger

      ViStatus ri3152b_trigger (ViSession instrHandle);

  Purpose

      Panel Name:    Trigger instrument
      
      Description:   Sends a trigger to the device.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trigger_delay

      ViStatus ri3152b_trigger_delay (ViSession instrHandle,
                                      ViInt32 triggerDelay);

  Purpose

      Panel Name:    Trigger Delay
      
      Description:   Selects the delay between receipt of the trigger
                     and the triggered action performed by the
                     instrument waveform generator.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.

      triggerDelay

          Variable Type       ViInt32

          Control Name:  Trigger Delay
          
          Description:   Selects a Trigger Delay.  This value is
                         the number of sample points to delay after
                         receipt of the trigger.  For example, with
                         a trigger delay of 10 and a sample clock
                         frequency of 100 MHz, a 100 nanosecond delay
                         is programmed.
          
          Valid Range:   0, 10 to 2E6
          
          Default:       0

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trigger_delay_query

      ViStatus ri3152b_trigger_delay_query (ViSession instrHandle,
                                            ViPInt32 trigger_delay_value);

  Purpose

      Panel Name:    Trigger Delay Query
      
      Description:   Returns the current value of the trigger delay.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      trigger_delay_value

          Variable Type       ViInt32 (passed by reference)

          Control Name:  Trigger delay value
          
          Description:   Reads the current trigger delay.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trigger_level_query

      ViStatus ri3152b_trigger_level_query (ViSession instrHandle,
                                            ViPReal64 trigger_level_value);

  Purpose

      Panel Name:    Trigger Level Query
      
      Description:   Returns the current value of the trigger level in
                     volts.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      trigger_level_value

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Trigger level value
          
          Description:   Reads the current trigger level in volt.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trigger_rate

      ViStatus ri3152b_trigger_rate (ViSession instrHandle,
                                     ViReal64 triggerPeriod);

  Purpose

      Panel Name:    Internal Trigger Rate
      
      Description:   Selects the trigger rate.  This is meaningful
                     only when the Internal Trigger is selected.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      triggerPeriod

          Variable Type       ViReal64

          Control Name:  Trigger Period
          
          Description:   Select a Trigger Period (if the instrument is
                         Internal mode only.)
          
          Valid Range:
           1.0E-6 to 20.0 seconds  (3152B)
           60E-6 to 1000 seconds   (3152A)
          
          Default:
           15.0E-6    (3152B)
           1.0E-4     (3152A)

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trigger_rate_query

      ViStatus ri3152b_trigger_rate_query (ViSession instrHandle,
                                           ViPReal64 triggerPeriod);

  Purpose

      Panel Name:    Trigger Rate Query
      
      Description:   Returns the current value of the trigger rate in
                     seconds.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      triggerPeriod

          Variable Type       ViReal64 (passed by reference)

          Control Name:  Trigger Period
          
          Description:   Reads the current trigger period in seconds.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trigger_slope

      ViStatus ri3152b_trigger_slope (ViSession instrHandle,
                                      ViInt16 triggerSlope);

  Purpose

      Panel Name:    External Trigger Slope
      
      Description:   Selects the active edge of the External Trigger.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      triggerSlope

          Variable Type       ViInt16

          Control Name:  Trigger Slope
          
          Description:   This parameter configures the instrument to
                         trigger on either the rising or falling edge of
                         an external trigger.
          
          Valid Range:
           RI3152B_SLOPE_POS (1) - Positive Slope
           RI3152B_SLOPE_NEG (0) - Negative Slope
          
          Default:   RI3152B_SLOPE_POS

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trigger_slope_query

      ViStatus ri3152b_trigger_slope_query (ViSession instrHandle,
                                            ViPInt16 triggerSlope);

  Purpose

      Panel Name:    Trigger Slope Query
      
      Description:   Returns the current trigger slope.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      triggerSlope

          Variable Type       ViInt16 (passed by reference)

          Control Name:      Trigger Slope
          
          Description:       Reads the current trigger slope.
          
          Valid Range:
           RI3152B_SLOPE_POS (1) - Positive Slope
           RI3152B_SLOPE_NEG (0) - Negative Slope
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trigger_source

      ViStatus ri3152b_trigger_source (ViSession instrHandle,
                                       ViInt16 triggerSource);

  Purpose

      Panel Name:    Trigger Source
      
      Description:   Selects the input trigger source.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      triggerSource

          Variable Type       ViInt16

          Control Name:  Trigger Source
          
          Description:   Select one of the Trigger Sources: Internal
                         External, or one of the TTLTRG lines on the
                         VXI backplane.
          
          Valid Range:    RI3152B_TRIGGER_INTERNAL     - 0
                          RI3152B_TRIGGER_EXTERNAL     - 1
                          RI3152B_TRIGGER_TTLTRG0      - 2
                          RI3152B_TRIGGER_TTLTRG1      - 3
                          RI3152B_TRIGGER_TTLTRG2      - 4
                          RI3152B_TRIGGER_TTLTRG3      - 5
                          RI3152B_TRIGGER_TTLTRG4      - 6
                          RI3152B_TRIGGER_TTLTRG5      - 7
                          RI3152B_TRIGGER_TTLTRG6      - 8
                          RI3152B_TRIGGER_TTLTRG7      - 9
                          RI3152B_TRIGGER_ECLTRG1      - 11
                          RI3152B_TRIGGER_BUS          - 12
          
          Default:        RI3152B_TRIGGER_EXTERNAL

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trigger_source_adv_query

      ViStatus ri3152b_trigger_source_adv_query (ViSession instrHandle,
                                                 ViPInt16 triggerSource);

  Purpose

      Panel Name:    Trigger Source Advance Query
      
      Description:   Returns the current trigger source for the
                     instrument.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      triggerSource

          Variable Type       ViInt16 (passed by reference)

          Control Name:   Trigger Source
          
          Description:    Reads the presently selected
                          trigger source.
          
          Valid Range:    RI3152B_TRIGGER_INTERNAL  - (0)
                          RI3152B_TRIGGER_EXTERNAL  - (1)
                          RI3152B_TRIGGER_TTLTRG0   - (2)
                          RI3152B_TRIGGER_TTLTRG1   - (3)
                          RI3152B_TRIGGER_TTLTRG2   - (4)
                          RI3152B_TRIGGER_TTLTRG3   - (5)
                          RI3152B_TRIGGER_TTLTRG4   - (6)
                          RI3152B_TRIGGER_TTLTRG5   - (7)
                          RI3152B_TRIGGER_TTLTRG6   - (8)
                          RI3152B_TRIGGER_TTLTRG7   - (9)
                          RI3152B_TRIGGER_ECLTRG1   - (11)
                          RI3152B_TRIGGER_BUS       - (12)
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_trigger_source_query

      ViStatus ri3152b_trigger_source_query (ViSession instrHandle,
                                             ViChar _VI_FAR triggerSource[]);

  Purpose

      Panel Name:    Trigger Source Query
      
      Description:   Returns the current trigger source.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      triggerSource

          Variable Type       ViChar []

          Control Name:  Trigger Source
          
          Description:   Reads the current trigger source.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_version_query

      ViStatus ri3152b_version_query (ViSession instrHandle,
                                      ViChar _VI_FAR version[]);

  Purpose

      Panel Name:    Version Query
      
      Description:   Queries the instrument's version.

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

      version

          Variable Type       ViChar []

          Control Name:  Version
          
          Description:   Holds the version returned
                         by the instrument.
          

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_WaitForOPC

      ViStatus ri3152b_WaitForOPC (ViSession instrHandle, ViInt32 maxTime);

  Purpose

      Panel Name:    Wait for OPC
      
      Description:   This function waits for Operation Complete.
      

  Parameter List

      instrHandle

          Variable Type       ViSession

           Control Name:  Instr Handle
          
           Description:   The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          
                          Note that the initialize function may
                          be used to associate multiple instrument
                          handles with a single instrument.
          

      maxTime

          Variable Type       ViInt32

          Control Name:      Max Time
          
          Description:       The Maximum about of time in seconds to wait
                             for OPC to goto 1.

  Return Value

           Control Name:  Error
          
           Description:   Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
           VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
           RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          
________________________________________________________________________________

  ri3152b_write_instrument_data

      ViStatus ri3152b_write_instrument_data (ViSession instrHandle,
                                              ViChar _VI_FAR writeBuffer[]);

  Purpose

      Panel Name:    Write Instrument Data
      
      Description:   This function writes a user-specified string to
                     the instrument.
      
      

  Parameter List

      instrHandle

          Variable Type       ViSession

          Control Name:   Instr Handle
          
          Description:    The Instrument Handle is used to
                          identify the unique session or
                          communication channel between the
                          driver and the instrument.
          
                          If more than one instrument of the
                          same model type is used, this
                          Handle will be used to
                          differentiate between them.
          

      writeBuffer

          Variable Type       ViChar []

          Control Name:  Write Buffer
          
          Description:   Pass the string to be written to the instrument.
          

  Return Value

          Control Name:   Error
          
          Description:    Displays the return status of the
                          function call.  If the function was
                          successful, it will return a status
                          of VI_SUCCESS, otherwise it will
                          return an error code.  Passing
                          the error code into the function
                          "ri3152b_error_message" will return
                          a string describing the error.
          
          VTL/VISA Errors:
                  See section 3.3 of the VPP 4.2.2 document
                  for a complete list of the WIN Framework
                  error codes and their values.  Appendix B
                  of VPP 4.2 contains descriptions and a
                  breakdown of the error codes returned by
                  each of the VTL functions.
          
          RI3152B Driver Errors
                  Errors returned from the RI3152B drivers
                  will be between 0xBFFC0800 and 0xBFFC0FFF
                  and are described in the source code for
                  the ri3152b_error_message function.  The
                  ri3152b_error_message function may be used to
                  convert the error code to a meaningful error
                  message.
          

